# Phase 2 Status

Stand: 2026-07-19
Owner: execution session
Zweck: Einzige Wahrheit über den tatsächlichen Fortschritt von Phase 2 (nicht das Zielbild — das steht in `00_phase_2_charter.md`..`05_claude_execution_prompt.md`). Jede abgeschlossene Einheit wird hier evidenzbasiert eingetragen, damit jede Folge- oder frische Sitzung den Stand kennt, ohne den Chatverlauf zu rekonstruieren.

## Aktueller Fokus

Welle 2 (freigegeben 2026-07-22). **Backend P2-D04 `platform_audit_jobs` vollständig `done` (alle vier Increments + voller `database`-Job-Gate lokal grün, siehe P2-D04-Zeile). Nächstes Item = Welle-2-Screens `04b_wave2_...md` (Screens SCR-020/051/052 + neuer Parties-Screen) — das ist eine Wellengrenze: erst das Wellendokument schreiben, und vor dessen Start beim Nutzer rückversichern. Backend P2-D03 `documents_compliance` ist `done` (alle drei Increments + voller `database`-Job-Gate lokal grün, siehe P2-D03-Zeile).** Welle 1 ist vollständig abgeschlossen und committet (alle AP1–AP9 inkl. Golden-Path, siehe Zeilen unten). Backend **P2-D01** `identity_access` Full Expansion ist **`done`** (alle drei Increments + voller `database`-Job-Gate lokal grün): Increment 1 Membership-Lifecycle-Schema/RPCs, Increment 2 Dart-Contract/Adapter, Increment 3 Member-Directory-RPC + Cloud-Admin-UI im Reference Slice + `AuthorizationPort` + Report-only-Reconciliation. Backend **P2-D02** `contacts_parties` ist **`done`** (alle drei Increments + voller `database`-Job-Gate lokal grün, siehe P2-D02-Zeile). Backend **P2-D03** `documents_compliance` ist **`done`** (privater Storage-Bucket, `SignedUrlPort`, löst `DEBT-007`/`DUP-011`). Backend **P2-D04** `platform_audit_jobs` ist **`done`** (alle vier Increments; CTR-005-Event-Envelope/Outbox, Task-/Notification-/ImportJob-Aggregate, abgeleiteter SearchIndex, plus Dart-Contract/Adapter/Realtime-Consumer/Dry-Run-Mapper/Integrationstest/CI). **Welle-2-Detaildokument `04b_wave2_contacts_documents.md` ist geschrieben (`done`, 2026-07-24), die eine offene Architektur-Entscheidung ist per Nutzerbestätigung gefallen (Feature-Contracts backend-gewählt; SQLite-Modus read-only-bis-migriert für Dokument-/Party-Mutationen).** **Welle-2-Umsetzung läuft: Arbeitspaket 0 (Provider-/`main.dart`-Wiring) und Arbeitspaket 1 (PartiesScreen, Pattern-Beweis) sind `done` (2026-07-28, siehe W2-AP0-/W2-AP1-Zeilen).** Zwei Nutzerentscheidungen sind dabei gefallen: die additive Parties-Sidebar-Navigation ist bestätigt, und für den in AP1 aufgedeckten **Cloud-Host-Befund** (`AppScaffold` mountet nur im SQLite-Modus) wurden **additive Cloud-Routen für die W2-Screens** bestätigt — siehe eigene Zeile. Nächstes Item = **ComplianceDashboard (SCR-052, Arbeitspaket 2)**, danach PropertyDocuments (SCR-020) → DocumentsScreen (SCR-051), die letzten beiden per Subagent. Der manuelle Golden-Path aller W2-Screens wird nach W1-Präzedenz gebündelt vor Wellenabschluss gefahren.

## Fortschritt je Einheit

| Einheit | Typ | Status | Evidenz / Notiz |
|---|---|---|---|
| Plan Phase 2 (00–05) | Doku | `done` | `docs/architecture/phase_2/00_phase_2_charter.md`..`05_claude_execution_prompt.md` |
| W0 — Design-Token-Verfeinerung (Light) | UI-Foundation | `done` | Warm-neutrale Light-Tokens in `AppColors` + `_lightTokens` (`lib/ui/theme/app_theme.dart`) + 3 Canvas-Drift-Stellen (`overview_screen.dart:1303`, `property_shell.dart:43`, `units_screen.dart:625`); analyze sauber, 253 Tests grün/8 Skips, 3 Reference-Slice-Goldens bewusst neu generiert |
| W0 — Design-Token-Verfeinerung (Dark) | UI-Foundation | `done` | Warm-neutrale Dark-Tokens in `_darkTokens` (`app_theme.dart`); Reference-Slice-Test um Dark-Golden-Variante erweitert (schließt Teil von `RISK-QA-007`); 3 neue Dark-Goldens generiert und visuell geprüft; analyze sauber, 256 Tests grün/8 Skips |
| W0 — SearchScreen/RentalOverview-Entscheidung (MOD-CLEAN-005 / OPEN-003) | Entscheidung | `done` | **Entschieden (2026-07-19):** SearchScreen (SCR-064) → entfernen (redundant zur `CommandPalette`, nicht produktiv verdrahtet); RentalOverviewScreen (SCR-065) → behalten, fest Welle 3 (leasing_operations); ungenutzten Import in `AppScaffold` dabei bereinigen. Ausführung im W0-Shell-Cleanup. |
| W0 — Token-Quellen-Zusammenführung (DEBT-TOKEN-001, W0-Anteil) | UI-Foundation | `done` | **2026-07-19.** Einzige `_Palette`-Quelle in `app_theme.dart`; `AppColors` + `_lightTokens`/`_darkTokens` referenzieren sie nur noch; Drift-Guard-Test `test/ui/theme/token_source_sync_test.dart`; Goldens pixel-identisch, analyze sauber, 258 Tests grün / 8 Skips. Rest (per-Screen-Hex, `AppColors.background`-Semantik) in Wellen 1/3/6. |
| W0 — V1/V2-Shell/Wrapper-Bereinigung + SearchScreen-Entfernung (MOD-CLEAN-001..005) | Refactor | `done` | **2026-07-19, mit Nutzerfreigabe vor jeder Löschung.** Paritäts-/Charakterisierungstests zuerst geschrieben und grün (V1-vs-V2 Properties-Liste, Legacy-vs-V2-Shell, Dashboard-Wrapper-Delegation), dann gelöscht: `search_screen.dart`+Test, `dashboard_screen.dart`-Wrapper, `properties_screen.dart` (V1), `property_shell_v2.dart`-Wrapper, `shell/sidebar.dart`+`topbar.dart` (V1), `ui_feature_flags.dart`+Test (alle 4 `UiScreenFlag`s referenzlos). V2→kanonisch umbenannt (`DashboardScreenV2`→`DashboardScreen`, `PropertiesScreenV2`→`PropertiesScreen`, `SidebarV2`→`Sidebar`, `TopBarV2`→`TopBar`), `v2/`-Ordner aufgelöst, `AppScaffold` auf eine Shell reduziert. Dauerhafte neue Coverage: `test/ui/screens/properties_screen_parity_test.dart`, `test/ui/shell/shell_consolidation_parity_test.dart` (Liste/Shell hatten vorher keinen dedizierten Widget-Test). Verifiziert: `flutter analyze` sauber, volle Suite 256 Tests grün / 8 Skips. Hinweis: der in MOD-CLEAN-005 genannte RentalOverview-Import in `AppScaffold` existierte nicht mehr (bereits früher entfernt) — Doc-Pointer in `02_architecture_modernization_backlog.md` korrigiert; `RentalOverviewScreen` bleibt (verwaist, geplant Welle 3). |

### Neuer Debt-Fund (2026-07-19)

Zwei parallele Token-Quellen: `AppColors` (statisch, ~61 Dateien / 293 Refs) **und** `_lightTokens`/`_darkTokens` (bauen `ThemeData`). Als `DEBT-TOKEN-001` in `02_architecture_modernization_backlog.md` erfasst. **W0-Anteil aufgelöst (2026-07-19):** beide Quellen lesen jetzt aus einer einzigen privaten `_Palette`-Const-Tabelle in `app_theme.dart` (inkl. der 3 Streu-Literale in `_buildTheme`); Drift ist strukturell unmöglich, zusätzlich abgesichert durch `test/ui/theme/token_source_sync_test.dart`. Null visuelle Änderung (Reference-Slice-Goldens pixel-identisch; volle Suite 258 Tests grün / 8 Skips). Bewusst offen gelassen: `AppColors.background` bleibt Weiß (= Surface) statt des warmen Canvas — Legacy-Screens nutzen es als weiße Fläche; Auflösung sowie die hardkodierten Hex-Werte einzelner Screens weiterhin pro Screen in dessen Welle (1/3/6).

| W2 — Wellen-Detaildokument | Doku | `done` | **2026-07-24.** `docs/architecture/phase_2/04b_wave2_contacts_documents.md`: 4 Screens (neuer PartiesScreen, ComplianceDashboard SCR-052, PropertyDocuments SCR-020, DocumentsScreen SCR-051) + Arbeitspaket 0 (Provider-/`main.dart`-Wiring beider Contracts, Backend-gewählt) im Sechs-Punkte-Template, Reihenfolge + Delegationsplan + Querschnittsthemen. Basiert auf Ist-Audit (Screen-Dateien/LOC/nx_-Nutzung/Farb-Literale/Provider-Reads + P2-D02/P2-D03-Contract-Oberflächen), nicht auf Plan-Annahmen. **Ein Befund vorab benannt und per Nutzerbestätigung entschieden statt still getroffen:** die Legacy-Adapter beider Domänen sind read-only (Mutation → `dependencyConflict`), daher konsumieren die Screens die Feature-Contracts backend-gewählt und zeigen im SQLite-Modus den Pflicht-Zustand „read-only bis migriert" für Mutationen (Präzedenz: P2-D01-Members-UI nur-Supabase) — lokales Dokument-/Party-Editieren im SQLite-Modus entfällt bewusst. **`OPEN-001`/`DUP-010` sind bereits entschieden+umgesetzt (P2-D02), kein Hard-Stop.** Umsetzung folgt (Arbeitspaket 0 zuerst). |

| W2-AP0 — Provider-/`main.dart`-Wiring (beide Contracts, Backend-gewählt) | Backend-Naht | `done` | **2026-07-28.** Funktions-Plan (kein Sechs-Punkte-Plan, keine UI) im Chat gezeigt. Neu: `lib/features/contacts_parties/application/party_providers.dart` (4 Ports + nullbarer `partyQueryInvalidationSourceProvider`), `lib/features/documents_compliance/application/document_providers.dart` (6 Ports + nullbarer `documentQueryInvalidationSourceProvider`) — beide **ohne SDK-Import**, Application-Layer bleibt backend-agnostisch, fail-closed (`StateError`) wie `referencePropertyRepositoryProvider`. Neu: `lib/app_backend_wiring.dart` als Kompositionswurzel — `legacy{Party,Document}ReadSourceProvider` + `legacySqlite{Party,Document}AdapterProvider` (gebaut aus den bestehenden SQLite-Repo-Providern in `app_state.dart`, Workspace-Scope aus `activeWorkspaceIdProvider`, `null` → `''`, d. h. der Adapter antwortet `forbidden` statt fremde Workspaces zu lesen) und `featureBackendOverrides({environment, client})`, das je `DataBackend` die Override-Liste liefert; `main.dart` spreizt sie in **beiden** Zweigen (Supabase-Zweig zusätzlich zu den bestehenden Reference-Slice-Overrides, SQLite-Zweig neben `databaseProvider`). Je Domäne bedient **eine** Adapter-Instanz alle Ports. Realtime nur im Supabase-Modus (`Supabase{Party,Document}QueryInvalidationAdapter`); im SQLite-Modus bleiben beide Invalidation-Provider bewusst `null` (Legacy-Adapter hat keinen Kanal). **Namenskollision vermieden:** neue Namen ≠ Legacy-`documentsRepositoryProvider` (`DocumentsRepo`), kein Legacy-Provider und kein bestehender Consumer angefasst. Verifiziert: `flutter analyze --no-pub` sauber; neuer `test/app_backend_wiring_test.dart` **10 Tests grün** — sqlite bindet alle 4 Party-/6 Dokument-Ports an dieselbe Legacy-Instanz, beide Invalidation-Provider `null`, **Lesen erfolgreich + Mutation → `dependencyConflict`** (belegt „read-only bis migriert" als echten, nicht angenommenen Zustand) und Fremd-Workspace → `forbidden`; supabase bindet alle Ports an die Supabase-Adapter + beide Invalidation-Adapter und verweigert Cloud-Overrides ohne Client; ohne Overrides werfen alle Ports `StateError`. Volle Suite **649 grün / 15 Skips**. |
| W2-AP1 — PartiesScreen (neu, Pattern-Beweis) | Screen-Neubau | `done` (manueller Golden-Path gebündelt offen) | **2026-07-28.** Sechs-Punkte-Plan im Chat gezeigt. Neu: `lib/ui/screens/parties/parties_screen.dart` + `widgets/{party_badges,party_table,party_detail_panel,party_dialogs}.dart`, Controller `lib/features/contacts_parties/application/parties_controller.dart` (Phasen-Modell analog `MembersAdminController`, Generation-Guard, Realtime-Abo im Supabase-Modus). **Voraussetzung mitgeliefert:** `WorkspaceSessionScope` + `workspaceSessionScopeProvider` in `lib/features/identity_access/application/workspace_session_scope.dart` (neben `AuthorizationPort`), backend-gewählt in `featureBackendOverrides` überschrieben — lokal aus `activeWorkspaceId`/`activeUserId`/`Rbac.permissionsForRole` mit `mutationsSupported: false`, in der Cloud aus der Reference-Slice-Session mit `true`; Default deny-all (fail closed). Alle vier W2-Screens werden diese Naht nutzen. **Navigation (vor Umsetzung bestätigt):** additiver `GlobalPage.parties` + Sidebar-Eintrag „Parteien" in der Gruppe Tagesgeschaeft; Rollen-Gating über das bestehende `isPageAllowedForRole` (bewusst **keine** Änderung an `rbac.dart` — die lokale Permission-Tabelle kennt kein `party.*`). **Zwei belegte Abweichungen vom Detaildokument, bewusst statt still:** (1) keine Rollen-Spalte in der Liste — die Contract-Listenprojektion `PartySummaryDto` führt keine Rollen, eine Rollen-Spalte wäre ein Read pro Zeile (genau das N+1-Muster, das W2 an anderer Stelle entfernt); Rollen stehen im Detailpanel, der Rollen-**Filter** deckt die Listenebene serverseitig ab. (2) Textsuche/Sortierung client-seitig über die geladenen Seiten — `PartyListQuery` hat kein Text-Prädikat, und das Dokument schließt neue Backend-Reads für diesen Screen aus. **`forbidden` kommt vom Server** (`PartyRepositoryFailureKind.forbidden`), nicht aus einem lokalen Capability-Check — ein Client-Vorcheck hätte Policy erfunden, die es lokal nicht gibt; `AuthorizationPort` gated nur die Mutations-Affordanz. Verifiziert: `flutter analyze --no-pub` sauber; neu `test/ui/screens/parties/parties_screen_states_test.dart` **20 Tests grün** — loading (Skeleton statt Vollflächen-Spinner), empty, error (Retry, kein Roh-Exception-Text), **forbidden** (eigener Zustand, getrennt von empty/error), Auswahl→Detail mit Rollen, **read-only-bis-migriert** (Banner + deaktivierte Primäraktion + Mutation meldet sich statt still zu no-oppen, `createCalls == 0`), **versionConflict** (Dialog mit beiden Versionen + „Aktuellen Stand laden"), fehlende `party.manage` (Mutation blockiert, `createCalls == 0`), Dubletten-Warnung im Anlege-Dialog, Merge nur mit Bestätigung (`mergeCalls == 0` bis bestätigt), plus **9 Responsive-Tests** (390×844 / 1024×768 / 1440×900 × comfort/compact/adaptive). Volle Suite **696 grün / 15 Skips**. **Offen (bewusst, W1-Präzedenz):** manueller Golden-Path im laufenden Build wird gebündelt vor Wellenabschluss gemacht (App-Start-/Stack-Kosten); im Cloud-Modus über `flutter run --route=/parties` erreichbar, nach Anmeldung im Reference-Slice unter `/properties` (Session persistiert). |
| W2 — Cloud-Host-Befund + Routen-Entscheidung | Befund/Entscheidung | `done` | **2026-07-28.** **Plan-vs-Realität-Befund, geflaggt statt umgangen** (Hard-Stop „Plan ist falsch"): `lib/app.dart:41` verzweigt die ganze App am Backend — im SQLite-Modus `SecurityGate` → `AppScaffold` (Sidebar + alle Screens), im Supabase-Modus **nur** die Reference-Slice-Routen `/properties` und `/members`; `AppScaffold` hat genau eine Aufrufstelle (`security_gate.dart:26`) und wird in der Cloud **nie** gebaut. Folge: alle vier W2-Screens (Sidebar-Parties, DocumentsScreen SCR-051, eingebettetes ComplianceDashboard SCR-052, PropertyDocuments SCR-020 in der PropertyShell) laufen in der echten App nur im SQLite-Modus — also genau dort, wo beide Contracts per Design read-only sind. Damit war die Wellen-DoD-Zeile „manueller Golden-Path … (Supabase-Modus für Mutationen)" für **keinen** der vier Screens erfüllbar. Die entschiedene Architektur (Backend-gewählte Contract-Konsumtion) war davon **nicht** betroffen — es fehlte der Host. **Entschieden (Nutzerbestätigung 2026-07-28): additive Cloud-Routen für die W2-Screens** neben dem Reference-Slice; keine bestehende Route, kein Sidebar-Eintrag, kein Shell-Verhalten geändert. Umgesetzt für AP1: `partiesRoute = '/parties'` in `app_navigation.dart` + Route in `app.dart`. Voraussetzung je Screen: keine Legacy-Provider-Reads (für PartiesScreen per Konstruktion erfüllt; für PropertyDocuments SCR-020 folgt die Objekt-ID-Frage in AP3). Detaildokument `04b` ist entsprechend zu ergänzen. |
| W2-AP2 — Workspace-weite Requirement-Projektion (P2-D03-Folge-Increment) | Backend | `done` (Backend; Screen SCR-052 weiterhin offen) | **2026-07-28. Zweiter Plan-vs-Realität-Befund, geflaggt statt improvisiert.** `04b` verlangt für SCR-052 „die serverseitige `evaluate_document_requirements`-Projektion — **eine Abfrage, workspace-scoped**". Diese Fähigkeit existiert nicht: `evaluate_document_requirements(p_workspace_id, p_entity_type, p_entity_id, p_scope_key)` verlangt eine **nicht-null Entity-ID** und antwortet sonst `validation_failed` (`20260723100000_p2_d03_documents_compliance.sql:2930`) — die Projektion ist strikt **pro Entität**, in beiden Adaptern. Verworfene Auswege: (a) Fan-out je Objekt = genau das N+1, das AP2 beseitigen soll; (b) Ableitung client-seitig aus `listRequirements` + `search` = zweite Wahrheit, die `DUP-011` verbietet. **Entschieden (Nutzerbestätigung 2026-07-28): neues RPC als kleines P2-D03-Folge-Increment**, danach AP2 darauf. **Design festgelegt (Modulvertrag-getrieben, nicht Geschmack):** die Entitätsmenge darf **nicht** per Join auf `public.properties` ermittelt werden — Vertragsregel 3 (`05_target_module_contracts.md:9`, „moduluebergreifende Referenzen … niemals fremde Persistenzmodelle") und DOM-006s deklarierte Ausgangsabhängigkeiten (nur DOM-001/Storage/DOM-010, **kein** DOM-002) verbieten das. Stattdessen `evaluate_workspace_document_requirements(p_workspace_id, p_entity_type default null, p_entity_ids uuid[] default null, p_only_unmet default false)`: ausgewertet werden (1) Instanz-Regeln (`required_documents.entity_id is not null`), (2) Typ-Regeln × Entitäten aus `document_links`, (3) Typ-Regeln × den vom Aufrufer übergebenen `p_entity_ids`. Der Screen holt die Objekt-IDs aus dem DOM-002-Port (IDs/DTOs — von Regel 3 ausdrücklich erlaubt) und schickt sie in **einem** Aufruf mit; Ableitungslogik bleibt vollständig serverseitig, kein N+1, keine zweite Wahrheit. Umfang: Migration + pgTAP + Rollback-Test + `RequirementPolicyRepository.evaluateWorkspace` + beide Adapter + Adaptertests + voller `database`-Job-Gate. **Blockiert:** Docker Desktop läuft nicht (`npx supabase status` scheitert am Docker-Pipe), damit ist der DB-Gate nicht lauffähig; Migration wird bewusst **nicht** blind geschrieben. Nächster Schritt sobald Docker läuft. **Abgeschlossen 2026-07-29** (Docker lief wieder), exakt nach dem oben festgelegten Design — nichts neu entschieden. Neu: `supabase/migrations/20260729100000_p2_d03_workspace_requirements.sql` mit `evaluate_workspace_document_requirements(p_workspace_id, p_entity_type default null, p_entity_ids uuid[] default null, p_only_unmet default false)`; Entitätsmenge = Instanz-Regeln ∪ `document_links` ∪ vom Aufrufer übergebene IDs, **kein Join auf `public.properties`** (Vertragsregel 3 / DOM-006 ohne DOM-002-Abhängigkeit). **Ein im Design offen gebliebenes Detail bewusst und sichtbar gelöst statt still:** workspace-weit gibt es keinen Entitäts-`scope_key`, ohne fremdes Vokabular zu importieren — die Funktion wertet deshalb scope-agnostische Regeln aus und liefert `scoped_rule_count` mit, damit ein Aufrufer sagen kann, was er *nicht* abgedeckt hat, statt Vollständigkeit zu suggerieren; scope-spezifische Regeln bleiben Sache der Pro-Entitäts-RPC. **Drift strukturell ausgeschlossen:** die Zustandsableitung ist nach `private.document_requirement_state` gezogen und die bestehende `evaluate_document_requirements` per `create or replace` darauf umgestellt — eine Wahrheit, zwei Einstiegspunkte, per pgTAP-Test belegt (beide Projektionen liefern für dieselbe Entität dieselben Zustände). Dart-Seite: `WorkspaceDocumentRequirementQuery` + `WorkspaceDocumentRequirements` (mit `scopedRuleCount`/`blocking`) und `RequirementPolicyRepository.evaluateWorkspace` im Contract; Supabase-Adapter ruft **eine** RPC (Test belegt `rpcCalls.length == 1`) und weist eine Projektion außerhalb des angefragten Typs zurück; der Legacy-Adapter spiegelt dieselbe Semantik lokal — dafür wurde die Pro-Entitäts-Schleife in `_projectForEntity` + `_loadProjectionContext` gezogen, damit auch lokal keine zweite Ableitung entsteht und die workspace-weite Auswertung den Store **einmal** liest statt je Entität. Beide Backends weisen `entityIds` ohne `entityType` mit `validationFailed` zurück, statt stillschweigend nichts auszuwerten (der Contract-Assert dafür ging nicht: der Typ wird in `const`-Kontexten gebaut, wo `List.length` nicht auswertbar ist — als Doc-Kommentar + Test festgehalten). Verifiziert (voller `database`-Job-Gate lokal): `db reset` ✅, `db lint --level error` ✅ (leer), Security-/Performance-Advisors ✅ (nur INFO, alle vorbestehend), **pgTAP 806 Tests grün** inkl. neuem `supabase/tests/016_p2_d03_workspace_requirements.test.sql` (26 Tests: Oberfläche, `security definer`+`search_path`, anon/authenticated-Grants, Helper bleibt privat, `forbidden` für Mitglied ohne `document.read` **und** für fremden Workspace, drei Validierungsfälle, Entitätsentdeckung ohne Aufrufer-IDs, „ein Objekt ohne Regel und ohne Dokument wird nicht erfunden", Typ-Regel × je Entität vs. Instanz-Regel nur für ihre eigene, Aufrufer-IDs erweitern die Menge, abgeleitete Zustände, `only_unmet`, `scoped_rule_count`, leere Projektion statt Fehler, Gleichstand beider Projektionen), Rollback-Replay `migration down --last 1` → `supabase/tests_rollback/018_p2_d03_workspace_requirements_down.test.sql` ✅ (5 Tests; prüft auch, dass die *wiederhergestellte* Pro-Entitäts-RPC noch fail-closed antwortet, nicht nur existiert) → `migration up` → erneut 806 grün. **Echter Local-Client-Integrationstest**: `test/integration/supabase_document_repository_integration_test.dart` um die workspace-weite Projektion erweitert (Gleichstand mit der Pro-Entitäts-Projektion, `scopedRuleCount == 0`, `onlyUnmet` filtert das erfüllte Requirement heraus) — über `./tool/verify_p2_d03_integration.ps1` gegen echtes PostgREST/Auth grün. CI-Verdrahtung: neuer Rollback-Schritt in `.github/workflows/flutter.yml` **vor** dem P2-D07-Schritt. Adaptertests: 4 neue Supabase-Fälle + 6 neue Legacy-Fälle. `flutter analyze` sauber, volle Suite **847 grün / 15 Skips**. **Zwei Fremddatei-Berührungen, bewusst minimal:** `supabase/tests/002_p1_003_rls.test.sql` führt das vollständige `private`-Funktionsinventar und brauchte `document_requirement_state`; beide Dateien tragen zugleich unfertige Änderungen der parallelen P2-D07-Sitzung — nur die eigenen Zeilen ergänzt, fremde unangetastet. **Offen:** der ComplianceDashboard-Screen (SCR-052) selbst — er ist jetzt unblockiert und konsumiert `evaluateWorkspace`. |
| W2-AP3 — PropertyDocumentsScreen (SCR-020) | Screen-Redesign | `done` (manueller Golden-Path gebündelt offen) | **2026-07-28, Commit `fe44f96`.** Zwei additive Increments fertig und analyze-sauber; **der bestehende Screen ist unangetastet und funktioniert weiter**, es ist also kein halb-migrierter Zustand im Baum. (1) `lib/ui/screens/docs/widgets/document_badges.dart` — die geteilte Statussprache und damit das `DUP-007`-Fundament: je eine Abbildung für STM-008 `DocumentStatus`, `DocumentVerificationStatus` und den abgeleiteten `DocumentRequirementState`, gedacht für **drei** Aufrufer (SCR-020/051/052), Semantikfarbe nur für aufmerksamkeitswürdige Zustände, nie Farbe allein. (2) `lib/features/documents_compliance/application/property_documents_controller.dart` — Orchestrierung der sechs Ports für **ein** Objekt nach dem AP1-Muster (Phasen je Zone, Generation-Guard, Pflicht-Zustände als Daten, Mutationen brechen bei read-only-Backend/fehlender Capability ab). Drei Domänen-Spezifika explizit modelliert: `confirmContent` **gelingt in beiden Ausgängen**, daher eigene sichtbare Action-Phase `contentRejected` für den MIG-BND-003-Fehlerpfad statt „Erfolg" oder „Infrastrukturfehler"; Verifikation hängt an der separaten `document.verify`-Capability; **kein Löschpfad** (`OPN-DOM-005` offen, `archived` terminal). **Ein Defekt beim Schreiben gefunden und behoben statt committet:** die Capability-Prüfung stand zuerst auf der lokalen RBAC-Schreibweise (`document.create`/`document.update`), die Cloud-Permission heißt aber `document.manage` — in der Cloud wäre `canMutate` dadurch immer `false` gewesen. Jetzt Cloud-Vokabular, mit dokumentierter Begründung, warum die lokale Abweichung folgenlos ist (lokal ist bis zur Migration ohnehin read-only). **Offen:** der eigentliche Screen-Umbau (922 LOC) + geteilte Tabelle/Dialoge unter `docs/widgets/` + Widget-/Responsive-Tests + Cloud-Route. **Kontext:** ein Subagent für AP3 wurde beauftragt und starb sofort an einem Nutzungslimit (Reset 17:30 Europe/Berlin), nicht an der Aufgabe — er hat **nichts** hinterlassen, der Baum war danach sauber. **Abgeschlossen 2026-07-29.** Sechs-Punkte-Plan im Chat gezeigt. Neu: `lib/ui/screens/property_detail/property_documents_panel.dart` (der eigentliche Wave-2-Umbau, konsumiert ausschließlich den Feature-Contract), `lib/ui/screens/property_detail/widgets/property_images_section.dart` (extrahiert, s. u.), sowie die geteilten `DUP-007`-Bausteine unter `lib/ui/screens/docs/widgets/`: `document_table.dart`, `document_requirement_table.dart`, `document_detail_panel.dart`, `document_dialogs.dart`, `document_notices.dart`, `document_formatting.dart`, `document_type_registry.dart` — alle sieben für **drei** Aufrufer gebaut (SCR-020/051/052), AP4 setzt darauf auf. `property_documents_screen.dart` bleibt der **Tab-Host** und ist auf 128 LOC geschrumpft. **Drei Plan-vs-Realität-Befunde, geflaggt statt umgangen:** (1) Die Datei ist kein Dokument-Screen, sondern ein **Drei-Tab-Host** — `property_page_router.dart:98/104/110` routet **drei** `PropertyDetailPage`-Werte (`audit`/`documents`/`reports`) mit `initialIndex` 0/1/2 hinein; `04b`s „ersetzt den TabController-Eigenbau" hätte wörtlich zwei Routen zerstört. Der Host bleibt, nur Tab 0 wurde neu gebaut; `Material(color: Colors.white)` → Token-`DecoratedBox`. (2) Der Dokumente-Tab enthielt die **Objektbild-Galerie** (Titelbild/Galerie/Schadensbild), deren `image_role`-Metadaten `propertyTitleImageProvider` (`app_state.dart:524`) liest — konsumiert von `property_card.dart:292` und `overview_screen.dart:228`. `04b` erwähnt Bilder nirgends; Löschen wäre eine V1-UI-Pfad-Löschung (Hard-Stop). **Erhalten**, in `widgets/property_images_section.dart` extrahiert, nur im lokalen Host gemountet, nie auf der Cloud-Route. **Bewusste Abweichung von der Wellen-Abschlusszeile „kein W2-Screen nutzt mehr Legacy-Dokument-Repos": genau diese eine Stelle tut es noch** — Nutzerentscheidung beim Wellen-Check-in. Dabei behoben: `propertyTitleImageProvider` wird nach einem Bild-Upload invalidiert (vorher zeigten Karte und Übersicht bis zum nächsten Vollreload das alte Titelbild). Nicht übernommen: der Tap-auf-Thumbnail-Dialog, der rohe Pfade und Metadaten-Schlüssel zeigte (verstößt gegen die UX-Copy-Regel des Design-Systems). (3) **P2-D03 hat keinen Upload-Port.** `DocumentContentDraft` *deklariert* ein Objekt, das bereits im privaten Bucket liegt; `SignedUrlPort` ist reines Download; der einzige Storage-Aufruf des Supabase-Adapters ist `createSignedUrl` (`supabase_document_repository_adapter.dart:203`). `04b`s Interaktionsschritt „Version **hochladen**" hat damit keine Implementierung. Die Dialoge sind gegen die *tatsächliche* Contract-Oberfläche gebaut (Pfad/SHA-256/Größe/MIME deklarieren) und sagen das im Klartext, statt einen Datei-Upload vorzutäuschen — **offener Bedarf: ein `DocumentUploadPort` als eigenes P2-D03-Folge-Increment**, sonst ist der manuelle Golden-Path „Dokument anlegen" in der Cloud nicht durchspielbar. **Ein Defekt im committeten Fundament gefunden und behoben:** `PropertyDocumentsController.createDocument` versprach im Doc-Kommentar die Objekt-Verknüpfung, rief aber nur `create_document` — der `DocumentLinkPort` war nicht einmal injiziert. Ein so angelegtes Dokument wäre nie in der objektbezogenen Liste erschienen. Jetzt `create` → `link`; da der Contract kein kombiniertes RPC hat, sind beide Schritte nicht atomar, und ein fehlgeschlagener Link wird als **eigener** Ausgang gemeldet statt als Erfolg, dem die Liste widerspricht. **Cloud-Route (additiv):** `propertyDocumentsRoute = '/property-documents'` + `propertyDocumentsRouteFor(id)`/`propertyDocumentsPropertyIdFromRoute(name)` (Objekt-ID im Pfad, gleiche Form wie `referencePropertyRoute`) → mountet **den Panel**, nicht den Host; damit erreicht die Cloud-Route keinen `dart:io`- oder Legacy-Provider-Pfad. Verifiziert: `flutter analyze --no-pub` sauber; neu `test/ui/screens/property_detail/property_documents_states_test.dart` **27 Tests grün** — loading (Sektions-Skeletons statt Vollflächen-Spinner), empty mit konkreter Folgeaktion, error (Retry, kein Roh-Exception-Text), **forbidden** getrennt von empty/error, **eigener `forbidden`-Zustand der Anforderungszone** (Zonen scheitern unabhängig), Pflicht- vs. optionale Anforderung, **read-only-bis-migriert** (Banner + deaktivierte Primäraktion + `createCalls == 0`), **versionConflict** (beide Versionen + „Aktuellen Stand laden"), fehlende `document.manage` (`createCalls == 0`), **`document.verify` getrennt gegated** (`canMutate` true, `canVerify` false, `verifyCalls == 0`), **MIG-BND-003 `contentRejected`** als sichtbarer Inline-Hinweis statt Erfolg oder verschwindender Snackbar, **Archivieren nur nach Bestätigung** (`transitionCalls == 0` vor und nach „Abbrechen"), **kein Löschpfad** (`OPN-DOM-005`), Anlegen verknüpft mit dem Objekt, fehlgeschlagener Link wird gemeldet, Signed-URL mit Ablauf, plus **9 Responsive-Tests** (390×844 / 1024×768 / 1440×900 × comfort/compact/adaptive). Dabei ein echter Layout-Fehler gefunden: im 1440er-Split ist die Listenspalte ~825 px und fiel damit unter `NxDataTableShell`s 900-px-Mobilbreakpoint — Desktop zeigte die Telefon-Kachelliste; `DocumentTable` hat jetzt einen `mobileBreakpoint`-Parameter, der Panel setzt ihn auf 640. Volle Suite **778 grün / 15 Skips**, `flutter build web --no-pub` grün. **Offen (bewusst, W1-Präzedenz):** manueller Golden-Path gebündelt vor Wellenabschluss; in der Cloud über `flutter run --route=/property-documents/<propertyId>` erreichbar. |
| W1 — Wellen-Detaildokument | Doku | `done` | **2026-07-20.** `docs/architecture/phase_2/04a_wave1_portfolio_property.md`: 11 Arbeitseinheiten (10 Screens + DEBT-012) im Sechs-Punkte-Template, Reihenfolge + Delegationsplan + Querschnittsthemen (NxPageHeader/NxChartContainer ohne Direkt-Nutzung — NxPageHeader erreicht 21 Screens via `ListFilterTemplate`; Farb-Hotspots overview ~48 / properties ~32 / dashboard ~28 / creation ~28 / shell ~25). Basiert auf Ist-Audit per Explore-Subagenten (Screens + Debt-/Backlog-Register), nicht auf Plan-Annahmen. Präzisierung zu Worked Example 2 (kein Side-by-Side im Legacy-PropertiesScreen in W1) dokumentiert statt still entschieden. |

| W1-AP1 — PropertiesScreen (SCR-006) + Sidebar-Token-Nachputz | Screen-Redesign | `done` | **2026-07-20.** Tabellenansicht als Default (`NxDataTableShell`, neue `PropertyTable` mit Status-Spalte aktiv/archiviert, Mobile-Fallback als Tile-Liste) + Karten als sekundäre Ansicht (SegmentedButton-Toggle); BIG-026-Split: `properties/widgets/{property_table,property_card,portfolio_kpi_header,property_formatters}.dart`, Screen 975→~470 LOC Orchestrierung. Zustände: Skeleton statt Vollflächen-Spinner, Fehler als `NxEmptyState` mit Retry (kein Roh-Exception-Text mehr), Empty unverändert. Token-Bereinigung: Metric-Tiles/Cover/Typ-Chip auf `surfaceAlt`/`inverseSurface`/Semantik statt ~32 Literalen (Cover-Gradienten bewusst durch ruhige Neutral-Fläche ersetzt, gemäß 03-Ästhetik „neutrale Flächen statt Farbe"); Sidebar-Navy als `_Palette.sidebar*`-Tokens zentralisiert (visuell identisch), raw `TextStyle` in Sidebar-Tiles → `textTheme`. Komponenten-Fix: `NxDataTableShell` mit eigenen ScrollControllern (Scrollbar-Assertion in scrollbaren Seiten). Verifiziert: analyze sauber; neue Zustands-/Responsive-Tests `test/ui/screens/properties_screen_states_test.dart` (empty/error/Toggle/390×844/1024×768) grün; Paritäts-, Shell- und Token-Drift-Tests grün; volle Suite 261+2 Tests grün / 8 Skips. Offen (bewusst): manueller Golden-Path im laufenden App-Build wird gebündelt nach mehreren APs gemacht (App-Start-Kosten), vor Wellenabschluss. |

| W1-AP2 — PropertyShell (SCR-010) | Screen-Redesign | `done` | **2026-07-20.** BIG-024-Split: `property_shell.dart` (1052→~330 LOC Gerüst) + neu `property_nav.dart` (Sichtbarkeits-/Label-/Icon-Mapping, `PropertyNavigationBar` mit Gruppen-Nav Desktop / Dropdown Phone) + `property_page_router.dart` (Enum→Screen, Scroll-Mapping). Kopf: `DetailTemplate` von `plainHeader` auf `NxPageHeader` umgestellt; `NxStatusBadge` Aktiv/Archiviert (gleiche Kinds wie AP1-Tabelle) + Szenario-Picker als contextBar. Token-Bereinigung: alle 11 `_asset*`-Konstanten + Streu-Literale/TextStyles entfernt; das ~100-zeilige `_propertyWorkspaceTheme` ersetzt durch `AppTheme.light(densityMode: ambient)` — **bewusst weiterhin forced-light**, bis die 32 eingebetteten Legacy-Modul-Screens tokenrein sind (Rest-Debt DEBT-TOKEN-001; danach kann der Theme-Wrapper ersatzlos entfallen); Nav-Navy auf `AppColors.sidebar*` (identisch mit App-Sidebar). Zustände: Shell-Skeleton statt Vollflächen-Spinner, Fehler als `NxEmptyState` mit Retry (kein Roh-Exception-Text), Auto-Szenario-Hinweis unverändert sichtbar; toter `_expanded`-State entfernt. Verifiziert: analyze sauber; bestehender `property_shell_navigation_test.dart` unverändert grün; neu `test/ui/property_detail/property_shell_states_test.dart` (8 Tests: Skeleton, Error+Retry, Auto-Creation, Badges aktiv/archiviert, 390/1024 → Dropdown [narrow < 1100 per App-Konvention], 1440 → Gruppen-Nav); volle Suite 271 Tests grün / 8 Skips. **Befund (vorbestehend, geflaggt statt still gefixt):** `PropertyTasksScreen` überläuft bei 390×844 um ~75px auch auf HEAD vor AP2 (empirisch per Stash verifiziert; AP2-Header addiert ~40px) — Modul-Screen ist Welle-3-Scope, als separater Task ausgekoppelt (weiter offen). Der Phone-Zustandstest filterte diesen Overflow zunächst; der Filter wurde nachträglich entfernt und der Test läuft im aktuellen Tree filterlos grün (2× verifiziert) — sollte er in CI erneut auslösen, Filter samt Kommentar wiederherstellen. Offen (bewusst, wie AP1): manueller Golden-Path gebündelt vor Wellenabschluss. |

| W1-AP3 — OverviewScreen (SCR-011) | Screen-Redesign | `done` | **2026-07-20, Umsetzung an Subagent delegiert, Verifikation eigenständig.** BIG-010-Split: `overview_screen.dart` 2065→270 LOC Orchestrierung (`ConsumerStatefulWidget`→`ConsumerWidget`, Cashflow-Modus-State im Chart-Widget); 8 neue Dateien unter `property_detail/overview/` (view_model, workflow_pipeline, metric_grid, snapshot_section, charts, onboarding_card, section_states, edit_property_dialog). `NxChartContainer` erweitert (erste echte Nutzung): `height` (SizedBox statt Expanded → funktioniert in fullPageScroll) + `trailing` (Header-Aktion für den bestehenden Annual/Monthly-Toggle), Default-Verhalten unverändert, neuer Komponententest (`test/ui/components/`, Verzeichnis neu). Token: 39 Farb-Literale (Ist am HEAD; 04a schätzte ~48) → 0, 2 per-Screen-TextStyles → `textTheme`/`tabularNumericStyle`, keine `AppColors.background`-Nutzung mehr. Zustände: Sektions-Skeletons + Fehler-mit-Retry pro Sektion (vorher Vollflächen-Spinner + roher `'Error: $error'`-Text), Onboarding als `NxEmptyState` mit den 4 bestehenden Next-Step-Aktionen; Metric-Tiles mit `InfoTooltip` + Navigation zum Analyse-Modul; LTV-Badge-Schwellen unverändert. Pipeline-BoxShadow entfällt (03-Regel gegen Drop-Shadows), aktiver Zustand via Rand/Primary-Tönung. Verifiziert (eigenständig): git-Status nur erlaubte Dateien, Reference-Slice-Goldens byte-identisch, grep 0 Literale in Screen+`overview/`, analyze sauber, volle Suite **283 Tests grün / 8 Skips** (Baseline 271/8; +12: 8 Overview-Zustands-/Responsive-Tests 390/1024/1440 + NxChartContainer-Tests). **Befund (vorbestehend, unangetastet):** Stammdaten-Dialog bietet kein Feld für Baujahr/Fläche, obwohl die Snapshot-Sektion beide anzeigt — Kandidat für späteres Paket. Offen (bewusst): manueller Golden-Path gebündelt vor Wellenabschluss. |

| W1-AP4 — DashboardScreen (SCR-004) | Screen-Redesign | `done` | **2026-07-20, Umsetzung an Subagent delegiert, Verifikation eigenständig.** BIG-007-Split: `dashboard_screen.dart` 2259→155 LOC (schlanke Orchestrierung, `export 'dashboard/dashboard_view_model.dart'` hält bestehende Modell-/Provider-Importe u. a. des nicht anfassbaren `test/ui/responsive_overflow_gate_test.dart` funktionsfähig — selbst verifiziert grün); 6 neue Dateien unter `dashboard/` (view_model mit Modellklassen+`dashboardOverviewProvider`+Aggregation, kpi_row, attention_list, charts, activity_table, section_states). Komposition: `NxPageHeader` (Refresh als secondaryAction), `NxCard(kpi)`-Tiles (3/2/1-spaltig), `NxChartContainer` (LineChart Wertentwicklung mit Perioden-`trailing`, BarChart Objektverteilung — nutzt AP3-Erweiterung, kein Fork), `NxDataTableShell` (Aktivitätstabelle mit Mobile-Fallback), `NxStatusBadge`. Zustände (heute fehlend): dashboard-förmiges Skeleton statt Vollflächen-Spinner, `NxEmptyState`+Retry statt rohem `'…: $error'`, Empty bei 0 Objekten → Objektanlage. Token-Ist (echte Zahl, nicht 04a-Schätzung ~28): 0 Hex-Literale (war schon sauber), 2 rohe `Colors.white/black`-Overlays + 2 rohe `TextStyle` → 0. `kpi_tile.dart` bewusst NICHT wiederverwendet (Material-`Card` mit Elevation = Drop-Shadow, verboten per 03; wie AP1/AP3 direkt `NxCard`). Verifiziert (eigenständig): git-Status nur erlaubte Dateien, analyze sauber, `responsive_overflow_gate_test` grün, volle Suite **289 Tests grün / 8 Skips** (Baseline 283/8; +6). **Sichtbare Inhaltsänderung (bewusst, dem Nutzer gemeldet):** die separate Signal-Grid-Sektion (`_SignalGrid`, 7 Kacheln) wird nicht mehr als eigener Block gerendert — redundant mit Attention-Liste (jedes Signal>0 erzeugt bereits eine Attention-Zeile) + KPI „Offene Aktionen"; Worked Example 1 listet sie nicht. `signalMetrics`/`_buildSignalMetrics` bleiben im Datenmodell/Provider unverändert (keine Datenvertragsänderung, reversibel). **Bewusste Abweichung:** kein Zeitraumfilter (Provider kann ihn nicht honorieren → P2-D09); der client-seitige 6M/YTD/1J-Wähler des Wertentwicklungs-Charts bleibt. Offen (bewusst): manueller Golden-Path gebündelt vor Wellenabschluss. |

| W1-AP5 — PropertyCreationWorkflowScreen (SCR-008) | Screen-Redesign | `done` | **2026-07-20, Umsetzung an Subagent delegiert, Verifikation eigenständig.** BIG-012-Split: `property_creation_workflow_screen.dart` 1791→376 LOC (schlanker Wizard-Container, öffentlicher Konstruktor + Aufrufer `properties_screen.dart:402` unberührt); 11 neue Dateien unter `properties/creation/` (support + 5 widgets/ + 5 steps/, je Schrittgruppe). Komposition: Material-`AppBar`→`NxPageHeader` (Titel + „Schliessen", im `_saving` deaktiviert), Schritt-Inhalte in `NxFormSectionCard`, Fußzeile `NxActionToolbar`, `ResponsiveConstraints` bleibt. Draft-Muster erhalten: Container hält mutablen `_draft` + einen `onChanged`-Rebuild-Callback (zugleich Dirty-Signal), an alle Schritt-Widgets/Editoren gereicht — keine Provider-/Validierungs-/Datenvertragsänderung. Token-Ist (echte Zahl, nicht 04a-Schätzung ~28): 0 Hex, alle `Colors.*` bereits `semanticColors`, real 1 rohe `TextStyle` → 0. **Geteilte Komponente erweitert (Blast-Radius geprüft):** `NxFormSectionCard` bekam optionalen `body` (vollbreiter Custom-Body statt `children`-Wrap); `children` jetzt optional mit Default → **rückwärtskompatibel**, beide Bestandsnutzer (settings, operations) unberührt (volle Suite grün), + neuer Komponententest. Zustände: Validierungsfehler inline (beide alten Sammel-`SnackBar`s entfernt), Save-Loading, **neu ergänzt** (fehlten): Abbrechen-Bestätigung nur bei dirty+ungespeichert, Save-Fehlerpfad mit „Erneut versuchen" ohne Datenverlust (try/catch um `onCreateProperty`). Erster Test überhaupt für den Screen (war testlos). Verifiziert (eigenständig): git-Status nur erlaubte Dateien + geteilte Komponente, analyze sauber, volle Suite **300 Tests grün / 8 Skips** (Baseline 289/8; +11). **Vorbestehender latenter Overflow gefixt** (im neuen Responsive-Test aufgedeckt): Dropdowns ohne `isExpanded` liefen bei 390px über → `isExpanded: true` (ellipsiert statt Overflow, kein Verhaltenswechsel). Offen (bewusst): manueller Golden-Path gebündelt vor Wellenabschluss. |

| W1-AP6 — PortfoliosScreen + PortfolioDetailScreen (SCR-043/044) | Screen-Redesign (Split) | `done` | **2026-07-21, eigenständig.** BIG-004-Split des 2958-LOC-Monolithen: `portfolios_screen.dart` 2958→807 LOC (`PortfoliosScreen`-Orchestrator + privater `_PortfolioLandingView` mit Übersicht/Eigenkapital-Tabs + Equity-Rechnung + Skeleton/Error) — **bleibt am Pfad `lib/ui/screens/portfolios_screen.dart`, damit `app_scaffold.dart:190` unberührt bleibt**; neu `portfolio/portfolio_detail_screen.dart` (SCR-044, 1097 LOC, inkl. PDF-Export und beiden Filter-/4-Tab-Strukturen verbatim) + `portfolio/widgets/{portfolio_landing_support,portfolio_landing_widgets,portfolio_landing_charts}.dart` (Modelle/Formatter/Trend-Math + Präsentationswidgets, ex-privat → public im Feature-Ordner). **Lademechanismus bewusst unverändert** (`FutureBuilder<_PortfolioLandingVm>` / `<_PortfolioDetailVm>`, kein Riverpod-AsyncValue) — nur Zustands-Präsentation gehoben: Landing-/Detail-Skeleton statt Vollflächen-Spinner, `NxEmptyState`+„Erneut versuchen" statt rohem `Error: $error` (Retry = `setState`, re-runt denselben Future). Beide TabBar-Strukturen und PDF-Export exakt erhalten. Token-Ist (echte Zahl, nicht 04a-Schätzung ~14): 2 rohe `Colors.blue/red` im Detail-BarChart → `colorScheme.primary/error`, 5 rohe `TextStyle` (2× Detail DataCell/analysisCard, 3× EquityRankingCard) → `textTheme`; 0 verbleibend (grep verifiziert). Erste dedizierte Widget-Tests (heute keine): `test/ui/screens/portfolios_screen_states_test.dart` (7: Skeleton, Empty, Error+Retry, Seeded+Equity-Tab-Switch, 390/1024/1440) + `test/ui/screens/portfolio/portfolio_detail_screen_states_test.dart` (3: Skeleton, Error+Retry, 4-Tab+PDF-Render). **Bewusste Scope-Verengung ggü. 04a-Text** (keine NxPageHeader/NxDataTableShell-Umkomposition) gemäß Nutzer-Vorgabe „nur Zustands-Präsentation heben"/„beide TabBar-Strukturen exakt erhalten". **3 vorbestehende latente Defekte (verbatim aus dem Monolith, von den neuen Tests aufgedeckt, wie AP5) gefixt:** (a) `EquityRankingCard`-Kopf-`Text` ohne `Expanded` → `Expanded`+Ellipsis (Overflow bei mittlerer Panelbreite); (b) 4 Detail-Filter-`DropdownButtonFormField` ohne `isExpanded` → `isExpanded: true`; (c) toter `Spacer()` in der Detail-Kopf-`Wrap` entfernt (warf ParentData-Fehler bei jedem Render; in einer `Wrap` wirkungslos → kein Layout-Wechsel). Verifiziert (eigenständig): git-Status nur erlaubte Dateien (`property_tasks_screen.dart` und `property_shell_states_test.dart` unberührt), analyze sauber, volle Suite **310 Tests grün / 8 Skips** (Baseline 300/8; +10). Offen (bewusst): manueller Golden-Path gebündelt vor Wellenabschluss. **Committet als `6f6e7c0`** (inkl. Status-Doc; die ~23 P1-Dateien und der AP1–AP5-Code bleiben unkommittiert/unberührt). |

| W1-AP7a/b/c — Analytics / DataQuality / Users (SCR-045/046/061) | Screen-Redesign | `done` | **2026-07-21.** Delegation an 3 parallele Subagenten scheiterte extern (Session-Limit-API-Fehler; alle 3 brachen in der Lese-/Explorationsphase ab, **keine Datei geschrieben** — Tree verifiziert sauber), daher eigenständig im Main-Loop umgesetzt (sequentiell, je verifiziert). **7a PortfolioAnalyticsScreen:** AppBar→`NxPageHeader` (+ expliziter Zurück, pushed route), Metriken→`NxCard`-Tiles, Cashflow-BarChart→`NxChartContainer` (Legende als volle `Wrap` unter dem Chart statt `trailing` — sonst Overflow bei 390), `setState`-Skeleton + `NxEmptyState`+Retry statt roher `_status`-Text, Export-Erfolg→SnackBar; Token: Hex-Gradienten (`0xFF10B981/34D399/EF4444/F87171`)→`semantic.success/error`, `Colors.blue/orange`→`primary`/Warn-Banner, rohe `TextStyle`→`textTheme`; 0 verbleibend. **7b DataQualityDashboardScreen:** AppBar→`NxPageHeader` (+ Zurück), `NxCard`-Score-Tiles, Befundliste→`NxDataTableShell` (+ `NxStatusBadge` je Schwere, Mobile-Card-Fallback), positiver Leerzustand „Alles im grünen Bereich" (scrollbar → kein Phone-Overflow), Skeleton + Error+Retry statt roher `_status`; Token: `AppColors.border/textSecondary` + 2 `TextStyle`→`NxCard`/`textTheme`; 0 verbleibend. **7c UsersScreen:** war bereits komponiert (`ListFilterTemplate`→`NxPageHeader`, `NxCard`, `NxStatusBadge`, 0 Farb-Literale) — neu: **expliziter Forbidden-Zustand** „Kein Zugriff" via `isPageAllowedForRole(GlobalPage.adminUsers, role)` (fail-closed, dieselbe RBAC-Prüfung wie die Navigation); `build` um den `securityControllerProvider`-`AsyncValue` restrukturiert (Security-Loading→Skeleton, Security-Error→Retry, Data→Forbidden/Inhalt); User-Load an Security-Kontext-Bereitschaft gekoppelt (Post-Frame statt `initState`); Vollflächen-Spinner→Skeleton; Rollenwechsel mit Bestätigungsdialog. **Bewusste Abweichung von 04a** (statt still): Nutzerliste bleibt responsive `_UserCard`-Liste (bereits Systemstandard; `NxDataTableShell` mit Rollen-Dropdown/Buttons je Zeile wäre schlechtere UX). **Pre-existente latente Overflows gefixt** (von neuen Tests aufgedeckt): 7a `NxChartContainer`-`trailing`-Legende, 7c `_UserCard`-Rollen-Dropdown ohne `isExpanded`, DataQuality Empty-State-Squeeze (jetzt scrollbar). Erste dedizierte Tests: `test/ui/portfolio/portfolio_analytics_screen_test.dart` (+6), `.../data_quality_dashboard_screen_test.dart` (+6), `test/ui/admin/users_screen_test.dart` (neu, 8 inkl. Forbidden). Verifiziert (eigenständig): analyze sauber, volle Suite **330 Tests grün / 8 Skips** (Baseline 310/8; +20). **Noch nicht committet** (Ansage abwarten). Offen (bewusst): manueller Golden-Path gebündelt vor Wellenabschluss. |

| W1-AP8 — SettingsScreen (SCR-062) | Screen-Redesign (Split) | `done` | **2026-07-21, eigenständig.** Ist-Befund bestätigt (nicht neu hergeleitet): der Screen war kompositorisch bereits auf Ziel (`SettingsTemplate`/`NxPageHeader`+Sektions-Rail, `NxFormSectionCard`, `SaveStatusIndicator`, `ResponsiveConstraints`, `rbacProvider`-Gating, farb-tokenrein — alle Farben `semanticColors`). Zwei Increments: **(a) Zustände+Test:** neuer `settings/settings_section_states.dart` (`SettingsSkeleton` keyed `settings_skeleton` in Rail+Formkarten-Silhouette; `SettingsLoadError`+„Erneut versuchen" via `NxEmptyState`, hartkodiertes Deutsch wie die Geschwister-`*_section_states`); Build-Gate `settings==null` löst Spinner (`settings_screen.dart:170`) durch Skeleton bzw. Fehler-Retry ab; `_load` in try/catch gehüllt — **Erstladefehler** setzt `_loadError`→Retry-Zustand, **Refresh-Fehler** (`_settings!=null`) `rethrow`t und lässt die bestehende Fehlerbehandlung in `_applySecurity`/`_restoreBackup` exakt unangetastet; letztes Streu-`TextStyle` (introCard-Titel) → `textTheme` (DEBT-TOKEN-001-W1-Rest). Erster dedizierter Widget-Test `test/ui/screens/settings_screen_test.dart` (8: loading→Skeleton statt Spinner, load-error→Retry ohne Roh-`Exception`, loaded, Sektionswechsel, Save-Status über echte Draft-Pipeline `Pending fields 0→1`, 390/1024/1440). **Vorbestehender Skeleton-Overflow** (18px, nur im ersten Ladeframe bei 390) sofort gefixt (Kopfzeile Fixbreiten → `Expanded`+Fixbreite). **(b) BIG-009-Split:** die 8 Sektions-Builder → `settings/sections/*.dart`, Container 2211→1701 LOC. **Bewusste Technik-Abweichung von AP1–AP7 (Standalone-Widgets), dokumentiert statt still gewählt:** die Sektionen sind private `extension`s auf `_SettingsScreenState` in `part`-Dateien (impliziter Receiver → direkter Zugriff auf die ~29 Controller + geteilten Draft-/Save-/RBAC-State), einzige Body-Änderung `setState(...)`→`_editDraft(...)`-Wrapper (hält den protected-`setState` intern, `analyze` sauber). **Gewählt genau, um das vom Nutzer als heikel markierte Re-Threading der ~29 Controller + Draft/Save/RBAC-State zu vermeiden — verhaltensidentisch by construction.** Unverändert: `settingsRevisionProvider`/`SaveStatusTone`-Konfliktverhalten, Draft-Clear-Listener, Backup/Restore-Bestätigungen, Save/Validierung/Nav/Header-Status/Draft-Overview, alle Inhalte. Verifiziert (eigenständig): `flutter analyze --no-pub` sauber; Settings-Test 8 grün; volle Suite **338 Tests grün / 8 Skips** (Baseline 330/8; +8). git-Status nur Settings-Dateien (`settings_screen.dart`, neuer `settings/`-Ordner, neuer Test) — die parallel-Session-Dateien (`property_tasks_screen.dart`, `property_shell_states_test.dart`, die ~23 P1-Dateien) unberührt. **Noch nicht committet** (Ansage abwarten). Offen (bewusst, wie AP1–AP7): manueller Golden-Path gebündelt vor Wellenabschluss. Optionaler Rest-Split (Change-Model/Draft-Overview aus dem Container) bewusst offengelassen — 04a betont die 8 Sektions-Builder, die sind raus. |

| W1-AP9 — DEBT-012: `deletePermanently` → Archiv/Tombstone (STM-002) | Verhaltensänderung (Löschpfad) | `done` | **2026-07-21, eigenständig, mit expliziter Nutzerfreigabe (Hard-Stop).** Ablauf regelkonform: **erst Charakterisierungstest** des heutigen Hard-Delete geschrieben und grün, **dann** Funktions-Plan + Designentscheidungen dem Nutzer vorgelegt. Nutzer wählte: **echter Tombstone + Migration**, **restore-fähig**, **SQLite jetzt / Supabase als Folge-Increment**. **SQLite-Increment (dieses) — `done`:** (1) Migration `v47` (`ALTER TABLE properties ADD COLUMN deleted_at INTEGER` + `deleted_by TEXT` + Index; `currentVersion` 46→47, in `onCreate` und `onUpgrade`). (2) `PropertyRecord.deletedAt`/`deletedBy` **read-only** (in `fromMap`, bewusst NICHT in `toMap`, damit generisches create/update den Marker nie setzt/löscht) + `isDeleted`-Getter. (3) `property_repo.dart`: der ~350-Zeilen-Hard-Delete-Cascade (+ 5 private Helfer) **ersetzt** durch `tombstone(id, {actorId})` (setzt `deleted_at`/`deleted_by` + `archived=1`, behält Row **und alle Kinder**, entfernt Such-Index, Append-only-Audit `delete`) und `restore(id, {actorId})` (löscht Marker + `archived=0`, re-indexiert, Audit `update`); Datei 1437→1087 LOC. **Kern-Designtrick (begrenzt Blast-Radius):** weil Tombstone `archived=1` setzt und **alle** aktiven Reads bereits `archived=0` filtern (u. a. `portfolio_repo`, `data_quality_repo`), verschwinden Tombstones überall automatisch — **keine Änderung an ~10 anderen Repo-Reads nötig**; `deleted_at` bleibt der distinkte, von „archiviert" unterscheidbare Marker. (4) `property_state.dart`: `deletePermanently`→`tombstone` + neu `restore` (Actor via `activeUserIdProvider`). (5) UI `properties_screen`/`property_table`/`property_card`: Lösch-Dialog jetzt „Objekt löschen" (reversibel, Kinder bleiben) → `tombstone`; Tombstone-Zeilen zeigen `NxStatusBadge('Gelöscht', error)` + einzige Menüaktion „Wiederherstellen" → `restore` (verhindert den inkonsistenten Live-aber-gelöscht-Zustand, den ein Unarchive erzeugt hätte); Archiv-Toggle-Label zur Abgrenzung „Wiederherstellen"→„Aus Archiv holen"; Delete-Label „Endgültig löschen"→„Löschen". (6) Contract-Test `test/data/repositories/property_repo_tombstone_test.dart` (7: Row+Kinder behalten/aktiv-Liste versteckt/Archiv-Liste sichtbar+`isDeleted`/Audit, Idempotenz, Unknown-No-op, Restore reaktiviert+Kinder, Restore-No-op, Archiv≠Tombstone-Kontrast, `property.delete`-Gate blockt Viewer) — ersetzt den Charakterisierungstest. Verifiziert (eigenständig): analyze sauber, volle Suite **345 grün / 8 Skips** (Baseline 338/8; +7; Migrations-Test trotz Versionsbump grün). **Supabase-Parität (Folge-Increment) — `done` (2026-07-22, eigenständig):** Nutzerentscheidung **„Archive IS the tombstone (minimal)"** — der Postgres-`archived`-Status **ist** bereits der restore-fähige Soft-Delete (P1-004 setzt `deleted_at`, behält Row/Kinder [Postgres hat keinen Cascade], versteckt aus aktiven Reads, restore-fähig via Un-Archive, append-only als `property.update` auditiert); der P1-004-Contract/RPC bleibt bewusst unberührt. **Additiv:** Migration `20260722130000_debt_012_property_delete_marker` (`deleted_by uuid` + Trigger `private.properties_apply_delete_marker`, der `auth.uid()` beim Archive-Übergang setzt und bei Restore/aktiv leert — `update_property`-Core nicht editiert). **Adapter:** aktive Reads schließen Tombstones per Marker aus (`.isFilter('deleted_at', null)` statt `neq status archived` — heute äquivalent, aber am kanonischen Marker), `deleted_by` in `PropertyDto` geparst, `PropertyRepository`-dartdoc dokumentiert archive==tombstone / un-archive==restore. **Tests:** pgTAP `007_debt_012_property_tombstone` (20), Rollback `006_debt_012_down` (5) + `flutter.yml`-Replay-Prepend (relative `--last`-Counts bleiben gültig), Adapter-Test +2, realer lokaler Client-Integrationstest `test/integration/supabase_property_tombstone_integration_test.dart` + `tool/verify_debt_012_tombstone.ps1` (+ CI-Step); P1-003-Funktions-Inventar um den Trigger erweitert. **Verifiziert (voller `database`-Job lokal):** `db reset`/`lint`/advisors sauber, pgTAP **232**, Rollback-Replay `006→005→004→003`+`up`, alle `tool/verify_*` grün (inkl. neuem), `flutter analyze` sauber, Flutter-Suite. **Vor-Befund gefixt:** `verify_p1_004_concurrency` war auf HEAD rot (Worker simulierten kein AAL2, das P1-015-Gate kam später) → Worker-Fixtures um `request.jwt.claims aal:aal2` ergänzt; P1-Batch committet als `ec3f061`. **Abweichung (gemeldet):** `deleted_by` per Trigger, daher trägt das `update()`-RPC-Ergebnis `deletedBy: null`, `getById`/`list` den echten Wert. **Tombstone/Restore-UI-Widget-Test erledigt** (`test/ui/screens/properties_screen_tombstone_test.dart`, 4 Tests). **Committet:** SQLite-Batch in `6073774`, Supabase-Parität in `cf59449`. **Golden-Path im laufenden Windows-Build bestanden (2026-07-22)** — Details im Abschnitt „Nächster Schritt". |

| P2-D01 — `identity_access` Full Expansion (Welle-2-Backend-Voraussetzung) | Backend (Domain-Expansion) | `done` | **2026-07-22, eigenständig, nach Nutzerfreigabe der Wellengrenze („start").** Framing-Korrektur zuerst: laut `04_screen_redesign_wave_plan.md` sind die **Welle-2-Screens** `contacts_parties`+`documents_compliance` (SCR-020/051/052 + neuer Parties-Screen); nächstes Item in Abhängigkeitsreihenfolge ist das **Backend P2-D01** (Gate für P2-D02/D03); `04b` wird erst nach deren Backend-Gates geschrieben. Funktions-Plan im Chat gezeigt (spiegelt P1-004-RPC-Form; P1-002-Enum + P1-003-Read-Policies + P1-017-Invalidierungs-Trigger existieren bereits). **Increment 1 (Schema) — `done`:** Migration `20260722200000_p2_d01_membership_lifecycle`: `membership_invitations` (Pre-Auth-E-Mail-Staging, default-deny RLS, `security.manage`-Read-Policy, partial-unique pending, FK-Indizes) + 5 RPCs (`invite_workspace_member`, `list_my_pending_invitations`, `accept_workspace_invitation`, `update_membership_status`, `change_membership_role`, `revoke_workspace_invitation`) — alle AAL2-gated, idempotent via `mutation_receipts`, auditiert (`membership.invite/accept/suspend/reactivate/revoke`, `membership_invitation.invite/revoke`), Übergangsmatrix STM-001 serverseitig, **Last-Admin-Guard** (letzter aktiver `security.manage`-Träger kann weder suspendiert/revoked noch herabgestuft werden); geteilte private Helfer (`membership_command_gate`/`claim_membership_mutation`/`finish_membership_mutation`/…) statt 5× P1-004-Boilerplate. `memberships_select_authorized` in place ersetzt (gleicher Name): Own-Row-Sichtbarkeit für jeden Status (invited/suspended sehen wo sie stehen) + `security.manage`; 2 Assertions in `002_p1_003_rls` auf den neuen, strengeren Vertrag angepasst (exakt eigene Row). **Design-Befund (im Test gefunden, gefixt):** Claim-vor-State-Validierung in allen 5 RPCs — P1-004s Reihenfolge (Validierung vor Receipt-Claim) bricht Replays, sobald die Mutation den validierten Zustand selbst ändert; Receipt-Cleanup bei Post-Claim-Validierungsfehlern, per Test abgesichert (Replay identisch, keine pending Receipts). **Dokumentierte Konsequenz:** revoked ist terminal + `unique(workspace,user)` ⇒ revoked-Mitglied kann nicht re-invited werden (Test-asserted; Auflösung wäre STM-001-Änderung → Nutzerentscheidung). Tests: pgTAP `008_p2_d01_membership` (**69**: Schema/RLS/Grants, AAL-Gate, beide Invite-Pfade, Twoworkspace-Isolation, Accept+Replay, Suspend-Replay, Lifecycle-Matrix, Guards, Audit-Zählungen, anon/DML-Denials), Rollback `007_p2_d01_down` (13) + `flutter.yml`-Replay-Prepend. Verifiziert: reset/lint/advisors (0 ERROR, nur Baseline-INFO), pgTAP **301/301**, Rollback-Replay `007→006→005→004→003`+up grün, `verify_p1_004/007/011/018` grün (Policy-Parität hält; ein transienter p1_018-Reset-Fehler war Container-Race, Wiederholung grün), analyze sauber, Flutter-Suite 350/9. **Benannte Lücke (per Backlog-Default, nicht erfunden):** DEC-016 (verpflichtende MFA-Regel privilegierter Rollen) bleibt offen — es gilt weiter nur das AAL2-Mutations-Gate. **Angekündigte Abweichung:** Schritt-7-Dry-Run-Mapper passt nicht auf Identity (lokale User ohne E-Mail; auth.users nicht clientseitig anlegbar) → geplant als Report-only-Reconciliation. **Increment 2 (Dart-Contract + Adapter) — `done` (2026-07-22):** Application-Contract `lib/features/identity_access/application/membership_admin_repository.dart` (DTOs inkl. `PendingInvitationEntry`/`InviteOutcome`, Command-Envelope mit `MembershipCommandContext`, sealed Results mit vollem Failure-Vokabular + typisiertem `MembershipVersionConflict` [Member- oder Invitation-Snapshot]); `supabase_membership_admin_repository_adapter.dart` (Gateway-Pattern wie Property-Adapter, Actor-Mismatch-Short-Circuit, exakte RPC-Parameter, Workspace-Scope-Checks auf jeder geparsten Entity, generische Fehlermeldungen ohne Leaks — **Umsetzung an Subagent delegiert, Verifikation eigenständig**); `legacy_sqlite_membership_admin_repository_adapter.dart` (read-only P1-006-Form: lokale User → always-active Members, Rollen = distinct Role-Strings, leere Invitation-Fläche, alle Mutationen `dependencyConflict`). Tests: Supabase-Adapter 19, Legacy-Adapter 6 (identity_access gesamt 41 grün); realer lokaler Integrationstest `test/integration/supabase_membership_admin_integration_test.dart` + Fixture `p2_d01_setup.sql` + `tool/verify_p2_d01_integration.ps1` + CI-Step (voller Lifecycle real: AAL1-Refusal, Invite-existing→invited, Pending-Liste, Accept→active v2, Suspend, Version-Conflict mit current entity, Promote, E-Mail-Invitation, Revoke). Verifiziert: analyze sauber, volle Suite **375 grün / 10 Skips** (Baseline 350/9; +19+6 Tests, +1 Skip = Integrationstest ohne Dart-Defines), Integrations-Gate grün. **Scope-Entscheidung (dokumentiert):** Member-*Directory* (Display-Namen/E-Mails für die Admin-Liste — braucht `auth.users`-joinende RPC) gehört zu Increment 3, wo der UsersScreen es konsumiert. **Design-Befund für Increment 3 (per Explore verifiziert, 2026-07-22):** Im Supabase-Modus routet `app.dart:44-51` ausschließlich auf die Reference-Slice-Präsentation (`/properties`); `AppScaffold`/`UsersScreen` sind sqlite-only (einziger Konstruktionsort `security_gate.dart:26`, nur bei sqlite als `home` verdrahtet) und `securityControllerProvider` hängt hart am sqlite-`SecurityRepo`. **Die Cloud-Rollen-UI von P2-D01 landet daher in der Reference-Slice-Präsentation** (Members-Bereich neben `/properties`), nicht im W1-AP7c-UsersScreen — das revidiert die 04a-Annahme („P2-D01-UI zieht in das fertige UsersScreen-Gerüst ein"); der legacy UsersScreen bleibt die SQLite-Modus-UI. `MembershipAdminRepository` ist noch nicht verdrahtet (kein Provider/Override/Consumer — bewusst, kommt mit Increment 3). **Increment 3 (Directory-RPC + Admin-UI + AuthorizationPort) — `done` (2026-07-22, eigenständig):** (1) Migration `20260722210000_p2_d01_member_directory`: security-definer RPC `list_workspace_members(p_workspace_id)`, `security.manage`-gated, Read (kein AAL2/Receipt/Audit), enveloped Result (`{ok,entity}`/`{ok,error:{code}}`) für distinktes Forbidden-Signal; joint `memberships × roles × user_profiles(display_name, LEFT) × auth.users(email, LEFT)`, deterministisch sortiert; Grants nur `authenticated`. Spiegelt `list_my_pending_invitations`. (2) pgTAP `009_p2_d01_member_directory` (**18**: Definer-Shape/Grants, Admin sieht email+display_name+role+status, null-Profil→null display_name, null-Workspace→validation_failed, Viewer→forbidden, Zwei-Workspace-Isolation, anon-Denial). (3) Rollback `008_p2_d01_directory_down` (4: Directory-Fn weg, 6 Lifecycle-RPCs + Invitations-Tabelle + Permission-Helper bleiben) + `flutter.yml`-Replay-Prepend (zwei relative `--last 1`-Schritte vor 007). (4) Contract: `WorkspaceMemberDirectoryEntry`-DTO + `listMemberDirectory` auf `MembershipAdminRepository`; `AuthorizationPort` (+ `PermissionSetAuthorizationPort`) in `application/` eingeführt. (5) Adapter: Supabase (Envelope→Result, Scope-Guard je Entity, generische Fehler; +4 Tests) + Legacy (lokale User→Directory-Einträge mit displayName/email, Scope-fail-closed; +2 Tests). (6) Provider `membershipAdminRepositoryProvider` (throwender Default, Muster `identityAccessRepositoryProvider`) + Override im Supabase-Zweig von `main.dart`. (7) UI im Reference Slice: `MembersAdminController` (family by `MembersAdminScope`, nutzt `AuthorizationPort.can('security.manage')`) + `ReferenceMembersScreen` (Route `/members` in `app.dart`; „Members"-Link im Properties-Header, optional/nullable → Goldens unverändert). Zwei Zonen: „Ihre Einladungen" (jeder, Accept-Banner via `listMyPendingInvitations` — funktioniert auch für invited-only-User ohne Workspace) + Directory (security.manage; sonst **Forbidden**-Zustand) mit Invite-per-E-Mail, Rollenwechsel/Suspend/Reactivate/Revoke (Bestätigungsdialog, AAL2+RBAC-gated, MFA-Hinweis-Banner bei !aal2), Pending-E-Mail-Invitations inkl. Revoke. Pflichtzustände (Skeleton/Empty/Error+Retry/Forbidden); 12 Widget-Tests (390/1024/1440) + 8 Controller-Unit-Tests. (8) Integrationstest um Directory-Coverage erweitert (Admin sieht displayName [neuer `user_profiles`-Fixture-Row] + email, Viewer→forbidden). (9) Report-only-Reconciliation (Mapper-Ersatz, angekündigte Abweichung) umgesetzt: `identity_reconciliation_report.dart` (deterministische read-only Summary: Counts, Rollenverteilung, `usersMissingEmail`=nicht-auto-provisionierbar, SHA-256-Checksum; **keine** auth.users-Erzeugung, kein Source-Write) + `sqlite_identity_reconciliation_source.dart` + 3 Unit-Tests. **DEC-016 bleibt benannte Lücke** (nur AAL2-Mutations-Gate, keine erfundene MFA-Policy); revoked-nicht-re-invitable (STM-001) unangetastet. **Verifiziert (voller `database`-Job lokal):** `db reset`/`lint`/advisors (0 ERROR), pgTAP **319/319** (+18), Rollback-Replay `008→007→006→005→004→003`+`up`, alle `tool/verify_*` grün (inkl. `verify_p2_d01_integration` mit Directory-Coverage, `p1_004/007/011/018`-Parität, `debt_012`, `p1_014`-Backup/Restore/Crash, `p1_021`-Performance), `flutter analyze` sauber, Flutter-Suite **404 grün / 10 Skips** (Baseline 375/10; +29: 6 Adapter, 12 Members-Widget, 8 Controller, 3 Reconciliation). |

| P2-D02 — `contacts_parties` (Welle-2-Backend, Gate für P2-D05/06/07; löst DUP-010/OPEN-001) | Backend (Domain-Expansion) | `done` | **Increment 1 (Schema + pgTAP + Rollback + CI-Prepend) — `done` (2026-07-22, eigenständig).** Funktions-Plan im Chat gezeigt (spiegelt P1-004 `update_property`-RPC-Form + P2-D01 Shared-Helper-Muster; **bewusste Abweichung: kein AAL2-Gate** — Parteien sind Workspace-Geschäftsdaten wie Properties, Zugriff über `party.read`/`party.manage`-Permissions statt AAL2, wie P1-004; kein offener DEC-*/OPEN-*-Punkt, daher als Default dokumentiert). Migration `20260722220000_p2_d02_contacts_parties`: Enums `party_type` (person/organization) + `party_role_type` (tenant/contractor/buyer/bank/company); **kanonische `parties`** (Standard-Cloud-Cols + `party_type`/`display_name`/`legal_name`/`email`/`phone`/`notes`/`merged_into_party_id`/`deleted_at`; PII minimiert auf ein E-Mail-/Telefon-Kanal, normalisiert); **`party_roles`** (zeitlich begrenzbar via `valid_from`/`valid_until`, Partial-Unique ≤1 offene Rolle je Typ); **`party_contractor_details`**-Satellit (einzige Rolle mit fachlichen Legacy-Attributen — trade_category/hourly_rate `numeric`/Ratings/Versicherung; keyed auf `party_id`, join auf `parties`; tenant/buyer/bank/company bekommen ihre Satelliten erst wenn ihre Domänen migrieren, dokumentiert statt leer gebaut); **`party_aliases`** (append-only Merge-Historie). Geteilte private Helfer (`party_command_gate`/`claim_party_mutation`/`finish_party_mutation`/`party_snapshot`/`party_role_snapshot`/`contractor_details_snapshot`). 6 RPCs: `create_party`, `update_party` (Optimistic-Concurrency wie `update_property`), `assign_party_role` (schreibt Contractor-Satellit bei `role_type=contractor`), `end_party_role`, `merge_parties` (re-pointet+schließt Quell-Rollen, verschiebt Satellit nur wenn Ziel keinen hat, Alias, Tombstone Quelle → Ziel, Audit `party.merge`), Read-RPC `detect_party_duplicates` (normalisierte E-Mail/Telefon/Name, permission-gated, kein Receipt/Audit/AAL). **Claim-vor-State-Validierung mit Receipt-Cleanup** (P2-D01-Befund) in assign/end/merge. Default-deny RLS + `party.read`-Read-Policies auf allen 4 Tabellen; Mutation nur via RPC. pgTAP `010_p2_d02_contacts_parties` (**68**: Schema/RLS/Grants/Definer-Shape, create/update-Happy + `version_conflict` + idempotenter Replay, assign/end + Satellit + Duplicate-Open-Reject, **Duplicate-Detection** [E-Mail/Name/Telefon-Digits, Tombstone-Ausschluss], **Party-Merge-Audit-Trail** [Tombstone+merged_into, Rollen re-pointet+geschlossen, Alias, `party.merge`-Audit, Ziel-Version-Bump, idempotenter Replay], **role-scoped Read** [Reader filtert `party_roles` nach Typ; Viewer ohne `party.read` sieht 0], Zwei-Workspace-Isolation, anon/Direct-DML-Denial, keine Pending-Receipts). 2 Assertions in `002_p1_003_rls` (Private-Funktions-Inventar) um die 6 neuen Helfer erweitert. Rollback `009_p2_d02_down` (12) + `flutter.yml`-Replay-Prepend (neuer `--last 1` → 009 vor 008). **Verifiziert (DB-Gate lokal):** `db reset`/`lint` sauber, Security- + Performance-Advisors 0 ERROR (nur Baseline-INFO `mutation_receipts` / `unused_index`), pgTAP **387/387**, Rollback-Replay `009→008→007→006→005→004→003`+`up` grün. **Increment 2 (Dart-Contract + Adapter + Adapter-Tests) — `done` (2026-07-22, eigenständig).** `lib/features/contacts_parties/{domain,application,data}`: Domain-DTOs (`PartyDto`/`PartySummaryDto`, `PartyRoleDto`, `ContractorDetailsDto`, `PartyDuplicateCandidate`, Eingaben `PartyDraft`/`PartyUpdateDto`/`ContractorDetailsInput`; Enums `PartyType`/`PartyRoleType`). Application-Contract mit den **vier DOM-003-Ports**: `PartyRepository` (getById/create/update/merge), `PartySearchPort` (keyset-paginierte, optional role-scoped `search`), `PartyRoleRepository` (listForParty/getContractorDetails/assign/end), `DuplicateDetectionPort` (detect) — `PartyCommandContext`, Command-/Query-Objekte, sealed `PartyRepositoryResult<T>` mit vollem Failure-Vokabular + typisiertem `PartyVersionConflict` (currentParty **oder** currentRole). Supabase-Adapter (eine Klasse implementiert alle 4 Ports; Gateway-Muster wie Property-Adapter; RPC-only Mutation mit exakten Parametern, `changes`-jsonb aus `PartyUpdateDto`, Contractor-Details-Payload; keyset `.gt('id')`+`limit+1`; role-scoped Read via `party_roles!inner`-Embed; Actor-Mismatch-Short-Circuit; Workspace-Scope-Guard je Entity; generische Fehlermeldungen ohne Leaks). Legacy-read-only-Adapter über `LegacyPartyReadSource` (Projektion `TenantRecord`→person+tenant, `ContractorRecord`→organization+contractor+Satellit, `ContactRecord`→Party mit aus `contacts.role` abgeleiteter Rolle; alle Mutationen `dependencyConflict`, Scope fail-closed); `PropertyModulesRepo.listAllContacts()` als minimaler additiver globaler Read ergänzt. Tests: 17 Supabase-Adapter + 10 Legacy-Adapter (`test/features/contacts_parties/`). Verifiziert: `flutter analyze` sauber, volle Suite **432 grün / 10 Skips** (Baseline 404/10; +28). **Increment 3 (Realtime + Dry-Run-Mapper + Integrationstest + CI) — `done` (2026-07-22, eigenständig).** (a) **Realtime-Invalidation** (Schritt 6): 2. Migration `20260722230000_p2_d02_party_realtime` (`alter publication supabase_realtime add table public.parties`, spiegelt P1-011); `application/party_query_invalidation_source.dart` + `data/supabase_party_query_invalidation_adapter.dart` (workspace-scoped `parties`-UPDATE-Subscription mit Coalescing/Ready-Reconcile-Guard wie P1-011); Rollback `010_p2_d02_realtime_down` (3) + `flutter.yml`-Replay-Prepend (`--last 1` → 010 vor 009); 4 Adapter-Unit-Tests. (b) **Dry-Run-Mapper** (Schritt 7, MIG-BND-001): `application/party_migration_dry_run.dart` (eigene deterministische Report-/Request-/Snapshot-/Issue-/Mapping-/Summary-Typen + SHA-256-Checksum, spiegelt P1-012-Prinzipien) + `data/sqlite_to_postgres_contacts_parties_dry_run_mapper.dart` (tenants→person+tenant, contractors→organization+contractor+Satellit, contacts→Party mit aus `role` abgeleiteter Rolle bzw. Identity-only; UUIDv5-Ziel-IDs, per-Entity Count-+Checksum-Reconciliation, read-only, keine Legacy-Tabelle gedroppt) + `data/sqlite_contacts_parties_migration_source.dart` (raw-row-Read); 7 Unit-Tests (reconciled+import-ready, deterministisch, Rollen-Ableitung, unmapped-role→Identity-only, invalid-row→invalid, invalid-request, aborted). (c) **Realer Integrationstest** (Schritt 8): `test/integration/supabase_party_repository_integration_test.dart` + Fixture `p2_d02_setup.sql` + `tool/verify_p2_d02_integration.ps1` + CI-Step — voller Lifecycle gegen lokalen Stack (create → assign contractor+Satellit → getContractorDetails → role-scoped search → detect duplicates → merge+Tombstone-Verifikation → version_conflict → update → Viewer ohne `party.read`: search leer [RLS] + detect forbidden [RPC]). (d) **CI-Wiring** (Schritt 9): flutter.yml um Realtime-Rollback-Step + P2-D02-Integrations-Step erweitert. **Provider-/main.dart-Wiring bewusst auf Welle 2 verschoben** (kein Consumer im Backend-Item; das Shared-Pattern verdrahtet Provider erst mit den abhängigen Screens — der Integrationstest konstruiert den Adapter direkt). **Verifiziert (voller `database`-Job lokal):** `db reset`/`lint` sauber, Security- + Performance-Advisors **0 ERROR/WARN** (nur Baseline-INFO), pgTAP **387/387**, Rollback-Replay `010→009→008→007→006→005→004→003`+`up` grün, `verify_p2_d02_integration` (real end-to-end) + `verify_p1_018` (PostgREST-Autorisierungs-Parität, RLS-sensibel) grün, `flutter analyze` sauber, Flutter-Suite **443 grün / 11 Skips** (Baseline 432/10; +11: 7 Mapper, 4 Realtime; +1 Skip = Party-Integrationstest ohne Dart-Defines). **P2-D02 vollständig abgeschlossen.** |

| P2-D03 — `documents_compliance` (Welle-2-Backend, Gate für P2-D05/06/07/08; löst DEBT-007/DUP-011) | Backend (Domain-Expansion) | `done` | **Increment 1 (Schema + Storage-Bucket + pgTAP + Rollback + CI-Prepend) — `done` (2026-07-23, eigenständig).** Funktions-Plan im Chat gezeigt (spiegelt die P2-D02-Vertikale: enveloped RPCs, `p_expected_version`, `mutation_receipts`-Idempotenz, Append-only-Audit, Default-deny-RLS, geteilte private Helfer, **Claim-vor-State-Validierung mit Receipt-Cleanup**; **kein AAL2-Gate** wie P2-D02/P1-004, Zugriff über `document.read`/`document.manage`/`document.verify` — letzteres bereits in CLAUDE.md als Capability benannt). **Drei Defaults vor dem ersten Edit vom Nutzer bestätigt:** (a) **ein** privater Bucket `documents` mit Pfad-Präfix `{workspace_id}/{document_id}/{version_no}/{file}` statt Bucket-pro-Workspace; (b) Signed-URL-TTL **300 s Default / 3600 s Max** (Clamp im Adapter, Increment 2); (c) **OPN-DOM-005-Register-Default** übernommen — keine automatische Löschung, `retention_until` rein informativ, kein Löschpfad und **keine DELETE-Policy**; Sperrung via Status `archived`, Entscheidung auditiert. **OPN-DOM-005 bleibt offen.** Migration `20260723100000_p2_d03_documents_compliance`: Enums `document_status` (STM-008-Vokabular), `document_verification_status`, `document_link_entity_type` (kontrollierte Registry statt DEBT-006-Freitext); 5 Tabellen — `document_types` (**workspace-scoped Registry-Tabelle, kein erfundenes Enum** — der Legacy-Befund zeigte eine nutzergepflegte `document_types`-Tabelle), `documents` (STM-008-Status + Gültigkeit; **bewusst kein `deleted_at`**, `archived` ist der terminale Zustand), `document_versions` (Pfad/Hash/Größe/MIME per Protected-Column-Trigger unveränderlich, nur Verifikations-/Supersede-Felder mutabel), `document_links` (Composite-FK ⇒ „Links workspacegleich" ist Schema-Invariante), `required_documents` (Policy-Regeln; `entity_id` null = Workspace-Regel, gesetzt = Instanz-Anforderung; `scope_key` generalisiert Legacy-`property_type` ohne Portfolio-Vokabular in DOM-006 zu ziehen). **Storage (MIG-BND-003):** privater Bucket + RLS auf `storage.objects` (SELECT=`document.read`, INSERT=`document.manage`) über `private.document_storage_workspace` (sicherer UUID-Parse des 1. Pfadsegments, null ⇒ fail-closed); **bewusst keine UPDATE/DELETE-Policy** ⇒ „Versionen werden nicht überschrieben" ist strukturell, nicht konventionell. 11 RPCs (`upsert_document_type`, `create_document`, `add_document_version`, `confirm_document_content`, `verify_document_version`, `transition_document_status`, `link_document`, `unlink_document`, `upsert_required_document` + Read-RPCs `evaluate_document_requirements`, `resolve_document_content_ref`). **DUP-011 konsolidiert:** die Legacy-Checkliste (write-only Tabelle, 20 hartkodierte Keys, Zustände `vorhanden/fehlt/angefordert/nicht_relevant`) wird zur **abgeleiteten** Projektion — `requested_at` trägt „angefordert", `waived_at/waived_by/waiver_reason` trägt „nicht_relevant" (verlustfreie Vereinigung), Erfüllung wird nie gespeichert; das 45-Tage-`expiring`-Fenster ist **verbatim aus `DocumentsRepo._resolveDocumentStatus`** übernommen, nicht erfunden. **MIG-BND-003-Upload-Verifikation:** `confirm_document_content` prüft die reale `storage.objects`-Zeile (Existenz + `metadata->>'size'`) gegen die deklarierten Werte und führt bei Abweichung den STM-008-Fehlerpfad nach `rejected`, statt einen ungeprüften Link zu veröffentlichen. Tests: pgTAP `011_p2_d03_documents_compliance` (**127**: Schema/RLS/Grants/Definer-Shape, Bucket-Privatheit + genau 2 Policies + 0 UPDATE/DELETE-Policies, **upload→confirm→verify→supersede→archive-Lifecycle inkl. Erhalt von Hash/Verifikation der superseded Version**, beide Upload-Verifikations-Fehlerpfade [fehlendes Objekt, Größen-Mismatch], STM-008-Matrix + Terminalität, Link-Port inkl. `dependency_conflict` für unmigrierte Domänen, Requirement-Projektion satisfied/expired/missing/requested/waived + Retire, **workspace-scoped Storage-Isolation** [Fremd-Präfix und malformed-Präfix unsichtbar, Overwrite/Delete unmöglich], Replay-Identität + `mutation_conflict` + keine pending Receipts + 1:1-Audit-Deckung, Immutabilität, anon/Direct-DML-Denials). Private-Funktions-Inventar in `002_p1_003_rls` um die 10 neuen Helfer erweitert. Rollback `011_p2_d03_down` (14) + `flutter.yml`-Replay-Prepend (`--last 1` → 011 vor 010). **Befund im Rollback-Test gefunden und gefixt (statt Test abgeschwächt):** `supabase migration down` baut *Schema* aus den Migrationen neu, stellt *Daten* aber aus einem Dump wieder her — die `storage.buckets`-Zeile überlebt daher den Rollback, die Policies nicht. Ohne `on conflict` wäre der `migration up`-Replay-Schritt der CI an einem Duplicate-Key gescheitert; der Insert ist jetzt idempotent und re-assertiert `public = false`, und der Down-Test prüft die sicherheitsrelevante Eigenschaft („was überlebt, ist inert: privater Bucket mit 0 Policies") statt einer Wunschaussage. **Benannte Lücke (statt erfundener Lösung):** Download-Zugriffe werden **nicht** auditiert — ein Read-RPC schreibt per Konvention kein `audit_events`; Per-Access-Logging gehört in die P2-D04-Event-Envelope. **Verifiziert (DB-Gate lokal):** `db reset`/`lint` sauber, Security- **und** Performance-Advisors **0 Issues**, pgTAP **514/514** (Baseline 387; +127), Rollback-Replay `011→010→009→008→007→006→005→004→003`+`up` grün, danach volle Suite am Head erneut 514/514. **Increment 2 (Dart-Contract + beide Adapter + Realtime-Invalidation) — `done` (2026-07-23).** `lib/features/documents_compliance/{domain,application,data}`: Domain-DTOs (`DocumentTypeDto`/`DocumentDto`/`DocumentVersionDto`/`DocumentLinkDto`/`RequiredDocumentDto`/`DocumentRequirementProjection`/`DocumentContentRef`/`SignedDocumentUrl` + Eingaben; Enums `DocumentStatus` [STM-008], `DocumentVerificationStatus`, `DocumentLinkEntityType` [kontrollierte Registry], `DocumentRequirementState` [verlustfreie Vereinigung aus Legacy-Compliance **und** Legacy-Checkliste]). Application-Contract mit den **sechs DOM-006-Ports** (`DocumentRepository`, `DocumentContentPort`, `DocumentLinkPort`, `RequirementPolicyRepository`, `DocumentVerificationPort`, `SignedUrlPort`), sealed `DocumentRepositoryResult<T>` + typisierter `DocumentVersionConflict` (trägt **immer** das aktuelle Dokument — anders als P2-D02, wo Party *oder* Rolle möglich ist, da alle versionierten Document-RPCs die Dokument-Version führen). **TTL-Policy lebt an genau einer Stelle:** `SignedUrlPort.clampTtl` (Default 300 s, Max 3600 s) — der Adapter re-implementiert sie nicht, sondern ruft den statischen Helper. **Bewusste Abweichung von DOM-006 (dokumentiert statt still):** die Dokumenttyp-Registry (`listTypes`/`upsertType`) hängt an `RequirementPolicyRepository` statt an einem siebten Port — Typen definieren, welche Anforderungen existieren können, und DOM-006 nennt exakt sechs Ports. Supabase-Adapter (eine Klasse, alle 6 Ports; Gateway-Seam über `SupabaseClient`, RPC-only-Mutation mit exakten Parametern, Hash als **Lowercase-Hex** [keine Bytes über die Leitung], keyset-Suche, Entity-Filter via `document_links`-Inner-Join-Embed, Actor-Mismatch-Short-Circuit **vor** jedem Netzwerkaufruf, Workspace-Scope-Guard je geparster Entity); **`confirmContent` behandelt `rejected` als Erfolg mit Status `rejected`, nicht als Failure** — sonst würde der MIG-BND-003-Fehlerpfad als Infrastrukturfehler verschwinden. Legacy-read-only-Adapter über `LegacyDocumentReadSource` (Projektion `DocumentRecord`→Dokument + **ein** synthetischer `DocumentVersionDto` mit `storageBucket: ''` — das leere Bucket **ist** die sichtbar gemachte DEBT-007-Lücke —, Statusableitung und 45-Tage-Fenster verbatim aus der Legacy-Logik, nicht mappbarer Legacy-`entity_type` degradiert zu „kein Link" statt zu werfen; alle 9 Mutationen und `createSignedUrl` → `dependencyConflict`, Fremd-Workspace → `forbidden` **ohne** zu lesen). Realtime (Schritt 6): 2. Migration `20260723110000_p2_d03_document_realtime` + `DocumentQueryInvalidationSource` + Supabase-Adapter (Muster P1-011/P2-D02) + Rollback `012_p2_d03_realtime_down` (4) + `flutter.yml`-Replay-Prepend. **Ehrlich benannte Realtime-Lücke (statt überzogener Behauptung):** `link_document`/`unlink_document`/`upsert_required_document` fassen die `documents`-Zeile **nicht** an und lösen daher keine Invalidierung aus; die Kind-Tabellen stattdessen zu publizieren löst es nicht sauber (DELETE-Payloads tragen nur Replica-Identity-Spalten, ein `workspace_id`-Filter wäre ohne `REPLICA IDENTITY FULL` unmöglich), und die Eltern-Zeile mitzubumpen würde das Optimistic-Concurrency-Token unter laufenden Clients verschieben. Cross-Table-Invalidierung bleibt damit explizit für **P2-D04** offen (dessen erklärter Zweck genau diese Verallgemeinerung ist, CTR-005). **Delegations-Hinweis:** die beiden Adapter wurden an Subagenten delegiert; **beide brachen extern mit Session-Limit-API-Fehler ab** (gleiche Fehlerklasse wie bei W1-AP7), hatten die Adapter aber bereits geschrieben und starben in der Verifikationsphase — es existierten **keine Tests**. Adapter daher im Main-Loop reviewt (1 Analyzer-Fehler gefixt, `Supabase.instance`-Leck ausgeschlossen, Invarianten gegen die SQL geprüft) und **alle 39 Tests eigenständig geschrieben**. Dabei zwei Test-Befunde geklärt statt weggedrückt: der Server-`message` wird bewusst durchgereicht (P2-D02-Konvention, es sind unsere eigenen kontrollierten Strings) — getestet wird stattdessen, dass **unerwartete Exception-Texte** nie durchschlagen; und SQL-`date` wird als lokale Kalender-Mitternacht geparst (kein Tagesversatz). **Verifiziert:** `flutter analyze` sauber, volle Suite **482 grün / 11 Skips** (Baseline 443/11; +39: 19 Supabase-Adapter, 14 Legacy-Adapter, 6 Realtime), Rollback-Replay `012→011`+`up` grün. **Increment 3 (Dry-Run-Mapper + realer Integrationstest + CI) — `done` (2026-07-23, eigenständig).** (a) **Dry-Run-Mapper** (Schritt 7, MIG-BND-003): `application/document_migration_dry_run.dart` (eigene deterministische Report-/Request-/Snapshot-/Issue-/Mapping-/Summary-Typen + SHA-256-Manifest, spiegelt P2-D02/P1-012) + `data/sqlite_to_postgres_documents_compliance_dry_run_mapper.dart` + `data/sqlite_documents_compliance_migration_source.dart` (raw-row-Read + streamendes Hashing der lokalen Dateien). Vier Entitäten: `document_types`, `documents` (je Zeile Dokument + `document_versions`-Zeile + optionaler `document_links`-Satz), `required_documents` und — als **DUP-011-Hälfte** — `property_document_checklist` (je Zeile eine **Instanz**-Anforderung mit `entity_id` plus ein je `document_key` synthetisierter Dokumenttyp; `angefordert`→`requested_at`, `nicht_relevant`→`waived_at`/`waived_by`/`waiver_reason`, `vorhanden`/`fehlt` speichern nichts, weil Erfüllung abgeleitet wird). **Der P2-D03-spezifische Teil (Legacy-`file_path` statt Cloud-Objekt):** der Snapshot trägt je Legacy-Pfad eine `DocumentContentProbe` (Existenz/Größe/SHA-256 der echten Bytes) und je Ziel-Objektpfad eine `DocumentObjectProbe`; der Mapper hasht **zuerst** den Inhalt und verifiziert **danach** den Upload gegen genau diesen Hash, entlang der Zustandsmaschine `sourceMissing → hashMismatch → uploadMissing → uploadMismatch → uploadUnhashed → verified`. `contentLinkAuthorized` ist nur `true`, wenn der Report vollständig reconciled **und** jedes Dokument `verified` erreicht hat. **Fail-closed-Entscheidung (dokumentiert statt still):** Supabase Storage liefert nativ die Größe, aber keinen SHA-256 — reine Größengleichheit gilt daher als eigener, **nicht** autorisierender Zustand `uploadUnhashed` statt als Verifikation. **Strukturelle Absicherung von „vor der Reconciliation nichts umschalten":** der Mapper emittiert Dokumente ausnahmslos mit `status: uploaded` und `content_confirmed_at: null` — Veröffentlichen bleibt `confirm_document_content`, das Storage serverseitig erneut prüft. Property-Links nutzen die **P1-012-Namespace-Ableitung** (`neximmo/p1-012/property/<id>`), damit sie auf die tatsächlich migrierte Zeile zeigen; für alle anderen Domänen wird bewusst **kein** Link erzeugt (fremde Migrations-Namespaces ⇒ sonst hängende Referenzen), mit eigenem Issue-Code. 21 Unit-Tests, darunter der entscheidende Zwei-Pass-Test: **die geplanten Zeilen sind vor und nach dem Upload byte-identisch** (gleiche `candidateChecksum`/`targetChecksum`), nur die Autorisierung kippt. (b) **Realer Integrationstest** (Schritt 8): `test/integration/supabase_document_repository_integration_test.dart` + `tool/verify_p2_d03_integration.ps1` + CI-Step, gegen den lokalen Stack: Typ-Registry → Anforderungsregel → **echter Upload über die Storage-API** → create → confirm → link → verify → `evaluate`-Projektion `missing`→`pendingVerification`→`satisfied` → supersede (die superseded Version behält Hash **und** Verifikation) → Version-Conflict mit current entity → archive (aktive Liste vs. `includeInactive`-Auditsicht) → **MIG-BND-003-Fehlerpfad** (deklariertes, nie hochgeladenes Objekt ⇒ `rejected`) → **Signed-URL** (Clamp einer 10-h-Anforderung auf 1 h; 1-s-URL per echtem HTTP zuerst 200, nach Ablauf ≠200) → **workspace-scoped Storage-Isolation** über die echte API (Upload unter Fremd-Präfix und unter malformed Präfix abgewiesen; Viewer: `search` leer, `resolveContentRef`/`createSignedUrl` `forbidden`, `download` abgewiesen, `list` leer, Upload abgewiesen). Das Skript startet zusätzlich den `supabase_storage`-Container neu (analog zum Kong-Restart) und wartet auf dessen Health, weil Storage Bucket-/Policy-Zustand über einen `db reset` hinweg cached. (c) **Vom Integrationstest gefundener Adapterfehler (gefixt, statt Test abgeschwächt):** `listVersions` liest `document_versions` direkt über PostgREST, wo die `bytea`-Spalte `content_hash` als `\x2d09…` serialisiert wird, während alle RPC-Pfade `encode(content_hash,'hex')` liefern — der geteilte `_parseVersion` nahm die RPC-Form an und verletzte auf genau diesem Lesepfad die DTO-Zusage „lowercase hex". Fix an der Parse-Naht (`_parseContentHash`: Präfix entfernen, lowercase, SHA-256-Form validieren) für `_parseVersion` **und** `_parseContentRef`. Die 39 Adapter-Tests aus Increment 2 konnten das nicht sehen, weil die Fixture den Hash als sauberen Hex hartkodierte; sie nimmt ihn jetzt als Parameter, +2 Tests (Normalisierung, Zurückweisung eines Nicht-SHA-256). (d) **CI-Wiring** (Schritt 9): `flutter.yml` um den P2-D03-Integrations-Step erweitert (die Rollback-Prepends 011/012 lagen aus Increment 1/2 bereits vor). **Bewusste Grenze (benannt statt kaschiert):** `UploadedDocumentObjectInventory` hat als Default eine leere Implementierung — ein erster Dry-Run läuft absichtlich **vor** dem Upload und listet dann exakt die noch fehlenden Objektpfade (`pendingUploadPaths`); eine Storage-listende Inventory-Implementierung gehört zum tatsächlichen Migrationslauf, nicht zum Dry-Run. **Verifiziert (voller `database`-Job lokal):** `db reset`/`lint` sauber, Security- und Performance-Advisors **0 ERROR/WARN** (nur Baseline-INFO `mutation_receipts` / `unused_index`), pgTAP **514/514**, Rollback-Replay `012→011→010→009→008→007→006→005→004→003`+`up` grün (danach erneut 514/514 am Head), **alle** `tool/verify_*`/`test_*` grün — inkl. des neuen `verify_p2_d03_integration` sowie `p1_004/007/011/018`, `debt_012`, `p2_d01`, `p2_d02`, `p1_014` (Guard/Corrupt-Archive/Restore/Crash) und `p1_021` (Guard + Profil, Artefakt vorhanden); `flutter analyze` sauber, Flutter-Suite **505 grün / 12 Skips** (Baseline 482/11; +23: 21 Mapper, 2 Adapter-Hash; +1 Skip = Dokument-Integrationstest ohne Dart-Defines). Ein transienter `verify_p2_d01_integration`-Reset-Fehler (Container-Race, gleiche Klasse wie früher bei `p1_018`) war bei Wiederholung grün. **OPN-DOM-005 bleibt offen**; Download-Zugriffe bleiben unauditiert und Cross-Table-Realtime-Invalidierung (`link`/`unlink`/`upsert_required_document`) bleibt wie in Increment 2 benannt **P2-D04** vorbehalten. **P2-D03 vollständig abgeschlossen.** |

| P2-D04 — `platform_audit_jobs` Full Expansion (Welle-2-Backend, CTR-005-Envelope; Gate für DEBT-009) | Backend (Domain-Expansion) | `done` | **P2-D04 vollständig abgeschlossen (`done`, 2026-07-24).** Alle vier Increments verifiziert; voller `database`-Job-Gate lokal grün. **Increment 4 (Dart-Contract + Adapter + Realtime-Consumer + Outbox-Reader + Dry-Run-Mapper + realer Integrationstest + CI) — `done` (2026-07-24, eigenständig; die zwei Legacy-Adapter/Mapper-Teile an Subagenten delegiert, reviewt und mit korrigierter Severity übernommen).** Neues Feature `lib/features/platform_audit_jobs/{domain,application,data}`, spiegelt die `contacts_parties`/`documents_compliance`-Vertikalen. **domain/**: `platform_entity_type.dart` (`PlatformEntityType` + `PlatformEntityRef`; **spiegelt die SQL-`document_link_entity_type`-Registry verbatim statt sie aus `documents_compliance` zu importieren** — eine migrierte Vertikale darf nicht von einer anderen abhängen; abgesichert durch `platform_entity_type_parity_test.dart`, der bei Drift der beiden Wire-Vokabulare bricht), `task_dto.dart` (`TaskStatus` STM-012 mit client-seitigem `canTransitionTo`-Spiegel, `TaskPriority`, `TaskDto`, `TaskDraft`, `TaskUpdateDto` mit `TaskFieldEdit`-Tri-State „absent/set/clear" — trennt „Feld unberührt lassen" von „auf null setzen", was ein nullbares Feld nicht ausdrücken kann), `notification_dto.dart` (`NotificationDto`, `NotificationDraft`, `NotificationFanOutReceipt` — der Fan-out gibt die Batch-Quittung zurück, **keine** Einzelzeile, weil der Server bewusst keine „*die* Empfängerzeile" wählt), `import_job_dto.dart` (`ImportJobStatus` STM-013, `ImportJobDto` mit `hasCommitEvidence` aus dem Serverzustand gelesen, `ImportJobTransitionEvidence` als geschlossene Drei-Konstruktor-Menge `none()/ready()/failure()` — jedes Artefakt reitet genau eine Transition), `search_entry_dto.dart` (bewusst ohne Version — DOM-010-abgeleitet). **application/**: `platform_repository.dart` mit den vier Daten-Ports `TaskRepository`/`NotificationPort`/`JobRepository`/`SearchIndexPort` (Methoden aggregat-qualifiziert, da ein Adapter mehrere Ports zugleich erfüllt und Dart kein per-Interface-Method-Scoping hat), sealed `PlatformRepositoryResult<T>` + typisierter `PlatformVersionConflict` (trägt **immer** genau eines von `currentTask`/`currentImportJob` — Notifications haben kein `expectedVersion`, Search-Einträge keine Version), `PlatformKeysetCursor` als **Composite (timestamp, id)** weil `now()` transaktionsgebunden ist und ein reiner Timestamp einen Fan-out-Feed nicht ordnen kann; `platform_domain_event.dart` (CTR-005-`DomainEventEnvelope` + **zwei** Ports: `DomainEventConsumer` als Live-Weckung, `OutboxPort` als autoritative, wiederaufsetzbare Nachlese — die Outbox ist die Wahrheit, der Broadcast nur Transport); `platform_query_invalidation_source.dart` (`PlatformQueryInvalidation` mit `reconcile`-Signal). **data/**: `supabase_platform_repository_adapter.dart` (ein `SupabasePlatformGateway`-Seam, RPC-only-Mutation mit den **exakten** Parametern aus `20260723130000`/`140000` [SQL-vs-Dart-Parameter-Diff geprüft], Actor-Mismatch-Short-Circuit **vor** jedem Netzaufruf, Workspace-Scope-Guard je Zeile, Keyset-Reads mit dem Composite-Cursor; der Search-Index-Pfad ist **un-enveloped** — `reindexSearchEntry`/`removeSearchEntry` tragen kein `mutation_id`/keine Version, ein `version_conflict` von dort gilt als Vertragsbruch, nicht als behebbarer Concurrency-Fehler), `supabase_domain_event_consumer_adapter.dart` (subscribt die drei permission-gescopten Topics `workspace:<id>:<task.read|notification.read|import.read>`, filtert den Subscription-Ready-Marker aus dem Envelope-Strom, mappt `notification_batch`/`notification` beide auf `PlatformAggregate.notification`; ein Envelope einer fremden Domäne auf demselben Topic wird **übersprungen**, nicht geworfen; ein einzelnes verweigertes Topic liefert nichts, erst **alle drei** verweigert werfen), `supabase_outbox_adapter.dart` (Nur-Lese-Nachlese, append-only, aufsteigender Composite-Keyset), `legacy_sqlite_platform_repository_adapter.dart` (read-only-Projektion; **alle** Mutationen → `dependencyConflict`, Fremd-Workspace → `forbidden` **ohne** zu lesen; `todo`→`open`; unmappbare Status/Priority/Entity-Typen degradieren statt zu werfen), `sqlite_to_postgres_platform_audit_jobs_dry_run_mapper.dart` + `_migration_source.dart` (deterministisch, UUIDv5, SHA-256, per-Entity-Count+Checksum-Reconciliation; der **`todo`→`open`-Rename** landet hier). **`ImportsRepository.listMappings`** neu ergänzt (vorher las nichts die `import_mappings`-Tabelle vollständig aus — ohne sie hätte der Legacy-Adapter Jobs mit leerem `mapping` projiziert, obwohl Mapping-Zeilen existieren). **Korrigierter Subagenten-Befund (statt still übernommen):** der Dry-Run-Mapper stufte legacy `running`/`succeeded`-Importjobs als **Error** ein (AGG-020: keine Commit-Evidenz produzierbar), was `productionImportReady` **permanent** false gelassen und die Task-/Notification-Migration mitblockiert hätte — auf **Warning** `import_job.history_not_migratable` geändert: die Zeile wird weiterhin abgewiesen (kein Target), aber unmigrierbare Betriebshistorie nimmt die anderen Aggregate nicht in Geiselhaft; Test entsprechend auf `status == ready`/`productionImportReady == true` umgestellt. **Realer Integrationstest** `test/integration/supabase_platform_repository_integration_test.dart` (3 Tests, gegen den lokalen Stack, `skip:` ohne Dart-Defines wie der Dokument-Test): (1) STM-012-Lifecycle inkl. auditiertem Reopen + Terminalität, Sparse-Update mit Clear-vs-absent, Version-Conflict mit current entity, archivierte Task verlässt Arbeitsliste bleibt aber im Audit, **AGG-019**-Konvergenz (gleicher `generated_key` → dieselbe Task), Fan-out + recipient-scoped Feed (Empfänger ohne `notification.read` sieht nur die eigene Zeile, Fremd-Notification → `not_found` nie `forbidden`); (2) STM-013 + **AGG-020** (Mapping friert nach `validating` ein; kein `ready` ohne Dry-Run+Reconciliation; Artefakt auf falscher Transition abgewiesen; `completed` terminal) + Search-Index-Upsert (content-adressiert, last-writer-wins, idempotenter Delete); (3) **CTR-005-Broadcast real empfangen** + Outbox-Replay + permission-/workspace-Isolation (Viewer mit nur `workspace.read` sieht 0 Tasks, `forbidden` bei Mutation; Fremd-Workspace `forbidden`/leer). **Echter Race gefunden und im Test behoben (statt kaschiert):** ein Realtime-Channel meldet `subscribed`, **bevor** der Server DB-Broadcasts an ihn routet — eine Single-Shot-Assertion testete den Race, nicht den Vertrag; der Broadcast-Test feuert die Sonde jetzt wiederholt (die Outbox-Zeile ist genau deshalb durable), irgendeine ankommende Sonde beweist den Zustellpfad. Kein Realtime-Container-Restart im Guard-Script (probiert, half nicht — nicht als Aberglaube mitgeschleppt). `tool/verify_p2_d04_integration.ps1` + `p2_d04_setup.sql` (3 Mitglieder: Manager voll, Recipient nur `task.read`+`workspace.read`, Viewer nur `workspace.read`; zweiter Workspace für Isolation) + `flutter.yml`-Step nach P2-D03. **Provider-/`main.dart`-Wiring bewusst zurückgestellt** auf die Welle-2-Screens (kein Backend-Consumer heute), exakt wie `contacts_parties`/`documents_compliance`; der Integrationstest konstruiert die Adapter direkt. **Verifiziert (voller `database`-Job-Gate lokal):** `db reset`/`lint` sauber, Security- + Performance-Advisors **0 ERROR/WARN** (nur Baseline-INFO `mutation_receipts`/`unused_index`), pgTAP **696/696** (unverändert — Increment 4 ist reine Dart-Schicht), Rollback-Replay `015→014→013`+`up`+erneut **696/696** am Head, DB-berührende `tool/verify_*` grün (`p1_004` nach eigenem `db reset` zuerst, `p1_011` Realtime-e2e, `p1_018` PostgREST, **neu `verify_p2_d04_integration`**), `flutter analyze` sauber (ganzes Projekt), Flutter-Suite **612 grün / 15 Skips** (Baseline 505/12; +107 Feature-Tests [19 Supabase-Adapter, 16 Legacy-Adapter, 37 Dry-Run-Mapper, 15 Consumer, 4 Outbox, 3 Entity-Type-Parity, +weitere], +3 Skips = die 3 Integrationstest-Subtests ohne Dart-Defines). **P2-D04 vollständig abgeschlossen. Nächstes Item = Welle-2-Screens (`04b_wave2_...md`), das zuerst als Wellendokument geschrieben werden muss — Wellengrenze, vor Start beim Nutzer rückversichern.** **DEBT-009** (serverseitiger Scheduler-Job für periodische Task-Generierung) bleibt offen; das Task-Aggregat + `generated_key`-Idempotenz stehen, der Job fehlt. **OPN-DOM-005** bleibt offen. **Increment 3 (ImportJob-Aggregat [STM-013/AGG-020] + abgeleiteter SearchIndex [DOM-010]) — `done` (2026-07-24, eigenständig).** Funktions-Plan im Chat gezeigt; spiegelt die Increment-2-Vertikale exakt. Migration `20260723140000_p2_d04_import_jobs_search_index`: Enum `import_job_status` (STM-013: `draft→validating→ready→running→completed`, Fehler nur aus `validating`/`running`, `completed`/`failed` terminal). **`import_jobs`** — **AGG-020 als Schema-Invariante, nicht als Seitenkanal**: `mapping` (im Draft deklariert, danach eingefroren), `dry_run`/`reconciliation`/`error_report` als explizite Spalten, deren Präsenz per Check-Constraint an den Status gebunden ist: `import_jobs_commit_evidence_check` erzwingt, dass **kein** Job `ready` (und damit kein Commit) ohne Dry-Run **und** Reconciliation erreicht; `failed` trägt immer `error_report`; terminale Jobs tragen `finished_at`, laufende `started_at`. Legacy-`import_mappings` (per-Ziel-Zeilen) kollabiert in `mapping` (ein Job = ein `target_scope`, dokumentiert). 3 RPCs, spiegeln create/update/transition_task: `create_import_job` (→draft, `import.manage`), `update_import_job` (Mapping/Source/Scope nur im Draft, Optimistic Concurrency; Status nicht editierbar), `transition_import_job_status` (STM-013-Matrix via neuem `private.import_job_status_can_transition`; hängt Dry-Run+Reconciliation an `→ready`, Error-Report an `→failed`, und **kein** Artefakt darf die falsche Transition reiten). Wiederverwendung der geteilten `platform_*`-Helfer, `mutation_receipts`-Idempotenz (Claim-vor-State-Validierung + Receipt-Cleanup), Append-only-Audit, publiziert `import_job.created`/`import_job.status_changed` (`import.read`) via `private.publish_domain_event`. Retry = neuer Job (kein Pfad aus terminal). **`search_index`** — die **bewusst relaxierte, abgeleitete DOM-010-Ausnahme** („Suchindex ist abgeleitet und keine Wahrheit"): **kein** Version-Token, **kein** Audit, **kein** `mutation_receipt` — ein Reindex ist ein content-adressiertes Upsert auf (workspace, entity_type, entity_id), idempotent per Konstruktion (last-writer-wins ist die korrekte Semantik; ein Receipt würde ein legitimes Re-Projizieren fälschlich blockieren). Es ist die **einzige** Tabelle hier mit DELETE-Pfad (veraltete Index-Zeilen müssen entfernbar sein). 2 RPCs (`reindex_search_entry` Upsert, `remove_search_entry` idempotenter Delete), `search.reindex`-gated, RPC-only, Default-deny-RLS (`search.read`), domänenagnostisch (generische title/subtitle/body — DOM-010 trägt keine Fachmodelle); reused `document_link_entity_type`-Registry; kein Domain-Event (terminale Projektion, Consumer lesen direkt). Neue Permissions `import.read`/`import.manage`/`search.read`/`search.reindex` (in Tests geseedet, wie `task.*`); neue private Helfer `import_job_snapshot`/`import_job_status_can_transition`/`search_index_snapshot` (Private-Inventar in `002_p1_003_rls` erweitert). pgTAP `014_p2_d04_import_jobs_search_index` (**71**: Schema/RLS/Grants/0-Write-Policies-und-Grants, STM-013-Matrix + voller Lifecycle draft→…→completed, **AGG-020 strukturell** [`ready` ohne Dry-Run/Reconciliation per Direct-DML-CHECK **und** per RPC abgewiesen, `failed` ohne Report abgewiesen], Version-Conflict + Replay + Receipt-Cleanup + No-op-/Artefakt-auf-falscher-Transition-/Draft-only-Edit-Reject, Terminalität [completed/failed → keine Transition], **SearchIndex** [Upsert-Idempotenz last-writer-wins, DELETE idempotent, ungültiger entity_type], Permission-Denials [Member ohne import.manage/import.read/search.reindex/search.read], Append-only-Audit [3 Creates] + **kein Audit für Reindex** [als Superuser geprüft, da Manager kein `audit.read` hält], Domain-Events publiziert, Zwei-Workspace-Isolation, Direct-DML-/anon-Denial). Rollback `015_p2_d04_import_jobs_down` (**8**: import_jobs/search_index/Enum/RPCs/neue Helfer entfernt, Increment-1/2-Aggregate überleben) + `flutter.yml`-Replay-Prepend (`015→014`). **Verifiziert (DB-Teil des Gates lokal):** `db reset`/`lint` sauber, Security- + Performance-Advisors **0 ERROR/WARN** (nur Baseline-INFO `mutation_receipts`/`unused_index`, inkl. des neuen `import_jobs_workspace_idx`), pgTAP **696/696** (Baseline 625; +71), Rollback-Replay `015→014`+`up`+erneut 696/696 am Head, DB-berührende `tool/verify_*` grün (`p1_011` Realtime-e2e, `p1_018` PostgREST-Autorisierung, `p1_004` Concurrency — Letzteres schlug zunächst mit `forbidden` fehl, weil das Script kein eigenes `db reset` macht und Rest-Daten des vorher gelaufenen `p1_018`-Integrationstests [`property.read`/`property.update`-Permission-Keys] mit den Fixed-UUIDs der p1_004-Fixture kollidierten und deren Setup vor dem Membership-Insert abbrach; nach sauberem `db reset` grün — additive Schema-Änderung berührt Property-Autorisierung nicht, was pgTAP `003` bestätigt), `flutter analyze` sauber, Flutter-Suite **505/12** (kein Dart in diesem Increment — additive DB-Schicht; Contract/Adapter kommen in Increment 4). **Increment 4 offen:** Dart-Contract für alle P2-D04-Aggregate + Adapter + Realtime-Consumer (`domain_events`) + realer Integrationstest + CI-Step; der `todo`→`open`-Rename der Legacy-`tasks.status` landet im Task-Dry-Run-Mapper dort. **DEBT-009** (Scheduler-Job) bleibt offen. **Increment 2 (Task- + Notification-Aggregate) — `done` (2026-07-23, eigenständig).** Funktions-Plan im Chat gezeigt; spiegelt die P2-D02/D03-Vertikale exakt (enveloped RPCs, `p_expected_version`, `mutation_receipts`-Idempotenz mit Claim-vor-State-Validierung + Receipt-Cleanup, Append-only-Audit, Default-deny-RLS, geteilte private `platform_*`-Helfer; **kein AAL2-Gate** — Tasks/Notifications = Workspace-Geschäftsdaten, `task.read`/`task.manage`, `notification.read`/`notification.manage`). Migration `20260723130000_p2_d04_tasks_notifications`: Enums `task_status` (STM-012) + `task_priority` (low/normal/high). **`tasks`** (STM-012; optionaler Entity-Link **reused** die kontrollierte `document_link_entity_type`-Registry — deren Werte *sind* die Workflow-Entitäten, ein Parallel-Enum wäre reine Debt, dokumentiert statt still). **AGG-019:** `generated_key` nullable + Partial-Unique `(workspace_id, generated_key)`; weil STM-012-`archived` terminal ist und keinen Löschpfad hat, **ist die Task-Zeile selbst das dauerhafte Idempotenz-Ledger** — keine separate `task_generated_instances`-Tabelle (zweite Wahrheit vermieden); `create_task` mit existierendem `generated_key` gibt die bestehende Task als Erfolg zurück (idempotente Generierung), distinkt von der `mutation_id`-Replay-Schicht. RPCs: `create_task`, `update_task` (Optimistic Concurrency; Status **nicht** editierbar), `transition_task_status` (STM-012-Matrix via `private.task_status_can_transition`, `archived` terminal, `done→open` Reopen). **`notifications`** recipient-adressiert (`recipient_user_id`) ⇒ `create_notification(recipient_user_ids uuid[], …)` ist der **Fan-out** (eine Zeile je Empfänger, **ein** `mutation_id`); `mark_notification_read` (nur Empfänger, idempotent). RLS: Empfänger sieht eigene, `notification.read` sieht den ganzen Feed. Alle Aggregate publizieren über den **Increment-1-Helfer** `private.publish_domain_event` (`task.created`/`task.updated`/`task.status_changed` mit `task.read`; **eine** grobe, permission-gescopte `notification.fanned_out`-Envelope ohne Per-Empfänger-Leak — die Per-Empfänger-Weckung ist Consumer-Sache in Increment 4). Geteilte Helfer: `platform_command_gate`, `claim_platform_mutation`, `finish_platform_mutation`, `task_snapshot`, `notification_snapshot`, `task_status_can_transition` (Private-Inventar in `002_p1_003_rls` erweitert). pgTAP `013_p2_d04_tasks_notifications` (**57**: Schema/RLS/Grants, 0 Write-Policies/Grants, AGG-019-Ledger-Constraint, STM-012-Matrix + voller Lifecycle inkl. Reopen-Sperre bei terminal, No-op-Transition-Reject, `update_task` verweigert Status-Bewegung, Assignee-muss-aktives-Mitglied, Fan-out zählt Empfänger + eine Zeile je Empfänger + Replay ohne Doppel-Insert + leerer/Fremd-Empfänger-Reject, recipient-scoped `mark_read` [Fremd-Notification → `not_found`, nicht `forbidden`], Read-Scoping [Mitglied ohne `task.read` sieht 0 Tasks, ohne `notification.read` nur eigene], Permission-Denial, Append-only-Audit, `generated_key` protected, Domain-Events publiziert, Zwei-Workspace-Isolation, anon-Denial). Rollback `014_p2_d04_tasks_notifications_down` (6) + `flutter.yml`-Replay-Prepend (`014→013`). **Vom pgTAP gefundener echter Schema-Befund (gefixt):** `mutation_receipts` hat `result_pair_check` (`result_entity_type`/`result_entity_id` both-or-neither); der Fan-out-Batch hat einen Typ, aber keine einzelne ID. `finish_platform_mutation` schreibt die Receipt-Result-Spalten jetzt als Paar (beide null bei fehlender Einzel-ID) — die Audit-Zeile trägt weiter `notification_batch`, was der Replay nachschlägt; die Receipt-Result-Spalten sind ohnehin nur informativ. **Verifiziert (DB-Teil des Gates lokal):** `db reset`/`lint` sauber, Advisors **0 ERROR/WARN** (nur Baseline-INFO), pgTAP **625/625** (Baseline 568; +57), Rollback-Replay `014→013`+`up` grün, danach erneut 625/625 am Head, DB-berührende `tool/verify_*` (p1_004/011/018, p2_d03) grün, `flutter analyze` sauber (kein Dart in diesem Increment). **Increment 3–4 offen:** (3) `import_jobs` (STM-013, Dry-Run/Reconciliation AGG-020) + `search_index`; (4) Dart-Contract/Adapter/Realtime-Consumer + realer Integrationstest + CI-Step. **DEBT-009** hängt weiter an Increment 2/3 (Task-Aggregat steht jetzt, der serverseitige Scheduler-Job fehlt noch). **Increment 1 (CTR-005-Event-Envelope + Outbox + generalisierte Invalidierung) — `done` (2026-07-23, eigenständig).** Funktions-Plan im Chat gezeigt. **Ist-Befund zuerst (nicht aus dem Plan-Vokabular abgeleitet):** `audit_events` und `mutation_receipts` aus P1-002 bedienen die DOM-010-Ports `AuditSink` und `IdempotencyStore` bereits — P2-D04 ist damit tatsächlich ein *extends*-Item. Es existieren **zwei** Invalidierungsmechanismen: P1-011 publiziert ganze Tabellen nach `supabase_realtime`, P1-017 sendet per `realtime.send()` auf ein Nutzer-Topic mit RLS-Policy auf `realtime.messages` und degradiert bei Ausfall bewusst nur mit `warning`. **Gewählt: P1-017s Broadcast-Form, nicht P1-011s Tabellen-Publikation** — genau das löst die von P2-D03 benannte Lücke: DELETE-Payloads der WAL-Publikation tragen nur Replica-Identity-Spalten (kein `workspace_id`-Filter ohne `REPLICA IDENTITY FULL`), und das Bumpen der Elternzeile hätte das Optimistic-Concurrency-Token unter laufenden Clients verschoben; ein AFTER-Trigger sieht dagegen die vollständige OLD/NEW-Zeile. Migration `20260723120000_p2_d04_domain_event_envelope`: **`domain_events`** als append-only Outbox in CTR-005-Feldern (`id`=eventId, `event_type`, `schema_version`, `workspace_id`, `aggregate_type` [Zusatz zu CTR-005: eine ID allein identifiziert domänenübergreifend kein Aggregat], `aggregate_id`, `aggregate_version`, `occurred_at`, `actor_id`, `correlation_id`, `payload`) — **die Outbox ist die Wahrheit, der Broadcast nur Transport**; ein fehlgeschlagener Broadcast darf die auslösende Mutation nie scheitern lassen (P1-017-Muster). **Sicherheitsentscheidung (tragend):** ein workspace-weiter Stream würde die Existenz und IDs von Zeilen verraten, die der Leser nicht sehen darf (ein `document.linked`-Event bei jemandem ohne `document.read`) — jede Envelope trägt daher ihr eigenes **`required_permission`**, und sowohl die Tabellen-Policy als auch das Broadcast-Topic (`workspace:<id>:<permission>`) sind danach gescoped; Topic-Parsing über zwei `private`-Helfer, die bei malformed Segmenten `null` liefern ⇒ **fail-closed** statt Exception in der Policy. Append-only ist strukturell (BEFORE-UPDATE/DELETE-Trigger + **keine** INSERT/UPDATE/DELETE-Policy + kein Write-Grant); `payload` ist auf ein `jsonb`-Objekt ≤ 4096 Byte begrenzt — **die Envelope ist ein Zeiger, keine Kopie**. **Beide von P2-D03 offen gelassenen Punkte geschlossen:** (1) Cross-Table-Invalidierung via AFTER-Trigger auf `document_links` (insert/delete → `document.linked`/`document.unlinked`) und `required_documents` (insert/update → `document.requirement_changed`), per Test belegt **ohne** Bewegung der `documents.version`; (2) Per-Access-Recording von Downloads — `resolve_document_content_ref` publiziert `document.content_accessed`. **Bewusster, dokumentierter Konventionsbruch:** der Zugriff landet in der Outbox, **nicht** in `audit_events`, damit „`audit_events` verzeichnet Mutationen" intakt bleibt; die Funktion verliert dafür ihren `stable`-Marker (eine stable Funktion darf nicht schreiben) — der Rest des Bodies ist **verbatim** aus P2-D03 übernommen (alle Error-Codes, Messages und Rückgabefelder unverändert), und der Rollback-Test prüft explizit, dass die Down-Path-Version wieder `stable` ist. Nur der Erfolgszweig wird verzeichnet; ein abgelehnter Resolve verzeichnet nichts (per Test). pgTAP `012_p2_d04_domain_events` (**54**: Schema/RLS/Grants/Definer-Shape, 0 Write-Policies, CTR-005-Feldabdeckung, Topic-Parsing fail-closed [malformed UUID, fremder Namespace, null, Nicht-Key-Permission, fehlendes Segment], Link/Unlink/Requirement-Envelopes inkl. unveränderter Elternversion, Append-only-Enforcement, Access-Recording + `audit_events` bleibt unberührt + Refusal verzeichnet nichts, **Permission-Scoping** [Halter von `document.read` sieht, Workspace-Mitglied ohne `document.read` sieht **0**], Forge-Denial, anon-Denial, Envelope-Constraints [Nicht-Key-Event-Type, Nicht-Key-Permission, Übergröße], Fremd-Domänen-Nutzung, Zwei-Workspace-Isolation). Private-Funktions-Inventar in `002_p1_003_rls` um die 6 neuen Helfer erweitert. Rollback `013_p2_d04_domain_events_down` (7) + `flutter.yml`-Replay-Prepend. **Vom `verify_p1_011_e2e`-Gate gefundener echter Regressionsfehler (gefixt):** die neue `realtime.messages`-Policy rief zwei `private.`-Topic-Parser auf, die für `authenticated` per `revoke all` gesperrt waren. Eine Policy, deren Ausdruck die auswertende Rolle nicht ausführen darf, **wirft** — und eine werfende Policy auf `realtime.messages` bricht die Realtime-Autorisierung **für jede** Subscription, nicht nur für die eigene; P1-011 (Property-Realtime, berührt P2-D04 fachlich gar nicht) fiel reproduzierbar aus. Die pgTAP-Suite hatte das nicht sehen können, weil sie die Helfer als Superuser prüfte. Fix: `grant execute` auf **genau** die beiden reinen String-Parser (lesen keine Daten) an `authenticated`; die schreibenden Helfer (`publish_domain_event`, beide Trigger-Funktionen, `reject_domain_event_change`) bleiben ungranted. Neue Assertions pinnen beides getrennt — inkl. zweier `lives_ok`-Prüfungen **als Rolle `authenticated`**, der einzigen Form, die diesen Fehler fängt. **Zwei weitere Test-Befunde geklärt statt weggedrückt:** (a) `now()` ist transaktionsgebunden — alle in einer Transaktion erzeugten Envelopes teilen `occurred_at`, das daher **nicht** ordnen kann; die Assertion zählt jetzt statt zu sortieren, und Clients müssen für Reihenfolge `aggregate_version` nutzen (im Test kommentiert). (b) `anon` hat gar kein SELECT-Grant, „sieht 0 Zeilen" war die schwächere Behauptung — geprüft wird jetzt die harte `42501`-Abweisung. **Verifiziert (DB-Teil des Gates lokal):** `db reset`/`lint` sauber, Security- und Performance-Advisors **0 ERROR/WARN** (nur Baseline-INFO), pgTAP **568/568** (Baseline 514; +54), Rollback-Replay `013→012`+`up` grün, danach erneut 568/568 am Head; voller `tool/verify_*`/`test_*`-Sweep grün. **Increments 2–4 offen:** (2) `tasks` + `task_generated_instances` (STM-012, `generated_key`-Idempotenz AGG-019) + `notifications` (Fan-out); (3) `import_jobs` (STM-013, Dry-Run/Reconciliation AGG-020) + `search_index`; (4) Dart-Contract/Adapter/Realtime-Consumer + realer Integrationstest + CI-Step. **Vorab benannter Mapping-Befund für Increment 2:** die Legacy-`tasks.status` führt `todo`/`in_progress`/`done` als Freitext, STM-012 schreibt `open -> in_progress -> blocked -> done -> archived` — die Umbenennung `todo`→`open` gehört in den Task-Dry-Run-Mapper und ist hier vorab festgehalten, damit sie nicht spät auffällt. **DEBT-009** (periodische Task-Generierung vom UI-Shell-Timer auf einen serverseitigen Job) hängt an Increment 2/3. |

Status-Werte: `todo`, `in_progress`, `blocked`, `done`.

## Offene Punkte, die eine Nutzerentscheidung brauchen

- OPEN-003 (SearchScreen/RentalOverviewScreen: integrieren oder entfernen) — vor W0-Wrapper-Bereinigung.
- Alle `DEC-014`..`017`, `OPN-DOM-*` unverändert offen (siehe `phase_0/11_decision_register.md`).
- **OPN-DOM-005** (Aufbewahrungs-/Löschfristen je Dokument-/Personenart) bleibt **offen**. P2-D03 hat den im Register dokumentierten Default vom Nutzer bestätigen lassen und exakt diesen umgesetzt (keine automatische Löschung, `retention_until` informativ, kein Löschpfad, keine DELETE-Policy auf `storage.objects`, Sperrung via `archived` + Audit) — die eigentliche Fristenfrage ist damit *nicht* entschieden, nur unschädlich vorbelegt.

## Nächster Schritt

| W3 — `OPN-DOM-001` entschieden (Wave-3-Voraussetzung) | Entscheidung | `done` | **2026-07-29, Nutzerentscheidung — dokumentierter Default ausdrücklich überstimmt.** P2-D05 löst laut Backlog `OPN-DOM-001` „mit dem dokumentierten Default, sofern nicht überstimmt" — das ist ein Hard-Stop, also wurde vor der ersten Migrationszeile gefragt statt still entschieden. **Ergebnis: eine Einheit darf mehrere gleichzeitig wirksame Verträge haben** (Teilflächen-Vermietung); der Default in `02_domain_map.md` sagte das Gegenteil. Vollständig ausgeschrieben inkl. bindender Folgen in `docs/architecture/phase_0/02_domain_map.md` (neuer Abschnitt „Entschieden: OPN-DOM-001"), damit die nächste Sitzung nicht den alten Default liest: **kein** (auch kein partieller) Unique-Constraint auf „aktiver Vertrag pro Einheit"; **`AGG-004` ist umzuformulieren** (`occupied` = *mindestens ein* wirksamer Vertrag, `vacant` = keiner — die alte Formulierung setzte Einzahl voraus und war auf den überstimmten Default gebaut); **Rent Roll aggregiert je Einheit** über alle zum Stichtag wirksamen Verträge, jede Einheitskennzahl ist eine Summe; der Belegungs-Invariantentest muss beide Richtungen abdecken. `AGG-007` bleibt unberührt. |
| P2-D03 — `DocumentUploadPort` (Folge-Increment) | Backend-Naht | `done` | **2026-07-29, auf Nutzerentscheidung („Upload-Port zuerst, damit der Pfad vollständig ist").** Der in AP3 geflaggte Befund ist damit behoben: der Contract konnte Inhalte nur **deklarieren** (`DocumentContentDraft` beschreibt ein Objekt, das *schon* im privaten Bucket liegt), es gab aber keinen Weg, Bytes dorthin zu bringen — der Golden-Path „Dokument anlegen" war in der Cloud nicht durchspielbar. **Serverseitig war nichts zu tun** (privater Bucket, Insert-Policy auf `document.manage`, absichtlich **keine** UPDATE/DELETE-Policy, Pfadprüfung `workspace_id/%` + ≥3 Segmente — alles seit P2-D03 vorhanden); es fehlte ausschließlich die Client-Naht. Neu im Contract: `DocumentUploadPort` (mit `bucket`, `maxByteSize`, `contentHashOf` und `storageObjectPath` als **eine** Heimat der Pfad- und Hash-Regel, analog `SignedUrlPort.clampTtl`) und `DocumentFileSelection` als bewusst eigener Typ — die gewählte Datei ist *nicht* dasselbe wie eine Storage-Deklaration, und genau diese Verwechslung machte vorher ein Anlege-Formular möglich, das niemals ein echtes Dokument erzeugen konnte. `uploadObject` im Supabase-Gateway (nie `upsert`: ohne UPDATE-Policy wäre ein stilles Überschreiben ohnehin ein Bruch der Versions-Immutabilität), Legacy-Adapter blockt wie jede Mutation. Beide Controller laden jetzt **erst** hoch und registrieren **dann** — schlägt der Upload fehl, wird kein Aggregat angelegt, dessen Inhalt nie ankam. UI: die fünf Handeingabe-Felder (Pfad, SHA-256, Bytes, MIME, Dateiname) sind durch einen echten `file_selector`-Dateiwähler ersetzt, Primäraktion bleibt deaktiviert bis eine Datei gewählt ist, Größen-/Leer-Prüfung vor dem Upload. **Keine neuen Pakete** (`crypto` und `file_selector` waren schon Abhängigkeiten). Verifiziert: `flutter analyze` sauber; **5 neue Supabase-Adaptertests** (Pfadkonvention, Hash = Hash der wirklich gesendeten Bytes, Limit- und Leer-Ablehnung *ohne* Netzaufruf, kein Leck des Storage-Fehlertexts, Traversal bleibt im Workspace-Prefix) + **3 neue Screen-Tests** (Bytes vor Registrierung, fehlgeschlagener Upload ⇒ `createCalls == 0`, read-only-Backend liest gar keine Bytes); **der echte Local-Client-Integrationstest lädt jetzt über den Port statt über das rohe SDK** und übergibt dessen Deklaration unverändert an `create_document` — d. h. Upload → Confirm → Verify ist end-to-end gegen echtes Storage/PostgREST bewiesen (`./tool/verify_p2_d03_integration.ps1` grün). Volle Suite **928 grün / 15 Skips**, `flutter build web` grün. |
| W2 — Objektbild-Galerie entfernt | Bereinigung | `done` | **2026-07-29, auf ausdrückliche Nutzerentscheidung („drop the feature") nach Vorlage beider Optionen.** `lib/ui/screens/property_detail/widgets/property_images_section.dart` gelöscht und aus dem Tab-Host entfernt; damit liest **kein** W2-Screen mehr ein Legacy-Dokument-Repo, und die Wellen-Abschlusszeile ist erfüllt. **Folge, benannt statt später entdeckt:** bereits gespeicherte Bilder werden auf Objektkarte und Übersichtskopf weiter über `propertyTitleImageProvider` angezeigt, aber **neue** lassen sich nicht mehr hinzufügen, bis Objekt-Medien als eigenes Thema migriert sind — der Dokument-Contract hat kein `image_role`-Vokabular. `propertyTitleImageProvider` und seine zwei Leser (`property_card.dart:292`, `overview_screen.dart:228`) sind bewusst **nicht** angefasst: das sind Welle-1-Screens, und ihre Anzeige zu entfernen war nicht Teil der Entscheidung. |
| W2-AP2b — ComplianceDashboardScreen (SCR-052) | Screen-Redesign | `done` (manueller Golden-Path gebündelt offen) | **2026-07-29.** Sechs-Punkte-Plan im Chat gezeigt. Der Screen-Teil von Arbeitspaket 2, auf dem im selben Tag fertiggestellten Backend-Increment (siehe W2-AP2-Zeile). Neu: `lib/features/documents_compliance/application/compliance_dashboard_controller.dart`; `lib/ui/screens/docs/compliance_dashboard_screen.dart` von 119 LOC undesigntem Legacy-Code (Vollflächen-`CircularProgressIndicator`, `Text('Error: $_error')`, `Text('No … found.')`, Material-`Card`/`ListTile`, **N+1-Schleife `checkComplianceForEntity` je Property**) auf System-Standard + **eine** Server-Abfrage umgebaut. Der Screen leitet **nichts** ab: die KPI-Kacheln zählen nur die vom Server klassifizierten Zeilen (Zählen ≠ zweite Ableitung — `DUP-011` verbietet das Nachrechnen, nicht das Zählen). Wiederverwendet aus AP3: `DocumentRequirementTable` (jetzt erstmals mit der Objekt-Spalte, für die sie gebaut war), `DocumentNotice`, Badges, Formatierung — der dritte Konsument der geteilten Bausteine, wie in `04b` vorgesehen. **Objekt-IDs und -Namen kommen aus dem DOM-002-Port** (`referencePropertyRepositoryProvider`, IDs/DTOs über die Modulgrenze — von Vertragsregel 3 erlaubt) und reisen in **einem** Aufruf mit; ein Test belegt `calls.length == 1` mit beiden IDs. **Befund und ehrliche Auflösung:** dieser Port ist nur im Cloud-Modus gebunden (lokal `StateError`, fail closed). Statt zu crashen oder die Lücke zu verschweigen, kapselt `complianceObjectDirectoryProvider` das als `PropertyRepository?`, und der Screen zeigt bei fehlendem Verzeichnis den Pflicht-Hinweis „Eingeschränkte Abdeckung" — ausgewertet werden dann nur Objekte mit bereits vorhandener Regel oder vorhandenem Dokument. Zweiter Abdeckungs-Hinweis „Nicht vollständig ausgewertet" für `scoped_rule_count > 0`, dritter „Teilweise ausgewertet", wenn das Objektverzeichnis die Paging-Grenze (10 × 100) reißt: eine Compliance-Sicht, die still Zeilen auslässt, ist schlimmer als eine, die sagt, was sie nicht geprüft hat. **Vier statt drei KPI-Kacheln** (Offen / Läuft ab / In Prüfung / Erfüllt, plus „davon N pflichtig" auf der Offen-Kachel) — bewusste Abweichung von `04b`s drei Kacheln, weil drei die `pending_content`/`pending_verification`-Zeilen aus der Summe fallen lassen würden, obwohl die Tabelle sie zeigt. `versionConflict`/read-only-bis-migriert sind hier **nicht** anwendbar (rein lesende Projektion, keine Mutation) — das steht im Screen-Doc, statt leere Zustände zu erfinden. **Der von AP4 bewusst deaktivierte „Fix"-Absprung ist wieder verdrahtet**, jetzt mit ehrlichem Ziel: als `onOpenRequirement`-Callback, damit der Screen kein Shell-Wissen trägt — der lokale Host navigiert in-Shell (`selectedPropertyIdProvider`/`propertyDetailPageProvider`/`globalPageProvider`), die additive Cloud-Route `complianceRoute = '/compliance'` schiebt `/property-documents/<id>`. Verifiziert: `flutter analyze --no-pub` sauber; neu `test/ui/screens/docs/compliance_dashboard_states_test.dart` **22 Tests grün** — loading (Skeleton statt Vollflächen-Spinner), **positiver Leerzustand „Alles erfüllt" mit Prüfzeitpunkt**, getrennt davon „Keine Anforderungen hinterlegt" (leeres Regelwerk ist nicht dasselbe wie Erfüllung), error (Retry, kein Roh-Exception-Text), **forbidden** getrennt von empty/error, Gruppierung nach Objekt**namen** statt roher ID, **ein** Aufruf mit allen IDs, KPI-Zählung inkl. pflichtig-vs-optional, alle drei Abdeckungs-Hinweise, Absprung liefert die Entitäts-ID, „nur offene" wird serverseitig neu abgefragt, plus **9 Responsive-Tests** (390×844 / 1024×768 / 1440×900 × comfort/compact/adaptive). Volle Suite **887 grün / 15 Skips**, `flutter build web --no-pub` grün. **Offen:** manueller Golden-Path gebündelt vor Wellenabschluss. |
| W2-AP4 — DocumentsScreen global (SCR-051) | Screen-Redesign | `done` (manueller Golden-Path gebündelt offen) | **2026-07-29, per Subagent umgesetzt, danach vom Haupt-Thread verifiziert.** Neu: `lib/features/documents_compliance/application/documents_workspace_controller.dart` (workspace-scoped, Muster von `PropertyDocumentsController`: Phasen je Zone, Generation-Guard, `canMutate`/`canVerify` getrennt, Keyset-Paging, Realtime, `contentRejected`, kein Löschpfad), `lib/ui/screens/docs/documents_workspace_panel.dart` (der eigentliche Umbau, nur Feature-Contract, komponiert aus AP3s `docs/widgets/`), `lib/ui/screens/docs/legacy_document_rules_tabs.dart` (die Legacy-Registries unverändert herausgezogen). `documents_screen.dart` 1196 → 123 LOC (**BIG-022** aufgelöst); `DUP-007` damit vollständig eingelöst — beide Dokument-Screens teilen Tabelle, Dialoge, Detailpanel, Badges, Formatierung. Additiv erweitert (AP3-Aufrufer bleibt unberührt): `DocumentDetailPanel.links` (nullbar) und `documentEntityTypeLabel()`. **Dritter Tab-Host-Befund, gleiche Form wie AP3:** SCR-051 ist ein **Vier**-Tab-Host — `navigation_actions.dart:104` schickt die Command-Palette-Aktion `jump_missing_documents` über `documentsRequestedTabProvider` auf Tab 3, Tabs 1/2 sind die einzige UI der lokalen Typ-/Pflichtregel-Registries, und Tab 3 ist die einzige Einbettung des ComplianceDashboards (SCR-052). Ein Komplettersatz hätte drei funktionierende V1-Flächen gelöscht → Host bleibt, Tab 0 ist der Umbau, Cloud-Route (`documentsWorkspaceRoute = '/documents'`) mountet **den Panel**. **Zwei weitere Contract-vs-Plan-Befunde:** (1) der von `04b` beschriebene Entity-Filter ist auf Workspace-Ebene nicht möglich — `DocumentListQuery` erzwingt `(entityType == null) == (entityId == null)`, und beide Adapter setzen den `document_links`-Embed nur bei vorhandener **ID**; umgesetzt stattdessen ein kontrollierter Ebenen-Filter über `DocumentTypeDto.entityType` (als „Ebene / Aus dem Dokumenttyp" beschriftet, damit er nicht vorgibt, die Verknüpfung zu sein) plus der echt serverseitige `documentTypeId`-Filter; die tatsächlichen Links stehen je gewähltem Dokument (ein `listLinks`-Read, kein N+1). (2) Anlegen aus dem globalen Screen kann **nicht** verknüpfen (kein ehrlicher Eigentümer ohne Entitäts-Picker) — der Controller sagt das in der Erfolgsmeldung, ein Test belegt `linkCalls == 0`. **Eine Löschung, offengelegt statt still:** `test/ui/docs/documents_screen_test.dart` (1 Test) entfernt — er trieb die Validierung des Legacy-Dialogs „Dokument erfassen", also genau das lokale Dokument-Editieren, das die bestätigte Entscheidung vom 2026-07-24 zurückzieht; der Codepfad existiert nirgends mehr, der Test war nicht umhängbar. Ersatzabdeckung sind die contract-basierten Anlege-Tests. Ebenfalls entfallen: der Batch-Auswahl-Stub (schrieb nur einen Status-String) und „Kontext öffnen" (hing an Entity-Feldern, die `DocumentDto` nicht führt); `ComplianceDashboardScreen.onFixIssue` ist jetzt `null` (deaktiviert), weil es den entfernten Legacy-Dialog öffnete — der echte Fix-Pfad kommt mit SCR-052. Verifiziert (vom Haupt-Thread nachgefahren, nicht nur berichtet): `flutter analyze --no-pub` sauber; `test/ui/screens/docs/documents_screen_states_test.dart` **31 Tests grün** — inkl. **gefilterter Leerzustand als eigener Zustand** (Client- und Backend-Variante), `forbidden`, `versionConflict`, read-only-bis-migriert (`createCalls == 0`), `document.manage`/`document.verify` getrennt, `contentRejected`, **abgebrochene Archivierungs-Bestätigung (`transitionCalls == 0` vor und nach „Abbrechen")**, kein Löschpfad, Host behält vier Tabs, plus 9 Responsive-Tests. Volle Suite **847 grün / 15 Skips**. **Offen:** manueller Golden-Path gebündelt vor Wellenabschluss. |
| P2-D05 — `leasing_operations` Increment 1 (units + leases + Realtime) | Backend (Domain-Expansion) | `done` (Increment 1 von 3; Increments 2–3 offen) | **2026-07-30. Increment 1 wurde von der Vorsitzung geschrieben (Dateizeitstempel 12:00–12:10), aber weder verifiziert noch hier eingetragen — die Sitzung endete davor. Diese Sitzung hat den vollen `database`-Gate selbst nachgefahren, statt die Arbeit als grün zu übernehmen.** Neu: `supabase/migrations/20260730100000_p2_d05_leasing_operations.sql` (Enums `unit_status`/`lease_status`, Tabellen `units` + `leases`, 6 RPCs `create_unit`/`update_unit`/`transition_unit_status`/`create_lease`/`update_lease`/`transition_lease_status`, geteiltes Command-Trio `private.leasing_command_gate`/`claim_leasing_mutation`/`finish_leasing_mutation` statt Per-RPC-Boilerplate), `20260730110000_p2_d05_leasing_realtime.sql` (**beide** Aggregate publiziert — die von `sync_unit_occupancy` abgeleitete Belegungs-Umschaltung ändert eine `units`-Zeile, von der kein `leases`-Abonnent erfährt; die daraus folgende doppelte Invalidierung je Lease-Aktivierung ist im Kopf der Migration offengelegt statt entdeckt zu werden), `supabase/tests/017_p2_d05_leasing_operations.test.sql`, `supabase/tests_rollback/019_p2_d05_down.test.sql` + `020_p2_d05_realtime_down.test.sql`, plus Rollback-Replay-Wiring im `database`-Job (`.github/workflows/flutter.yml`). Mutations-Oberfläche spiegelt P2-D03: `{ok,entity}`/`{ok,error:{code}}`-Envelope, `p_expected_version`, Idempotenz über `mutation_receipts` + Request-Hash, append-only `audit_events`, Default-Deny-RLS, `reject_protected_column_update`, Claim-vor-State-Validierung mit Receipt-Cleanup. **Kein AAL2-Gate** (Einheiten/Verträge sind Workspace-Geschäftsdaten wie Properties/Parteien/Dokumente) — Zugriff über `lease.read`/`lease.manage`. **`OPN-DOM-001` strukturell umgesetzt, nicht nur notiert:** kein Unique-Constraint (auch kein partieller) auf „aktiver Vertrag pro Einheit", und der Gate-Test sichert die *Abwesenheit* ab (prüft `pg_index.indisunique` über alle `leases`-Indizes, schließt Unique-/Exclusion-**Constraints** aus und pinnt, dass der Belegungs-Partialindex `leases_unit_effective_idx` non-unique bleibt) — ein späteres Wiedereinführen lässt diesen Test fehlschlagen, statt die Entscheidung still zurückzudrehen. **`AGG-004` in umformulierter Fassung als Trigger-Invariante, beide Richtungen getestet:** `vacant` mit wirksamem Vertrag wird abgewiesen, `occupied` ohne wirksamen Vertrag ebenso; zusätzlich belegt: ein zweiter Vertrag auf einer belegten Einheit ist gültig, das Beenden *eines* von zwei wirksamen Verträgen lässt die Einheit belegt, erst das Beenden des letzten macht sie frei. Belegung ist **abgeleitet**, nicht setzbar (direktes `occupied` → `validation_failed`, und die Ablehnung nennt den aus den Verträgen folgenden Status). **„Wirksam" = Status `active`, bewusst nicht datumsbasiert** — die Invariante wird per Trigger erzwungen, Trigger feuern nur bei Writes; ein Datumsprädikat würde eine gespeicherte `occupied`-Einheit um Mitternacht still ungültig machen und den Fehler beim nächsten unbeteiligten Write auslösen. Datumsgetriebenes Auslaufen ist ein expliziter `transition_lease_status`-Aufruf (Konvention wie Dokument-Gültigkeiten in P2-D03). **`STM-003` `occupied → offline` gelesen als „die Lage ist zu prüfen", nicht „die Einheit muss vertragsfrei sein"** (die wörtliche Lesart wäre eine tote Kante, da `occupied` per AGG-004 immer einen wirksamen Vertrag hat): `offline` ist von der Belegungs-Invariante ausgenommen, der Übergang verlangt einen Grund und schreibt die Zahl der zu diesem Zeitpunkt wirksamen Verträge ins Audit — die Prüfung ist belegt statt behauptet; das Verlassen von `offline` rechnet den Status aus den dann wirksamen Verträgen neu. **Kein `tenants`-Cloud-Pendant, per Design** (`AGG-005` + P2-D02): `party_role_type` führt `tenant`, `leases.tenant_party_id` referenziert `public.parties`, das Mieterverzeichnis *ist* das Parteienverzeichnis — die Legacy-SQLite-`tenants`-Tabelle wird vom Dry-Run-Mapper in `parties` gefaltet, nicht übernommen. **Drei benannte Lücken (ausgeschrieben, nicht improvisiert):** (1) `units.property_id` referenziert nur `properties (id)` — eine Composite-FK auf `(workspace_id, id)` wäre stärker, aber `properties` trägt keinen solchen Unique-Constraint und ihn zu ergänzen wäre eine Strukturänderung an einer bestehenden Tabelle; die RPCs prüfen die Workspace-Zugehörigkeit des Objekts deshalb explizit (gleiche Lücke, gleiche Auflösung wie P2-D07). Alle Tabellen, die diese Migration selbst besitzt, tragen `(workspace_id, id)`, die Kante `units → leases` ist also eine echte Composite-FK. (2) Ein Vertrag trägt höchstens **einen** Mieter — Co-Tenancy (mehrere gemeinsam haftende Parteien) kann das Modell nicht ausdrücken, steht nicht im P2-D05-Deliverable und ist bewusst nicht halb gebaut; das ist **unabhängig** von `OPN-DOM-001` (dort geht es um mehrere *Verträge* je Einheit, was unterstützt wird). (3) `units.vacancy_since` wird bei Übergängen nach `vacant` automatisch gepflegt, aber **nicht** als non-null erzwungen, obwohl `operations_data_quality_engine.dart` genau diese Kombination meldet — ein harter Constraint würde Legacy-Zeilen ohne Leerstandsdatum unimportierbar machen, ohne ein Datum zu erfinden; Cloud-Daten sind vollständig, importierte bleiben ehrlich und werden weiter von der Quality-Engine gemeldet. **`AGG-006` im selben Increment umformuliert** (`02_domain_map.md`, Nachtrag 2026-07-30): sein Schlusssatz „konkurrierende aktive Vertraege pro Einheit sind unzulaessig" war derselbe überstimmte Default wie in `AGG-004`, nur auf das Lease-Aggregat bezogen — als Konsequenz der bereits gefallenen Entscheidung behandelt, **nicht** als zweite offene Frage. **Verifiziert (in dieser Sitzung selbst gefahren):** `npx supabase db reset --local --no-seed` — alle 20 Migrationen anwendbar; `db lint --level error` → `{"results":[]}`; Security-/Performance-Advisors **0 ERROR** (nur die vorbestehenden INFO-Befunde `rls_enabled_no_policy` auf `mutation_receipts` und `unused_index` auf frisch zurückgesetzter DB); `npx supabase test db --local` **914/914 grün** über 17 Dateien (**+108 gegenüber der 806er-Baseline**); **voller Rollback-Replay der CI-Kette `020 → 019 → 018 → … → 003`, alle 18 Schritte PASS**, danach `migration up` + erneut **914/914** — die neuen Down-Pfade hinterlassen also keinen Rest, der die tieferen Rollbacks bricht; `flutter analyze --no-pub` sauber; volle Flutter-Suite **945 grün / 15 Skips** (Baseline durch die parallele P2-D07-Sitzung von 928 auf 945 gestiegen, Increment 1 berührt keinen Dart-Code). **Offen und bewusst nicht in diesem Increment:** `lease_rent_schedule` (periodische Mietanpassung, der Indexierungs-Input), `leasing_cases` (`STM-004`) und `rent_roll_snapshots` (`AGG-007`) → **Increment 2**; Dart-Contract `lib/features/leasing_operations/{domain,application,data}` + beide Adapter + Adaptertests + realer lokaler Client-Integrationstest + Realtime-Consumer + Dry-Run-Mapper + CI-Integrationsskript → **Increment 3**. Von den drei Gate-Punkten des Backlogs ist damit **`AGG-004` (beide Richtungen) erfüllt**; `AGG-007`-Snapshot-Unveränderlichkeit und der **Zwei-Sitzungs-Concurrency-Test** stehen noch aus. |
| P2-D05 — `leasing_operations` Increment 2 (LeasingCase STM-004 + Rent Roll AGG-007) | Backend (Domain-Expansion) | `done` (Increment 2 von 3; Increment 3 offen) | **2026-07-30.** Funktions-Plan vor der ersten Zeile im Chat gezeigt. Neu: `supabase/migrations/20260730120000_p2_d05_leasing_cases_rent_roll.sql` (Enum `leasing_case_status`, Tabellen `leasing_cases` + `rent_roll_snapshots` + `rent_roll_snapshot_lines`, 4 RPCs `create_leasing_case`/`update_leasing_case`/`transition_leasing_case_status`/`create_rent_roll_snapshot`), `20260730130000_..._realtime.sql`, `supabase/tests/018_p2_d05_leasing_cases_rent_roll.test.sql` (**116 Tests**), `supabase/tests_rollback/021_*_down.test.sql` + `022_*_realtime_down.test.sql`, Rollback-Replay-Wiring im `database`-Job. **Kein zweites Command-Trio:** der Increment nutzt `private.leasing_command_gate`/`claim_leasing_mutation`/`finish_leasing_mutation` aus Increment 1 unverändert weiter (ein pgTAP-Test belegt deren Existenz, ein Rollback-Test belegt, dass Increment 2s Down-Pfad sie **nicht** mitnimmt — sonst wären Units/Leases still kaputt). Envelope, `p_expected_version`, `mutation_receipts`-Idempotenz mit Request-Hash, Claim-vor-State-Validierung mit Receipt-Cleanup, append-only Audit, Default-Deny-RLS, `lease.read`/`lease.manage`, kein AAL2-Gate — alles wie P2-D03/Increment 1. **`STM-004` als echtes Aggregat** (löst den UI-only-Status-String-Teil von `FTR-024`): die 10-stufige Kette streng **einen** Schritt vorwärts, `cancelled` aus jedem nichtterminalen Zustand. **Bewusst keine Rückwärtskanten**, weil `STM-004` keine nennt — eine gescheiterte Bonitätsprüfung ist ein Abbruch mit Grund, der nächste Versuch ein neuer Fall; das ist zugleich das ehrlichere Datenmodell, weil ein wiedereröffneter Fall die Historie des ersten Scheiterns still überschriebe. Die Vorstufe geht nicht verloren: sie steht in den `old_values` der Audit-Zeile. **Drei Fortschrittsbedingungen doppelt abgesichert** — im RPC (typisierter, feldadressierter Fehler) *und* als CHECK-Constraint (Zustand auch bei direktem SQL unerreichbar): Einheit ab `offer`, Interessent ab `screening`, Vertrag ab `signed`. `cancelled` ist von allen dreien ausgenommen, weil ein Abbruch die erreichte Stufe vergisst — die Statuslisten sind in den CHECKs **literal aufgezählt** statt über eine Rank-Funktion, damit sie dump-/restore-reihenfolgesicher bleiben. **Der Interessent braucht bewusst KEINE `tenant`-Parteirolle** (anders als `leases.tenant_party_id`): `party_role_type` kennt kein `prospect`, und eine Anfrage als Mieter zu stempeln behauptete eine Beziehung, die es noch nicht gibt — die Rolle wird verbindlich, wenn ein Vertrag die Partei nennt, was `create_lease` seit Increment 1 erzwingt. Ein Test belegt genau das. **`AGG-007` strukturell statt konventionell:** Before-Update- **und** Before-Delete-Trigger auf Kopf *und* Zeilen (Muster `domain_events`/P2-D04), keine UPDATE-/DELETE-Policy; vier Laufzeit-Tests versuchen alle vier Schreibwege **als `postgres`** und bekommen `P0001`. Ein unveränderliches Aggregat trägt deshalb **kein** `version`/`updated_at`/`updated_by` — drei `hasnt_column`-Tests pinnen das: ein Optimistic-Concurrency-Token bewürbe einen zweiten Schreiber, den es nicht gibt. **Reproduzierbarkeit als Struktur, nicht als Zusicherung:** Check-Constraints erzwingen `unit_count = occupied + vacant + offline` und `total = base + ancillary + parking`; der Test rechnet zusätzlich **jede** Kopfsumme aus den eingefrorenen Zeilen nach. **Snapshots sind bewusst NICHT unique je (Objekt, Stichtag)** — `AGG-007` fordert Unveränderlichkeit, nicht Einmaligkeit, und da `OPN-DOM-005` offen ist und es **nirgends** einen Löschpfad gibt, würde unique+unveränderlich+unlöschbar bedeuten, dass ein Fehllauf die Periode dauerhaft vergiftet; die Abwesenheit des Unique-Index wird wie bei `OPN-DOM-001` **strukturell getestet**, ein zweiter Snapshot derselben Periode ist explizit erlaubt und getestet. **`OPN-DOM-001` im Rent Roll umgesetzt:** genau **eine** Zeile je Einheit, jede Geldkennzahl eine **Summe** über alle am Stichtag wirksamen Verträge, `effective_lease_count` reist mit (eine Zeile „2 Verträge, 1050" ist prüfbar, ein nacktes „1050" nicht). `unique (snapshot_id, unit_id)` steht dazu nicht im Widerspruch — die Entscheidung verbietet, *Verträge* je Einheit zu beschränken, das hier beschränkt *Zeilen* je Einheit und macht die Kennzahl überhaupt erst zur Summe. Tests: zwei wirksame Verträge auf einer Einheit → eine Zeile, `count = 2`, Basis 1050 = 950+100, Gesamt 1280. **Eine bewusste Asymmetrie zu Increment 1, ausgeschrieben statt entdeckt:** die AGG-004-Invariante ist status-basiert (Trigger feuern nur bei Writes), der Rent Roll wendet **zusätzlich** ein Datumsfenster an (`start_date <= as_of <= end_date`), weil er ein einmalig berechneter Stichtagsbericht ist und ein im Juli beginnender Vertrag in einem März-Rent-Roll schlicht falsch wäre. Sichtbare Folge, in **beide** Richtungen getestet: eine Einheit kann `occupied` sein (AGG-004) und trotzdem 0,00 beitragen — der je Zeile eingefrorene `unit_status` macht das lesbar statt nach Rechenfehler aussehend; `generated_at` steht neben `as_of_date`, weil Status zur Erzeugungszeit gelesen werden. **`DEC-011` ohne stille Fremdwährungssumme:** die Währung wird aus den beitragenden Verträgen abgeleitet; mehrere → eigener Fehlercode `currency_mismatch` **mit der Liste der gefundenen Währungen** (getestet mit einem CHF-Vertrag neben EUR); trägt nichts bei (vollständig leerstehendes Objekt, das trotzdem einen Nullen-Rent-Roll verdient), ist sie nicht ableitbar und **muss** übergeben werden — Raten hätte Daten erfunden. Die Währungsprüfung läuft **vor** dem Claim, damit ein Datenfehler nicht die Mutation-ID verbrennt. **Eingefrorene Denormalisierung getestet:** Einheit nach dem Snapshot umbenannt → die Zeile trägt weiter den alten `unit_code`. **Audit-`new_values` ist das ganze Dokument inkl. Zeilen**, damit ein idempotenter Replay exakt dasselbe zurückgibt statt eines Kopf-Stummels (getestet); der Größen-Trade-off steht im Migrationskopf. **Realtime:** `leasing_cases` + `rent_roll_snapshots` publiziert, `rent_roll_snapshot_lines` **bewusst nicht** — das ist genau die Kind-Tabellen-Entscheidung, die Increment 1s Realtime-Migration ausdrücklich vertagt hatte: Zeilen entstehen nur mit ihrem Kopf und ändern sich nie, also deckt das Kopf-Event alles ab, während ein Publizieren bei einem 200-Einheiten-Objekt 201 Invalidierungen für **einen** logischen Befehl erzeugte; die `REPLICA IDENTITY FULL`-Frage aus P2-D03 stellt sich nicht, weil hier nie gelöscht wird. Beides getestet (2 publiziert, 0 für Zeilen). **Verifiziert:** `db reset` — alle 22 Migrationen anwendbar; `db lint --level error` → `{"results":[]}`; Security-Advisors **0 ERROR/WARN** und **kein** `rls_enabled_no_policy` für die drei neuen Tabellen, Performance-Advisors **0 ERROR/WARN**; `npx supabase test db --local` **1030/1030 grün** über 18 Dateien (**+116**); **voller Rollback-Replay der CI-Kette `022 → 021 → 020 → … → 003`, alle 20 Schritte PASS**, danach `migration up` + erneut **1030/1030**; **alle neun DB-berührenden `tool/verify_*`-Skripte des `database`-Jobs grün** (`p1_004_concurrency`, `p1_007_integration`, `p1_011_e2e`, `p1_018_postgrest`, `debt_012_tombstone`, `p2_d01`–`p2_d04_integration`) — jedes startet mit eigenem frischem Reset, belegt also zusätzlich, dass die vier neuen Migrationen den PostgREST-Schema-Cache und die bestehenden Realtime-/Concurrency-Pfade nicht stören. **Eine geteilte Datei angefasst:** `supabase/tests/002_p1_003_rls.test.sql` — die `functions_are`-Inventur des `private`-Schemas musste um die 9 neuen Helfer erweitert werden (sie schlug sonst fehl; das ist der Sinn der Inventur). **Kein Dart berührt**, Flutter-Baseline dieser Sitzung unverändert `analyze` sauber / **945 grün / 15 Skips**. **Bewusst NICHT gebaut: `lease_rent_schedule`.** Increment 1s Kopf hatte es für „das nächste Increment" angekündigt, es steht aber **nicht** im P2-D05-Deliverable des Backlogs, und der Rent Roll braucht es nicht (ein Snapshot friert die Miete zur Erzeugungszeit ein, künftige Staffeln ändern daran nichts). Als Abweichung offengelegt statt still gedroppt oder still gebaut. **Korrektur (2026-07-30, selber Tag, sachlicher Fehler):** die ursprüngliche Begründung behauptete zusätzlich, die Indexierungssemantik sei unentschieden, und berief sich auf `OPN-DOM-003`. Das war doppelt falsch und ist im Migrationskopf wie hier zurückgezogen: `OPN-DOM-003` betrifft **Mahnstufen**, nicht Indexierung, und die Semantik ist lokal längst entschieden **und implementiert** — `lease_indexation_rules` (`kind`, `effective_from_period_key`, `annual_percent`, `fixed_step_amount`, `cap_percent`, `floor_percent`) und `lease_rent_schedule` (`period_key`, `rent_monthly`, `source`) existieren im SQLite-Schema, und `LeaseRepo.rebuildRentSchedule` treibt sie über eine deterministische Engine, bei der manuelle Overrides einen Rebuild überleben. Der Grund, warum dieses Increment sie nicht trägt, ist also **Scope, keine offene Frage**. **Die eine echte Entwurfsfrage vor einer Migration ist eine andere:** der Zeitplan wird heute von einer deterministischen **Dart**-Engine erzeugt; eine Cloud-Fassung muss diese Berechnung entweder nach PL/pgSQL portieren (zweite Implementierung, die von der Dart-Fassung abdriften kann — genau die Gefahr, für die `RISK-QA-001` existiert) oder die Engine behalten und nur ihr Ergebnis speichern. Diese Wahl ist offen und gehört dem, der die beiden Tabellen migriert. **Offen: Increment 3** — Dart-Contract `lib/features/leasing_operations/{domain,application,data}` + beide Adapter + Adaptertests + realer lokaler Client-Integrationstest (`tool/verify_p2_d05_integration.ps1`) + Realtime-Consumer + Dry-Run-Mapper (faltet die Legacy-`tenants`-Tabelle in `parties`) + CI-Step. Von den drei Backlog-Gate-Punkten sind jetzt **`AGG-004` und `AGG-007` erfüllt**; der **Zwei-Sitzungs-Concurrency-Test** gehört zu Increment 3. |
| P2-D05 — `leasing_operations` Increment 3, Teil 1 (Domain + Contract + Supabase-Adapter) | Backend-Naht (Dart) | `partial` (Teil 1 von 2; siehe „Offen") | **2026-07-30.** Der backend-agnostische Vertrag und der Cloud-Adapter der P2-D05-Domäne, gebaut nach dem P2-D02-Muster (`contacts_parties` als nächstliegender Präzedenzfall, bewusst statt des größeren P2-D03). Neu: `lib/features/leasing_operations/domain/{unit,lease,leasing_case,rent_roll}_dto.dart`, `application/leasing_repository.dart` (**7 Ports**: `UnitRepository`/`UnitSearchPort`, `LeaseRepository`/`LeaseSearchPort`, `LeasingCaseRepository`/`LeasingCaseSearchPort`, `RentRollPort`; Command-/Query-Objekte, sealed `LeasingRepositoryResult<T>`, `LeasingVersionConflict`), `data/supabase_leasing_repository_adapter.dart`. **Kein SDK-Import im `application/`-Layer** (Vertragsregel eingehalten). **Eigene Failure-Art `currencyMismatch`** statt Einsortierung unter `validationFailed`: sie trägt die tatsächlich gefundenen Währungen (`RentRollCurrencyMismatch.currencies`) — „diese Verträge sind in CHF und EUR" ist handlungsfähig, „ungültig" nicht; ein Assert bindet Art und Nutzlast aneinander, wie beim `versionConflict`. **Vier Adapterklassen statt einer**, entgegen dem P2-D02-Muster „eine Instanz bedient alle Ports": die vier Aggregate benutzen absichtlich dieselben natürlichen Methodennamen (`getById`/`create`/`update`/`transitionStatus`/`search`), eine Klasse kann sie nicht viermal implementieren. Sie teilen sich eine `_SupabaseLeasingBase` (Gateway, `_executeCommand`, Fehler-Mapping, Paging, Workspace-Guard), also weiterhin **ein** Gateway und ein Client je Workspace. **Explizite Wire-Maps statt `.name`:** mehrere Dart-Labels sind camelCase, wo das Postgres-Enum snake_case ist (`tenantSigned`/`tenant_signed`, `documentsPending`/`documents_pending`, `contractDraft`/`contract_draft`, `walkIn`/`walk_in`) — abgeleitete Wire-Werte hätten still Labels erzeugt, die der Server ablehnt; Tests decken beide Richtungen ab. **`_dateToWire` nimmt Y/M/D ohne UTC-Umrechnung:** jede gesendete Spalte ist eine `date`-Spalte, und ein Mietbeginn „1. März" darf für Nutzer östlich von Greenwich nicht still zum 28. Februar werden. **Zwei Befunde beim Bauen, korrigiert statt umgangen:** (1) `transition_unit_status` hat **keinen** eigenen Offline-Grund-Parameter — der Server schreibt `p_reason` sowohl in `units.offline_reason` als auch ins Audit; das zunächst entworfene Feld `offlineReason` wäre ein Phantom ohne Ziel gewesen und wurde entfernt, der Vertrag dokumentiert jetzt, dass der Command-Reason *der* Offline-Grund ist (zwei Felder könnten über denselben Sachverhalt uneins werden). (2) Die Unit-/Lease-Updates nehmen serverseitig ein sparsames `jsonb` (`p_changes ? 'key'` = ändern, Abwesenheit = unverändert); der Adapter sendet dennoch **alle** Schlüssel (Whole-Record wie `PartyUpdateDto`), weil Optimistic Concurrency ohnehin garantiert, dass der Aufrufer die aktuelle Zeile hält — eine Form ist leichter zu belegen als zwei; im Adapter kommentiert. **DTO-Verhalten, das die Schema-Entscheidungen im Client sichtbar hält:** `LeaseSummaryDto.coversDate` ist bewusst getrennt von `isEffective` (Laufzeitfenster vs. AGG-004-Status), `RentRollSnapshotLineDto.isOccupiedButOutsideTerm` benennt die dokumentierte Asymmetrie, damit eine UI 0,00 bei belegter Einheit erklären statt wie einen Rechenfehler aussehen lassen kann; `RentRollSnapshotDto` trägt **kein** `version`/`updatedAt` (AGG-007), `occupancyRate` gibt bei 0 Einheiten `null` statt irreführender 0 %; `LeasingCaseStatus.canTransitionTo` und `LeasingCaseDto.blockedReason` spiegeln STM-004 nur, um Affordanzen zu deaktivieren — **der Server bleibt die Autorität**, das Mirror entscheidet nichts. Verifiziert: `flutter analyze --no-pub` sauber; neu `test/features/leasing_operations/supabase_leasing_repository_adapter_test.dart` **21 Tests grün** (Fake-Gateway, kein Netz) — RPC-Parameternamen und Datumsformat, numeric-als-String-Parsing, Enum-Mapping in beide Richtungen inkl. der vier mehrwortigen Labels, `version_conflict` landet in der **richtigen** der drei Entitäts-Slots, Actor-Mismatch blockt **ohne** RPC-Versuch, Fremd-Workspace-Zeile erreicht nie den Aufrufer, Keyset-Paging fragt limit+1, `effectiveOnly`, `openOnly`, eingebettete Rent-Roll-Zeilen inkl. OPN-DOM-001-Summe (2 Verträge → eine Zeile, 1280), `currencyMismatch` behält `['CHF','EUR']`, fehlender Snapshot ist `notFound` statt Infrastrukturfehler, STM-004-Mirror inkl. „keine Rückwärtskante" und `blockedReason`. Volle Suite **987 grün / 15 Skips**. **Offen (Teil 2, nicht angefangen):** read-only Legacy-SQLite-Adapter (Vokabular geprüft: `units.status` ist im Legacy exakt `vacant`/`occupied`/`offline`; **Achtung**, `lease_repo.dart` erzwingt lokal noch „höchstens ein aktiver Vertrag je Einheit" — genau die per `OPN-DOM-001` überstimmte Annahme, der Legacy-Adapter darf sie **nicht** übernehmen), Provider + `featureBackendOverrides`-Wiring, Realtime-Consumer, `tool/verify_p2_d05_integration.ps1` **inklusive des noch offenen Zwei-Sitzungs-Concurrency-Tests aus dem Backlog-Gate**, Dry-Run-Mapper (faltet die Legacy-`tenants`-Tabelle nach `parties`), CI-Step. Bis dahin ist die Naht **nicht verdrahtet** — kein Screen und kein Provider liest sie, der Baum bleibt grün. |

**Stand 2026-07-28, Sitzungsende.** AP0 + AP1 `done` und committet (`f7c6341`, `32383a9`), AP3-Fundament committet (`fe44f96`), AP2 `blocked`. **Zwei Blocker, beide extern:** (1) **Docker Desktop läuft nicht** → lokaler Supabase-Stack und damit der `database`-Gate nicht lauffähig; deshalb wurde die AP2-Migration bewusst **nicht** blind geschrieben. (2) **Nutzungslimit** (Reset 17:30 Europe/Berlin) → Subagenten-Delegation für AP3/AP4 scheiterte sofort. **Reihenfolge für die nächste Sitzung:** (a) AP3 zu Ende bringen — Screen-Umbau auf dem bereits committeten Controller + `document_badges.dart`, geteilte Tabelle/Dialoge nach `lib/ui/screens/docs/widgets/`, Widget-/Responsive-Tests, additive Cloud-Route; (b) AP4 DocumentsScreen (SCR-051) auf denselben geteilten Bausteinen, per Subagent sobald das Limit zurückgesetzt ist; (c) AP2, sobald Docker läuft — Design steht bereits fest (siehe W2-AP2-Zeile), nur noch Migration + pgTAP + Rollback + Adapter + Gate. Danach gebündelter manueller Golden-Path aller W2-Screens und Wellen-Check-in.

**Stand 2026-07-29, Sitzungsende.** Beide Blocker der Vorsitzung waren extern und sind weg: Docker lief, das Nutzungslimit war zurückgesetzt. Erledigt in dieser Sitzung: **AP3 `done`** (Commit `824075a`), **AP4 `done`** (Subagent, danach vom Haupt-Thread nachverifiziert), **AP2-Backend `done`** (voller lokaler `database`-Gate inkl. Rollback-Replay und echtem Client-Integrationstest). Stand jetzt: `flutter analyze` sauber, volle Suite **847 grün / 15 Skips**, pgTAP **806 grün** auf frischem Reset, `flutter build web` grün. **Nachtrag gleicher Tag: SCR-052 ist ebenfalls `done`** (siehe W2-AP2b-Zeile) — damit sind **alle vier W2-Screens plus Arbeitspaket 0 umgebaut**. **Verbleibend bis Wellenabschluss, in dieser Reihenfolge:** (1) **gebündelter manueller Golden-Path** aller vier W2-Screens im laufenden Build (Cloud-Modus für Mutationen, über `/parties`, `/documents`, `/property-documents/<id>`, `/compliance`). (2) **Wellen-Check-in beim Nutzer** (Hard-Stop) — Welle 3 erst danach. **Nachtrag 2026-07-29, Sitzungsende (zweiter Teil).** Die drei unten aufgeführten offenen Punkte sind **alle drei entschieden und umgesetzt** — siehe die Zeilen „P2-D03 — `DocumentUploadPort`" und „W2 — Objektbild-Galerie entfernt": (a) Galerie entfernt, damit ist die Wellen-Abschlusszeile „kein W2-Screen nutzt mehr Legacy-Dokument-Repos" erfüllt; (b) `DocumentUploadPort` existiert, der Golden-Path „Dokument anlegen" ist damit in der Cloud durchspielbar und end-to-end getestet; (c) der gelöschte Legacy-Test bleibt gelöscht (bestätigt). Stand jetzt: `flutter analyze` sauber, volle Suite **928 grün / 15 Skips**, pgTAP **806 grün**, `flutter build web` grün, `verify_p2_d03_integration.ps1` grün. **Verbleibend für Welle 2: nur noch der gebündelte manuelle Golden-Path** im laufenden Build (`/parties`, `/documents`, `/property-documents/<id>`, `/compliance`, Cloud-Modus für Mutationen) — er ist jetzt erstmals vollständig durchspielbar. **Welle 3 ist freigegeben** und beginnt mit `P2-D05 leasing_operations`; die dafür nötige `OPN-DOM-001`-Entscheidung ist gefallen (eigene Zeile oben). **Diese Sitzung wird hier bewusst beendet** (Handoff-Regel des Ausführungsprompts: nicht eine frische Domäne durch eine überladene Sitzung drücken) — Schema und RLS sind die Dateien mit der größten Streuwirkung im Repo. Historischer Kontext, was damals offen war:

**Drei offene Punkte, die eine Nutzerentscheidung brauchen und nicht still entschieden wurden:** (a) `PropertyImagesSection` liest weiterhin das Legacy-Dokument-Repo und verletzt damit als einzige Stelle die Wellen-Abschlusszeile „kein W2-Screen nutzt mehr Legacy-Dokument-/Party-Repos" — Alternative wäre die Löschung der Objektbild-Funktion, was `propertyTitleImageProvider` für Objektkarte und Übersicht trockenlegt; (b) **es fehlt ein `DocumentUploadPort`** — ohne ihn kann der Client keine Bytes in den privaten Bucket legen, und der manuelle Golden-Path „Dokument anlegen" ist in der Cloud nicht durchspielbar (eigenes P2-D03-Folge-Increment nötig); (c) AP4 hat `test/ui/docs/documents_screen_test.dart` gelöscht, weil der getestete Legacy-Dialog durch die bestätigte Entscheidung vom 2026-07-24 entfällt — offengelegt statt still, rückholbar über `git`.

**Stand 2026-07-30, Sitzungsende.** Diese Sitzung hat **P2-D05 Increment 1 verifiziert und Increment 2 gebaut**. Ausgangslage war unklar und wurde zuerst geklärt statt angenommen: die Vorsitzung hatte Increment 1 (Migration, pgTAP, beide Rollback-Tests, CI-Wiring) um 12:00–12:10 geschrieben, dann aber **weder verifiziert noch eine Statuszeile hinterlassen** — das Handoff-Dokument sprach noch davon, P2-D05 überhaupt erst zu beginnen. Deshalb wurde der volle `database`-Gate für Increment 1 selbst nachgefahren, bevor irgendetwas Neues entstand (Ergebnis: sauber, Details in der Increment-1-Zeile). Danach Increment 2: `leasing_cases` (STM-004) und `rent_roll_snapshots`/`_lines` (AGG-007), inkl. Realtime, 116 neuer pgTAP-Tests, zwei Rollback-Tests und CI-Wiring. **Stand jetzt:** `db lint` sauber, Advisors **0 ERROR/WARN**, pgTAP **1030/1030** auf frischem Reset, **voller Rollback-Replay 022→003 (20 Schritte) grün** mit anschließendem `migration up` + erneut 1030/1030, **alle neun DB-berührenden `tool/verify_*`-Skripte grün**, `flutter analyze` sauber, volle Flutter-Suite **945 grün / 15 Skips** (unberührt — beide Increments sind reines SQL). Damit ist der `database`-Job-Gate lokal vollständig nachgefahren, nicht nur in Teilen. **Zwei Dinge bewusst benannt statt still entschieden:** (1) **`lease_rent_schedule` wurde nicht gebaut**, obwohl Increment 1s Kopf es ankündigte — es steht nicht im Backlog-Deliverable und der Rent Roll braucht es nicht. **Korrektur am selben Tag:** die Erstbegründung nannte zusätzlich `OPN-DOM-003` als offene Semantikfrage; das war falsch (`OPN-DOM-003` = Mahnstufen) und die Indexierungssemantik ist lokal bereits implementiert. Details und die *tatsächlich* offene Frage (deterministische Engine portieren vs. ihr Ergebnis speichern, `RISK-QA-001`) stehen in der Increment-2-Zeile. (2) Die geteilte Datei `supabase/tests/002_p1_003_rls.test.sql` musste um die 9 neuen `private`-Helfer erweitert werden — das ist der Zweck dieser Inventur, aber sie trägt auch Änderungen der parallelen P2-D07-Sitzung, weshalb beim Commit nur die eigenen Hunks gestaged wurden. **Nachtrag gleicher Tag, auf Nutzerwunsch weitergearbeitet: Increment 3 Teil 1 ist `done`** (Domain-DTOs, backend-agnostischer 7-Port-Vertrag, Supabase-Adapter, 21 Adaptertests — eigene Zeile oben). Volle Suite jetzt **987 grün / 15 Skips**, `analyze` sauber. Die Naht ist bewusst **noch nicht verdrahtet**: kein Provider und kein Screen liest sie, damit der Baum ohne halbfertige Zwischenzustände grün bleibt. **Nächstes Item: P2-D05 Increment 3 Teil 2** — read-only Legacy-SQLite-Adapter, Provider + `featureBackendOverrides`-Wiring, Realtime-Consumer, realer lokaler Client-Integrationstest `tool/verify_p2_d05_integration.ps1` inkl. des noch offenen **Zwei-Sitzungs-Concurrency-Tests** aus dem Backlog-Gate, Dry-Run-Mapper (faltet die Legacy-`tenants`-Tabelle nach `parties`) und CI-Step. **Ein Fallstrick dafür ist schon belegt:** `lib/data/repositories/lease_repo.dart` erzwingt lokal weiterhin „höchstens ein aktiver Vertrag je Einheit" — exakt die von `OPN-DOM-001` überstimmte Annahme; der Legacy-Adapter darf sie nicht übernehmen und sollte sie beim Lesen auch nicht als Wahrheit behandeln. **Erst danach** die Welle-3-Screens, und die brauchen laut Ausführungsprompt zuvor ein eigenes `04c_wave3_*.md`-Detaildokument. **Unverändert offen aus Welle 2:** der gebündelte manuelle Golden-Path über `/parties`, `/documents`, `/property-documents/<id>`, `/compliance` — er blockiert Welle 3 nicht, sollte aber vor dem Welle-2-Abschluss laufen; ein visueller Check sollte weiterhin warten, bis die parallele P2-D07-Sitzung die geteilten Design-System-Primitiven nicht mehr anfasst.

**Welle 2, Arbeitspakete 0 und 1 (`done`, 2026-07-28) → danach Arbeitspaket 2: ComplianceDashboardScreen (SCR-052).** AP1 hat das Muster bewiesen, das die drei Dokument-Screens übernehmen: Screen liest ausschließlich die backend-gewählten Feature-Provider, Orchestrierung in einem Phasen-Controller im `application/`-Layer, `WorkspaceSessionScope` als Identitäts-Naht, alle Pflicht-Zustände als Daten (nicht als Widget-Zweig), Mutations-Affordanzen erklären ihre Nicht-Verfügbarkeit statt zu no-oppen. AP2 erbt zusätzlich die Badge-Abbildung aus `parties/widgets/party_badges.dart` als Vorlage für die `DocumentRequirementState`-/STM-008-Badges. **Zu beachten für AP2/AP3:** ComplianceDashboard ist heute **in `DocumentsScreen` eingebettet** (`documents_screen.dart:111`), nicht eigenständig geroutet — der Umbau muss diese Einbettung erhalten; zusätzlich bekommt jeder W2-Screen laut Routen-Entscheidung eine additive Cloud-Route.

**Welle 2, Arbeitspaket 0 (`done`, 2026-07-28).** AP0 hat die Backend-Naht gelegt: beide Welle-2-Contracts sind jetzt über eigene Provider konsumierbar, deren Implementierung `featureBackendOverrides` in `lib/app_backend_wiring.dart` je `DataBackend` wählt (Supabase-Adapter in der Cloud, read-only Legacy-Adapter lokal, Realtime nur in der Cloud) — Details und Verifikation in der W2-AP0-Zeile. **Damit ist die im Detaildokument als Voraussetzung markierte Naht erfüllt; jeder folgende W2-Screen liest ausschließlich diese Provider, nie die Legacy-Repos.** Nächstes Item ist der neue **PartiesScreen** (`04b_wave2_contacts_documents.md`, Arbeitspaket 1) als Pattern-Beweis für Feature-Contract-Konsumtion inkl. aller Pflicht-Zustände (`forbidden`/`versionConflict`/„read-only bis migriert"); **davor Hard-Stop:** die additive Parties-Navigation beim Nutzer bestätigen lassen. Danach ComplianceDashboard (SCR-052) → PropertyDocuments (SCR-020) → DocumentsScreen (SCR-051), die letzten beiden per Subagent, sobald das Muster steht.

**P2-D04 `platform_audit_jobs` — Increments 1 (CTR-005-Event-Envelope), 2 (Task- + Notification-Aggregate) und 3 (ImportJob/SearchIndex) `done` (Inc 1–2 2026-07-23, Inc 3 2026-07-24), Increment 4 offen.** Details in der P2-D04-Zeile. Kurzfassung für die nächste Sitzung: Increment 1 baute die Envelope-Infrastruktur (`domain_events`-Outbox + `private.publish_domain_event` + permission-gescopetes Broadcast-Topic) und schloss beide P2-D03-Lücken; Increment 2 lieferte Task- (STM-012, AGG-019-`generated_key`-Idempotenz mit der Task-Zeile **als** Ledger) und Notification-Aggregate (recipient-adressierter Fan-out); Increment 3 lieferte das ImportJob-Aggregat (STM-013; **AGG-020 als Schema-Invariante** — kein `ready`/Commit ohne Dry-Run **und** Reconciliation, per Check-Constraint) und den abgeleiteten SearchIndex (DOM-010 „abgeleitet und keine Wahrheit": bewusst ohne Version/Audit/Receipt, mit DELETE-Pfad, content-adressiertes Upsert). DB-seitig verifiziert (pgTAP **696/696**, Rollback-Replay `015→014`+`up`, Advisors 0 ERROR/WARN, DB-berührende `tool/verify_*` grün). **Als Nächstes Increment 4:** Dart-Contract für alle P2-D04-Aggregate (tasks, notifications, import_jobs) unter `lib/features/platform_audit_jobs/{domain,application,data}` + Supabase-/Legacy-Adapter + Realtime-Consumer, der die `domain_events`-Broadcast konsumiert (Topic `workspace:<id>:<permission>`, in Increment 1 gebaut) + realer lokaler Integrationstest + CI-Step. **`todo`→`open`-Rename** für die Legacy-`tasks.status` gehört in den Task-**Dry-Run-Mapper** (Migrations-Tooling, Increment 4) — dort umzusetzen. **DEBT-009** (Scheduler-Job) bleibt offen: das Task-Aggregat steht, der serverseitige periodische Generierungs-Job fehlt noch. **Wave-Grenze:** nach P2-D04-Abschluss ist das nächste Item `04b_wave2_...md` (Screens SCR-020/051/052 + Parties-Screen) — erst dieses Wellendokument schreiben, dann umsetzen; das ist eine Wellengrenze, also vor Start beim Nutzer rückversichern.

**P2-D03 `documents_compliance` vollständig abgeschlossen (`done`, 2026-07-23).** Alle drei Increments committet/verifiziert (Increment 1 Schema/Storage-Bucket/pgTAP/Rollback `6fee362`, Increment 2 Contract/Adapter/Realtime `ea58cbf`, Increment 3 Dry-Run-Mapper/Integrationstest/CI — dieser Commit). Voller `database`-Job-Gate lokal grün (Details in der P2-D03-Zeile: `lint`/Advisors 0 ERROR/WARN, pgTAP **514/514**, Rollback-Replay `012→…→003`+`up`, **alle** `tool/verify_*`/`test_*` inkl. des neuen `verify_p2_d03_integration`, analyze sauber, Flutter-Suite **505/12**). MIG-BND-003 ist damit end-to-end abgedeckt: der Dry-Run-Mapper hasht Inhalte und verifiziert Uploads, bevor irgendein Content-Link autorisiert wird, und der reale Integrationstest belegt Signed-URL-Ablauf und workspace-scoped Storage-Isolation über die echte Storage-API. **Nächstes Item in Abhängigkeitsreihenfolge: Backend P2-D04 `platform_audit_jobs`** (`01_domain_expansion_backlog.md`; nimmt zugleich die zwei aus P2-D03 bewusst offen gelassenen Punkte auf: Per-Access-Auditing von Downloads und Cross-Table-Realtime-Invalidierung für `link`/`unlink`/`upsert_required_document`, CTR-005). Erst danach `04b_wave2_...md` (Screens SCR-020/051/052 + neuer Parties-Screen). **Wave-2-Hinweise unverändert:** Provider-/`main.dart`-Wiring von `contacts_parties`, `documents_compliance` und der `identity_access`-Members-UI kommt mit den Welle-2-Screens (heute kein Backend-Consumer); manueller Golden-Path der Members-Admin-UI (nur Supabase-Modus) weiterhin ausstehend, durch Widget-/Integrationstests abgedeckt.

**P2-D02 `contacts_parties` vollständig abgeschlossen (`done`, 2026-07-22).** Alle drei Increments committet/verifiziert (Increment 1 Schema/pgTAP/Rollback `ae80e5f`, Increment 2 Contract/Adapter `6ded44e`, Increment 3 Realtime/Dry-Run-Mapper/Integration/CI — dieser Commit). Voller `database`-Job-Gate lokal grün (`db reset`/`lint`/Advisors 0 ERROR/WARN, pgTAP **387/387**, Rollback-Replay `010→009→008→007→006→005→004→003`+`up`, `verify_p2_d02_integration` real end-to-end + `verify_p1_018`-Parität grün, analyze sauber, Flutter-Suite **443/11**). **OPEN-001** wie entschieden umgesetzt (kanonische `parties`, zeitlich begrenzbare `party_roles`, per-Rolle `party_contractor_details`-Satellit, `party_aliases`-Merge-Historie); **kein AAL2-Gate** (Parteien = Workspace-Geschäftsdaten wie Properties, `party.read`/`party.manage`-Permissions, wie P1-004) als dokumentierter Default (kein offener DEC-*/OPEN-*-Punkt). **Nächstes Item in Abhängigkeitsreihenfolge: Backend P2-D03 `documents_compliance`** (`01_domain_expansion_backlog.md` — `documents`/`document_versions`/`required_documents` + private Storage-Buckets [`MIG-BND-003`], `SignedUrlPort`, löst `DEBT-007`/`DUP-011`; eigener Funktions-Plan im Chat vor der Umsetzung, spiegelt die P1-004/P2-D01/P2-D02-Vertikale; Gate: upload→verify→supersede-Lifecycle [`STM-008`], Signed-URL-Ablauf, workspace-scoped Storage-Isolation). Danach P2-D04 `platform_audit_jobs`; `04b_wave2_...md` (Screens SCR-020/051/052 + neuer Parties-Screen) erst wenn P2-D02+P2-D03-Backends ihr Gate bestanden haben — **also nach P2-D03**. **Offene Wave-2-Hinweise (unverändert):** (1) Provider-/main.dart-Wiring von `contacts_parties` **und** `identity_access`-Members-UI kommt mit den Welle-2-Screens (kein Backend-Consumer heute); (2) manueller Golden-Path der Members-Admin-UI (nur Supabase-Modus) weiterhin ausstehend, durch Widget-/Integrationstests abgedeckt.

**Wellengrenze Welle 1 erreicht.** Alle Arbeitspakete `done`: AP1–AP8 (PropertiesScreen, PropertyShell, OverviewScreen, DashboardScreen, CreationWorkflow, Portfolios-Split, Analytics/DataQuality/Users, SettingsScreen) sowie AP9 DEBT-012 **beidseitig** — SQLite-Tombstone (siehe AP9-Zeile) **und** Supabase-Parität (Folge-Increment, `done` 2026-07-22, voller lokaler `database`-Job grün). Commit-Stand (Arbeitsbaum sauber): AP6 `6f6e7c0`, AP7 `dd045c8`, der **P1-Hardening/Reference-Slice-Batch `ec3f061`** (committet, um die Feature-Layer-Dateien für die Supabase-Parität freizuräumen), die **Supabase-Parität `cf59449`**, und der **Wellen-1-Batch `6073774`** (AP1–AP5/AP8-Screen-Redesigns + AP9-SQLite-Tombstone; zusammen committet, da PropertiesScreen ↔ Tombstone-State ↔ geteilte `nx_*`-Komponenten wechselseitig abhängig sind — feinere Aufteilung ergäbe nicht-kompilierende Zwischen-Commits). **Tombstone/Restore-UI-Widget-Test erledigt** (`test/ui/screens/properties_screen_tombstone_test.dart`, 4: Gelöscht-Badge, Restore-only-Menü, Restore-Aufruf, Lösch-Bestätigung→`tombstone`; volle Suite **350 grün / 9 Skips**). **Welle 2 vom Nutzer freigegeben (2026-07-22, „start").** Reihenfolge per `01_domain_expansion_backlog.md`: Backend **P2-D01** (läuft, siehe eigene Zeile) → P2-D02 (`contacts_parties`) → P2-D03 (`documents_compliance`) → dann `04b_wave2_...md` (Screens SCR-020/051/052 + neuer Parties-Screen) und deren Umsetzung. **Gebündelter manueller Golden-Path — `done` (2026-07-22):** Windows-Desktop-Debug-Build (`flutter run -d windows`, SQLite-Backend), per Computer-Use Schritt für Schritt geprüft und mit Screenshots belegt: Dashboard (AP4: KPI-Tiles, Attention-Liste, Wertentwicklungs-Chart mit 6M/YTD/1J) → PropertiesScreen (AP1: Tabelle als Default mit Status-Badge-Spalte, Karten-Toggle, KPI-Zeile) → CreationWorkflow (AP5: 12-Schritte-Rail mit Live-Validierung [Pflichtfeld-Fehler Objektname wurde live erkannt und inline aufgelöst], „Zur Pruefung"-Sprung, Review „Pflicht offen 0", expliziter Final-Save, Erfolgs-Screen) → neues Objekt erscheint sofort „Aktiv" in der Liste → Löschen mit AP9-Bestätigungsdialog (reversibler Wortlaut) → Objekt verschwindet aus Aktiv, erscheint in „Archivierte Objekte" mit rotem **„Gelöscht"**-Badge direkt neben einem neutral „Archiviert"-Badge (Archiv≠Tombstone-Kontrast sichtbar) → Menü bietet nur „Wiederherstellen" → Restore reaktiviert (Aktiv-Zähler 15→16, volles Aktionsmenü wieder da). Kein visueller oder funktionaler Defekt gefunden. Testobjekt „ZZ Golden-Path Test" verbleibt absichtlich tombstoned in der Dev-DB (per Design kein Hard-Delete; jederzeit restaurierbar). **Welle 1 ist damit vollständig abgeschlossen.**
