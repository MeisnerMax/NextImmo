# P0 Decision Register

| ID | Decision | Status | Evidence / reason | Revisit |
|---|---|---|---|---|
| DEC-001 | Retain Flutter/Dart for all clients and deterministic engines. | accepted | `pubspec.yaml`, `lib/core`, product roadmap | only after measured platform blocker |
| DEC-002 | Use Supabase PostgreSQL as central system of record. | accepted | relational domain, product roadmap | after reference-slice benchmark |
| DEC-003 | Start as modular monolith, not microservices. | accepted | current team/AI delivery shape and transactional domain | when independent scaling boundary is measured |
| DEC-004 | Use online-first for Web/Desktop reference slice. | accepted | sync conflict model is not yet implemented | after Phase 3 mobile pilot |
| DEC-005 | Preserve SQLite only as legacy source and optional later client cache. | superseded by DEC-024 (2026-08-06) | `lib/data/sqlite`, migration risk; review trigger "after data reconciliation succeeds" was reached — no legacy source data remains (`phase_2/04x`, 2026-08-04) | — |
| DEC-006 | Default-deny authorization must be server-enforced with RLS. | accepted | local guard cannot secure remote data | never weaken |
| DEC-007 | Missing or loading session must have no privileges. | accepted | P1-008 started in `security_state.dart`, `app_navigation.dart` and navigation/RBAC tests | verified at P1-008 completion (`00_phase_status.md`); never weaken |
| DEC-008 | Property reference slice is the first vertical cloud increment. | accepted | covers auth, tenancy, CRUD, audit and Realtime | after Phase-1 gate |
| DEC-009 | Critical updates use expected version plus mutation id. | accepted | prevents silent overwrite and duplicate retry | after concurrency tests |
| DEC-010 | Audit is append-only and atomically written with mutation. | accepted | financial/contract traceability | never weaken |
| DEC-011 | Money uses PostgreSQL `numeric` plus currency code. | accepted | current Dart `double` is unsuitable as storage contract | schema design |
| DEC-012 | Existing V1/V2 paths are dispositioned before removal. | accepted | feature flags and wrappers are active | Phase 2 UI consolidation |
| DEC-013 | PowerSync is optional and limited to selected mobile workflows. | accepted | Web support and conflicts require pilot | Phase 3 |
| DEC-014 | Legal/tax/accounting rules require external domain validation. | open | no authoritative source in repository | before relevant production feature |
| DEC-015 | Production runs in an EU region, target Frankfurt. Staging is placed in the same regulatory/geographic target region as far as the chosen Supabase environment model allows, so no cross-region architecture is introduced without cause. **This decides *where*, not *whether*: it authorizes no provisioning and no paid resource — that remains `DEC-017`.** | **accepted** (was `proposed` until 2026-08-08) | Owner decision 2026-08-08, confirming the 2026-08-02 owner decision preserved in `rescue/codex-ai-ph00-baseline` (`docs/ai/phase_01/owner_decisions.md`, `AI-PH01-DEC-001`). Rationale: low latency for German users and EU data residency | revisit only on a legal or operational requirement |
| DEC-016 | Privileged security and administration capabilities require AAL2, enforced **server-side and fail-closed** through RLS, RPC/DB guards or Supabase auth claims — never by the Flutter client alone. Not every ordinary action requires AAL2. Mandatory at least for capabilities affecting access control, memberships, roles/permissions and security-critical workspace administration; the exact capability matrix may be extended later. Passwordless email login stays the initial authentication method, and a pending `aal2` challenge keeps blocking client-side access. **The capability matrix this decision deliberately left open was extended by `DEC-025` (2026-08-12). The initial authentication method was changed by owner decision on 2026-08-10 (`STAGING-PASSWORD-AUTH-01`): email + password is the primary login; TOTP remains the AAL2 factor.** | **accepted** (was `proposed` until 2026-08-08) | Owner decision 2026-08-08, confirming the 2026-08-02 owner decision (`AI-PH01-DEC-002`). Already implemented for property mutation (restrictive RPC policy, local TOTP AAL2, fail-closed client state, all tested); the capability-by-capability rollout is implementation work, no longer an open decision | revisit only with an explicit security review |
| DEC-017 | **Exactly one isolated remote staging environment is authorized, with synthetic data only.** Covered: a separate Supabase staging project in `eu-central-1` (`DEC-015`); staging auth — passwordless magic link/PKCE, MFA/TOTP, self-signup **off**, synthetic test users created administratively; web redirects for the staging address *proven after deployment*, plus `neximmo://auth/callback` for Windows; custom SMTP for staging (auth method revised 2026-08-10 by `STAGING-PASSWORD-AUTH-01`: email + password primary, TOTP AAL2, SMTP not configured and not required for login); the staging secrets; a controlled application of the repository migrations; the GitHub environment `staging`; an explicitly enabled staging deploy workflow; a Vercel **preview** deployment on a stable address without an own domain; and `GP-STAGING-WEB` / `GP-STAGING-WINDOWS`. Vercel Deployment Protection may be switched off for the app project as far as the real browser magic-link flow requires — that makes the static client publicly *loadable* and nothing more: auth, workspace membership, RLS, permissions and AAL2 keep protecting the data. **Cost rule:** resources that fit into existing quotas at no additional cost may be created; anything that would create a non-zero cost **stops** for an explicit owner approval naming provider, plan, one-off cost, monthly cost and cancellability. **Production stays unauthorized in every form** — no production Supabase or Vercel, no production secrets or SMTP, no real customer or inventory data, no migration out of production, no `app.neximmo.de`, no custom domain, no DNS, no production alias, deployment, cutover or installer, and no "temporary" production use. | **accepted** (was `open` until 2026-08-09) | Owner decision 2026-08-09, taken on the `STAGING-FOUNDATION-01` audit and after `STAGING-PREP-01` hardened the deployment path. Authorizing the environment is not creating it: provisioning is a separate work package | revisit before any production environment |
| DEC-018 | Realtime is a workspace-scoped query invalidation signal; repository readback remains canonical and the subscription lifecycle is bound to session, workspace and MFA state. | accepted | P1-011 adapter, controller lifecycle tests and local multi-client E2E | revisit only for an offline/sync pilot |
| DEC-019 | The legacy reference migration starts as a read-only dry run with explicit workspace/actor binding, deterministic UUIDv5 IDs and canonical SHA-256 reconciliation; reports contain no raw business or PII values. | accepted | P1-012 mapper, SQLite adapter and deterministic/negative tests | before any write-capable import executor |
| DEC-020 | The first backup/restore gate is a local, schema-scoped logical PostgreSQL drill into a new disposable database; database-global Realtime publication state is rebuilt from versioned migrations and then reconciled. | accepted | P1-014 target/corruption guards, non-empty restore, SHA-256 and invariant fingerprint | before authorized sandbox/staging drill |
| DEC-021 | SQLite und Supabase werden nur ueber explizite Environment-Auswahl initialisiert; der Supabase-Referenzschnitt besitzt stabile `/properties`- und `/properties/:id`-Routen. | accepted; SQLite-Teil superseded by `DEC-024` (2026-08-06) | `lib/main.dart`, `lib/app.dart`, Kaltstart-Deep-Link-Test und Web-Build | revisit only after routing-shell consolidation |
| DEC-022 | Entitlement-Aenderungen werden ueber private, nutzergebundene Realtime-Broadcasts signalisiert; kanonische Repository-Revalidation, Reconnect und ein begrenztes Intervall bleiben autoritativ. Client-Caches werden vor der Revalidation fail-closed geleert. | accepted | P1-017 Migration, Adapter-/Controller-Tests und lokaler Zwei-Client-Entzugstest | revisit for offline/sync or measured scale limits |

| DEC-024 | SQLite wird vollständig entfernt, nicht als Legacy-Quelle oder Client-Cache erhalten. Supabase ist die einzige Datenschicht; die Offline-Startfähigkeit des Desktop-Clients entfällt ersatzlos. Zielumgebung ist bis auf Weiteres der lokale Supabase-Stack; gehosteter Betrieb bleibt durch `DEC-015`..`DEC-017` gegated und ist vor dem Shipping zu entscheiden. Löst `DEC-005` ab und hebt die Offline-Zusage in `phase_2/00_phase_2_charter.md` (Z. 21) auf. **Seit 2026-08-09 ist das Staging-Gate `DEC-017` accepted; Staging ist provisioniert und deployt automatisch. Nur Production bleibt nicht autorisiert.** | accepted | Nutzerentscheidung 2026-08-06; der Review-Trigger von `DEC-005` ist erreicht — die Legacy-Objektdaten waren reine Testdaten und wurden am 2026-08-04 bewusst entfernt, ein Datencutover ist damit gegenstandslos (`phase_2/04x`, Z. 337) | Umsetzungsplan und Gates: `phase_2/04y_p2_x02_sqlite_decommission.md` |

| DEC-025 | **AAL2 is the boundary for the whole workspace business surface, not only for privileged capabilities.** Every workspace business read and every business command requires `aal2`, enforced server-side and fail-closed in `private.is_aal2()` through the central permission helper, the seven command gates and the RLS policies. Exactly two surfaces stay reachable at `aal1`: the GoTrue authentication and MFA bootstrap (sign-in, factor listing, enrolment, challenge, verification), which does not run through these policies at all and without which no user could ever reach `aal2`; and `public.user_profiles`, which answers only about the caller's own identity row and carries no workspace data. Extends the matrix `DEC-016` deliberately left open. | **accepted** | Threat model: a compromised password alone must not expose workspace business data — the password is the first factor, workspace data follows the second. `DEC-016` scoped AAL2 to access-control capabilities, and `REMOTE-SECURITY-GATE-01` audited the RPC surface under that scope, so the read surface was never in scope for either. Measured at `fcd2dea`: **41 of 41** client-reachable policies carried no assurance check and **64 of 65** `SECURITY DEFINER` RPCs had none of their own; `005_p1_015_aal.test.sql` pinned that as intended behaviour ("AAL1 retains authorized reads"), which is why this is an architecture change and not a bug fix. Proven by 30 behavioural assertions in `supabase/tests/027`, by `SR-21`/`SR-22` in `026`, and by a 7/7 mutation matrix | revisit only with an explicit security review, as `DEC-016` |

| DEC-026 | **Zeitversionierte Mietkomponenten werden als Daten in Postgres gefuehrt und serverseitig berechnet.** `lease_components` traegt `component_type`, `valid_from`, `valid_until`, `amount`, `currency_code` nach der Hauskonvention fuer zeitversionierte Werte (`party_roles`, `documents`): CHECK `valid_until >= valid_from`, partieller Unique-Index auf die offene Zeile, Validierung im Kommando — **kein EXCLUSION-Constraint**, weil `btree_gist` im Projekt nicht installiert ist. Der Mietplan wird serverseitig **abgeleitet**, nicht als vorberechnete Ausgabe einer Clientengine gespeichert. Die Legacy-Regeln werden **nicht semantisch portiert**, sondern neu gefasst. | accepted | Die Gegenueberstellung im Kopf von `20260730120000:46-51` („nach PL/pgSQL portieren und eine zweite, driftende Implementierung erzeugen **vs.** die Engine behalten und ihr Ergebnis speichern") ist seit `AP-X02-2b` sachlich ueberholt: die Dart-Engine laeuft nicht mehr im Runtime, `lib/data/repositories/` ist Legacy ohne Laufzeitpfad und in `AP-X02-9` zur Loeschung terminiert. Eine Portierung erzeugt heute **keine** zweite lebende Implementierung. Zwei Hauspraezedenzen haben dieselbe Frage bereits gegen die Clientvariante entschieden: `P2-D05a` (Ableitung „vollstaendig serverseitig in Postgres", verworfene Alternative ausdruecklich „Ableitung im Client") und `P2-D05b`, wo die client-seitige Live-Rechnung nach einem Release **zurueckgeholt** wurde mit der Begruendung „No drift by construction, not by discipline". §28 des Enterprise-Auftrags verlangt fuer die Warmmiete ausdruecklich serverseitige Autoritaet. Die Legacy-Engine ist zudem kein zu bewahrendes Asset: 113 Zeilen, ein einziger Test, der die Indexierung **nie ausloest** (erwartet 1000 EUR in allen drei Perioden trotz aktiver CPI-Regel), keine echte Indexmiete (`annual_percent` ist Handeingabe), Cap/Floor klemmen den *Prozentsatz* statt die Miete, Regeln ohne Ablaufdatum wirken kumulativ und ewig, die Anhebung haengt am Vertragsjahrestag statt am `effective_from` der Regel, und der erste Monat eines Rebuild-Fensters kann strukturell nie steigen (`if (i > 0)`). Kein UI-Schreibpfad existiert. **Klarstellung:** `RISK-QA-001` ist ein offenes QA-Risiko ueber fehlende Golden-Master-Fixtures (`09_test_baseline.md:139`) und steht in diesem Register nicht; die Fixture-Liste enthaelt fuer die Indexierung ohnehin **keinen** Eintrag. Es gated diese Entscheidung nicht | revisit, falls eine Client-Vorschaurechnung eingefuehrt wird — die bleibt Vorschau, nie Quelle |
| DEC-027 | **`end_date` ist ein geplantes Ende, keine Beendigung.** Ein Vertrag mit `status = 'active'` ist wirksam, unabhaengig davon, ob `end_date` in der Vergangenheit liegt; jede mietfuehrende Flaeche zaehlt ihn. Beendet wird ein Vertrag ausschliesslich ueber `ended` oder `cancelled`. Fuer einen **vergangenen** Stichtag ist ein `ended`-Vertrag wirksam bis `coalesce(move_out_date, end_date, ended_at::date)`. | accepted | Es ist der **Normalfall**, nicht der Randfall: es gibt keinen Scheduler (`DEBT-009`), keinen Trigger und keinen Constraint, der `active` ueber `end_date` hinaus verhindert, und `update_lease` weist jede Aenderung an einem aktiven Vertrag ab — ein `end_date` ist also nicht einmal korrigierbar. Jeder befristete Vertrag endet damit zwangslaeufig in diesem Zustand, der zugleich die **einzige darstellbare Form der gesetzlichen Fortsetzung** ist (Fortsetzung kraft Gesetzes, Miete geschuldet). Sie auszuschliessen weist die Ist-Miete systematisch zu niedrig aus. Die Divergenz bestand real: `property_leasing_summary` zaehlt sie voll (pgTAP 036 pinnt EUR 3000 inkl. 800 aus „Abgelaufen"), `rent_roll_unit_rows`/`rent_roll_live`/`create_rent_roll_snapshot` schliessen sie ueber `end_date >= p_as_of_date` aus, `property_overview` zaehlt sie **und** meldet sie als `critical`. Sie ist unbeaufsichtigt entstanden: fuer den Rent Roll ist der Datumsfilter begruendet entschieden, fuer `property_leasing_summary` gibt es keine Festlegung. **Folgearbeiten, die aus dieser Entscheidung fallen:** die Rent-Roll-Helfer und ihre pgTAP-Pins aendern sich; `property_leasing_summary` behaelt sein Verhalten, bekommt aber den **fehlenden `start_date`-Filter** (ein noch nicht begonnener Vertrag zaehlt heute mit — das ist ein Defekt, keine Entscheidung); und `operations_signals.lease_expiry` darf den Vertrag nicht laenger genau an dem Tag verlieren, an dem er handlungsbeduerftig wird | revisit nur mit mietrechtlicher Pruefung |
| DEC-028 | **Es gibt keinen globalen „Objektwert" fuer LTV. Ein Covenant traegt eine aufloesbare Wertquellen-Bindung; LTV bleibt `null`, solange sie nicht aufloesbar ist.** Standardziel der Bindung ist eine eigene Entitaet fuer externe Wertfeststellungen mit Wertbasis, Stichtag, Waehrung, Gutachter und Dokumentbezug. Kein Covenant rechnet gegen eine Zahl, die das Produkt selbst geschaetzt hat. | accepted | `METHOD-GOV-01` laesst die Frage nicht nur offen, es **verbietet jede heute verfuegbare Zahl**: der einzige gespeicherte Wertbetrag der Cloud ist `market_value_opinions.amount`, erzeugt von genau der `ReconciliationEngine`, die `METHOD-GOV-01` §5.1 als BLOCKED fuehrt. `valuation_cases` traegt weder Stichtag noch Waehrung noch `basis_of_value`; `approved` ist eine reine Workflowfreigabe ohne Reportpflicht; Varianten haben **keine** primary/selected-Kennzeichnung. Der Legacy-Weg `property_kpi_snapshots.valuation` kommt ausschliesslich per CSV (`upsertSnapshot` hat null Aufrufer). Marktkonvention stuetzt die Bindung statt eines Standards: die deutsche Praxis kennt Marktwert/Verkehrswert (§ 194 BauGB) **und** Beleihungswert (§ 16 PfandBG/BelWertV) nebeneinander, europaeische REF-Vertraege arbeiten mit Market Value nach RICS Red Book VPGA 2, wobei der Kreditgeber den Gutachter instruiert und die Bewertung oft als „conclusive evidence" gilt. §14.4 des Auftrags verlangt bereits fuer NOI loan-spezifische Definitionen statt globaler Hardcodes; fuer die Wertbasis gilt das erst recht, weil dort zusaetzlich Stichtag, Frequenz und Gutachter vertraglich variieren. Es existieren noch **keine** `loans`/`covenants`-Tabellen — die Entscheidung faellt ohne Migrationsschuld | revisit, sobald `METHOD-GOV-01` eine hausinterne Wertbasis freigibt: sie wird dann eine *waehlbare Quelle*, nie ein Zwang |
| DEC-029 | **Die drei flachen Geldspalten auf `public.leases` bleiben und bedeuten die bei Vertragsschluss vereinbarten Betraege. `lease_components` traegt den zeitlichen Verlauf.** Hat ein Komponententyp fuer einen Vertrag mindestens eine Zeile, sind die Komponenten fuer diesen Typ ueber die **gesamte** Vertragslaufzeit autoritativ; ein Datum innerhalb der Laufzeit ohne deckende Zeile ist eine **Luecke** und wird als unbekannt gemeldet — **niemals** aus der Inzeptionsspalte gefuellt. Schreibseitig: keine Ueberlappung je Typ (partieller Unique-Index auf die offene Zeile plus Pruefung im Kommando, nach der Konvention von `party_roles`/`documents`), keine Luecke innerhalb der Laufzeit beim Inkraftsetzen. Leseseitig traegt eine Luecke sich fort wie Waehrungskonflikt und Vorlaeufigkeit: verweigern oder als unvollstaendig markieren, nie darum herum summieren. | accepted | Der naheliegende Weg — die flachen Spalten als serverseitig gepflegten „aktuellen Stand" zu fuehren — wurde **verworfen**. Eine abgeleitete Spalte ist nur sicher, wenn sie ausser durch ihre Ableitung nicht beschreibbar ist; hier ist sie es: `create_lease` nimmt alle drei als Parameter, sie sind `NOT NULL`, und `update_lease` weist Aenderungen an aktiven Vertraegen ab — die Pflege muesste also per Trigger an der Kommandoflaeche vorbeilaufen. Ein verborgener Schreibpfad ist in einem auditierten System schlimmer als gar kein Cache: der Audit-Trail zeigte keine Mutation, waehrend sich die Zahl aendert. Die gewaehlte Trennung erzeugt dagegen keine zwei Wahrheiten fuer eine Zahl, weil Spalten und Komponenten **verschiedene Fragen** beantworten (was wurde vereinbart / was gilt wann). Das Luecken-Verbot ist die Lehre aus vier Faellen an einem Tag, in denen eine gruene Testsuite eine still falsche Zahl gedeckt hat: eine Luecke aus der Inzeptionsspalte zu fuellen liesse die Anfangsmiete fuer einen Zeitraum wiederauferstehen, fuer den die Parteien etwas anderes vereinbart haben. **Korrektur am selben Tag, bevor gebaut wurde:** die erste Fassung dieser Entscheidung schrieb die `party_roles`-Konvention vor, mit der Begruendung, ein EXCLUSION-Constraint stehe nicht zur Verfuegung. Die Praemisse war falsch — `btree_gist` ist nicht installiert, aber **verfuegbar (1.7)**. Und die Konvention hat ein belegtes Loch: der partielle Unique-Index greift ausschliesslich bei zwei **offenen** Zeilen; zwei ueberlappende **befristete** Zeitraeume verhindert er nicht, und `assign_party_role` prueft eine befristete neue Zeile gegen gar nichts (`20260722220000:1259-1276` prueft nur `if p_valid_until is null`). Bei Geldkomponenten hiesse das zwei verschiedene Mieten fuer denselben Tag, lautlos. Deshalb gilt hier: **EXCLUSION-Constraint ueber `btree_gist`** fuer die Ueberschneidungsfreiheit (deklarativ, luecken- und rennsicher) plus `pg_advisory_xact_lock` im Kommando fuer die Lueckenpruefung — letzteres nach dem neuesten Hausmuster aus FINANCE-01b/PROPERTY-MEDIA-01, nicht nach dem aelteren `select ... for update` aus P2-D02. Die Abweichung von der bestehenden Konvention ist bewusst und gilt nur fuer diese Tabelle; `DEC-026` wird dadurch in diesem Punkt praezisiert, nicht aufgehoben **Nachtrag 2026-09-07, Owner-Entscheidung: die Lueckenpruefung ist ein gemeldeter Unvollstaendigkeitszustand, kein Schreibverbot.** Die urspruengliche Fassung verlangte schreibseitig "keine Luecke innerhalb der Laufzeit beim Inkraftsetzen" mit `pg_advisory_xact_lock`. Beim Bau der Eingabeflaeche zeigte sich, dass diese Form die natuerliche Eingabereihenfolge unmoeglich macht: die **erste** Periode eines Typs kann eine Laufzeit nie sofort abdecken, ein strenger Check wiese also den allerersten Bestandteil jedes Typs zurueck. Der Owner hat deshalb entschieden, die Luecke zu **melden** statt den Schreibvorgang zu verweigern — was die Leseseite dieser Entscheidung ohnehin schon verlangte ("verweigern oder als unvollstaendig markieren"). Umgesetzt als `LEASING-COMPONENTS-01c` (Migration 57): `lease_components_as_of` liefert `coverage` je Vertrag und je Typ mit Historie — `in_force`, `gap_count`, erste Luecke und die bis zum Stichtag **offene** Luecke. Zwei Faelle wurden dadurch sichtbar, die vorher beide wie "nie erfasst" aussahen: ein Typ, der erfasst war und heute fehlt, und eine Luecke **anderswo** im Zeitraum, die am Stichtag gar nicht auffaellt. Das Summenverbot ist damit vollstaendig: eine Summe wird nicht mehr gebildet, wenn ein Typ mit Historie am Stichtag fehlt — bis dahin liess sie ihn stillschweigend weg. Das Fenster endet **am Stichtag**, nie spaeter: ein befristeter Vertrag ist nicht unvollstaendig fuer seine eigene Zukunft. `pg_advisory_xact_lock` entfaellt damit ersatzlos; die Ueberschneidungsfreiheit bleibt deklarativ ueber die EXCLUSION-Constraint | revisit, wenn die Inzeptionsspalten abgeloest werden sollen: das ist ein eigenes, spaetes Paket und keine Nebenwirkung von V-2 |
| DEC-030 | **Synthetische Daten duerfen auf Staging wiederholbar erzeugt werden — ueber einen zweiten kontrollierten Workflow, nicht von Hand.** Der Weg ist `.github/workflows/staging_seed.yml`, gebaut wie `staging_db_deploy.yml`: nur aus `main` dispatchbar, eigene Owner-Kill-Switch-Variable, die vier CI-Checks gruen auf dem dispatchten Commit, der Project-Ref gegen den dokumentierten Staging-Ref gepinnt, Ausfuehrung ueber `supabase db query --linked -f` (Management-API — kein zusammengebauter Connection-String, kein DB-Passwort in argv). **Unveraendert verboten bleiben:** echte Daten, Production in jeder Form, das Aufheben von `[db.seed] enabled = false`, der Service-Role-Key im Client und Handanfassen an Staging. | accepted | Owner-Entscheidung 2026-09-06. Anlass ist eine strukturelle Luecke: fuer ein cloud-only Produkt (`DEC-024`) ist ein Entwicklerrechner die falsche Vorfuehrflaeche, und genau dort lag der einzige belastbare Datenbestand. `DEC-017` hat synthetische Daten auf Staging immer erlaubt, und Runbook-Schritt 17 („synthetische Golden-Path-Daten erzeugen") wurde am 2026-08-10 ausgefuehrt — die Verbotsliste des Runbooks nennt ausdruecklich **echten** Datenseed. Es fehlte also nie die Erlaubnis, sondern der wiederholbare Mechanismus. **Zwei Auflagen, die aus der Entscheidung folgen:** (1) der Seed ist **additiv** und fasst die bestehende Golden Property (v11, 10 `audit_events`, 0 Storage-Objekte) nicht an — mehrere Closeouts beweisen an ihr, dass eine Sicherheitsuebung keine Geschaeftsdaten mutiert hat, und diese Beweise duerfen nicht nachtraeglich entwertet werden; (2) die Golden Baseline wird nach dem ersten Seed **neu gefasst statt aufgegeben**, damit „unveraendert"-Nachweise moeglich bleiben. Der Workflow faehrt deshalb einen rein lesenden Inventarmodus, der vor jedem Schreiben allein laufen kann | revisit nur, wenn Staging jemals andere als synthetische Daten fuehren soll — was diese Entscheidung ausdruecklich nicht erlaubt |

| DEC-023 | Bewertungsvarianten werden über zwei eigene Spalten auf `valuation_cases` gruppiert (`variant_group_id`, `variant_label`), nicht über den Legacy-Szenariobegriff und nicht über Titel-Konventionen. Eine Variante ist ein vollwertiger Fall mit eigener Version, eigenem Status und eigenem Bericht. | accepted | Nutzerentscheidung 2026-07-30; `scenario_id` als Gruppierung würde die gerade entkoppelte Legacy-Semantik wieder einführen, Titel-Konventionen wären implizite Semantik ohne Constraint | revisit only if variants ever need to differ structurally from a case |

## Open Decision Rule

Open decisions do not block independent local work. Each implementation backlog item must name any decision that must be resolved before merge or deployment.

## Central Open-Decision Index

This index is authoritative for completeness; details and defaults remain in the named source artefact.

| IDs | Topic | Source | Latest blocking point |
|---|---|---|---|
| OPEN-001..OPEN-005 | Party model, legacy users, dead screens, acquisition model, property-type scope | `04_duplicate_and_debt_register.md` | Phase 1 routing or Phase 2/5 schema as specified |
| OPN-DOM-001..OPN-DOM-005 | Lease, ownership, dunning, approvals, retention | `02_domain_map.md` | relevant module contract |
| DEC-SEC-001..DEC-SEC-005 | role matrix, scope, PII, retention, upload policy | `07_security_and_tenancy_baseline.md` | RLS/upload/import pilot as specified |
| DEC-SYN-001..DEC-SYN-005 | offline engine, field scope, tombstones, merge UX, checklist conflicts | `08_sync_conflict_matrix.md` | Phase 3 offline pilot |
| DEC-014 | external domain validation | this register | relevant later feature |

`DEC-015` (region) and `DEC-016` (privileged AAL2) left this index on 2026-08-08, `DEC-017`
on 2026-08-09; all three are `accepted` above.

**No open decision gates a remote *staging* environment any more.** `DEC-017` authorizes
exactly one, for synthetic data only — and authorizing is not creating: at the time of this
entry no remote environment exists and provisioning has not started. Production is a
different question and remains unauthorized; `DEC-017` says so explicitly rather than
leaving it to inference.

Update 2026-08-23: the staging environment was provisioned on 2026-08-09 (`STAGING-PROVISION-01`),
its auth configured on 2026-08-10 (`STAGING-PASSWORD-AUTH-01`) and it deploys automatically since
2026-08-10 (`STAGING-DEPLOY-ACTIVATION-01`); see `phase_0/00_phase_status.md` and
`cloud/05_phase_a_log.md`. Production remains unauthorized.

Where older documents name `DEC-015`..`DEC-017` together as a gate, or `DEC-017` alone as
open, those are point-in-time records and are deliberately not rewritten.
